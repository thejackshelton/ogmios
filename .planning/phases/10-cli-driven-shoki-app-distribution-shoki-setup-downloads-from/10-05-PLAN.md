---
phase: 10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from
plan: 05
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - README.md
  - CHANGELOG.md
  - docs/getting-started/install.md
  - docs/getting-started/permission-setup.md
  - docs/getting-started/vitest-quickstart.md
  - docs/getting-started/ci-quickstart.md
  - docs/api/sdk.md
  - docs/api/cli.md
  - docs/api/vitest.md
  - docs/api/matchers.md
  - docs/guides/matchers.md
  - docs/guides/migration-from-guidepup.md
  - docs/guides/troubleshooting.md
  - docs/background/architecture.md
  - examples/vitest-browser-qwik/README.md
  - .planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "README.md and every docs/ page show `npm install shoki` (or `pnpm add -D shoki`) as the install command — never `@shoki/sdk` or `@shoki/vitest` for end users."
    - "Every code snippet across docs/ imports from `shoki`, `shoki/matchers`, `shoki/vitest`, `shoki/vitest/setup`, or `shoki/vitest/browser`. Zero `@shoki/sdk` or `@shoki/vitest` imports remain in any docs/example markdown."
    - "The first-run DX flow is documented explicitly: `npm install shoki` → `npx shoki setup` → click through dialogs → done. README + getting-started/install.md + getting-started/permission-setup.md all describe this same path consistently."
    - "CHANGELOG.md [Unreleased] section documents Phase 10's three breaking changes: package rename, vitest collapse, app distribution split."
    - "Final end-to-end verification: `pnpm -r typecheck && pnpm -r test` exits 0; `cd docs && pnpm install --ignore-workspace && pnpm build` exits 0; `node packages/sdk/dist/cli/main.js setup --help` lists every Phase 10 flag; `npm pack --dry-run` on binding packages shows zero helper/ entries."
    - "10-SUMMARY.md exists and captures the final 3-package shape, the new release cadence (v* for SDK, app-v* for helper), and the consumer-facing DX."
  artifacts:
    - path: "README.md"
      provides: "Top-of-readme install block + 3-package architecture diagram"
      contains: "npm install shoki"
    - path: "docs/getting-started/install.md"
      provides: "End-user install + first-run flow page"
      contains: "npx shoki setup"
    - path: "CHANGELOG.md"
      provides: "Phase 10 breaking-change entries under [Unreleased]"
      contains: "Phase 10"
    - path: ".planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-SUMMARY.md"
      provides: "Phase-level summary across all 5 plans"
  key_links:
    - from: "README.md"
      to: "docs/getting-started/install.md"
      via: "Documentation section linking to install page"
      pattern: "getting-started/install"
    - from: "docs/getting-started/install.md"
      to: "shoki setup CLI"
      via: "First-run flow instructions"
      pattern: "npx shoki setup"
---

<objective>
Sweep README + every docs/ page + CHANGELOG.md to reflect the Phase 10 reality: 3 packages, single-name install (`npm install shoki`), CLI-driven first-run flow (`npx shoki setup`), `.apps` no longer in npm tarballs. Then run a final end-to-end verification gate (typecheck + tests + docs build + CLI smoke + tarball inspection) and write the phase SUMMARY.

Purpose: Plans 01-04 ship the code + workflows. This plan ships the user-facing story so consumers actually find it. Without docs sweep, Phase 10's DX win is invisible. Without the verification gate + SUMMARY, the phase isn't done.

Output: Documentation reflecting the 3-package world, all imports + install commands updated; CHANGELOG entries for Phase 10; final verification gate passes; 10-SUMMARY.md committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-CONTEXT.md
@.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-01-SUMMARY.md
@.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-02-SUMMARY.md
@.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-03-SUMMARY.md
@.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-04-SUMMARY.md
@README.md
@CHANGELOG.md
@docs/getting-started/install.md

<interfaces>
<!-- Substitution table for ALL markdown files (docs/, README.md, examples/*/README.md): -->

| Old text                                  | New text                              |
|-------------------------------------------|---------------------------------------|
| `@shoki/sdk`                              | `shoki`                               |
| `@shoki/vitest`                           | `shoki/vitest`                        |
| `@shoki/sdk/matchers`                     | `shoki/matchers`                      |
| `@shoki/vitest/setup`                     | `shoki/vitest/setup`                  |
| `@shoki/vitest/browser`                   | `shoki/vitest/browser`                |
| `pnpm add -D @shoki/sdk @shoki/vitest`    | `pnpm add -D shoki`                   |
| `npm install @shoki/sdk @shoki/vitest`    | `npm install shoki`                   |
| `npm i @shoki/sdk`                        | `npm i shoki`                         |
| `4 npm packages`                          | `3 npm packages`                      |
| `4-package-world`                         | `3-package-world`                     |
| `7 → 4` (only Phase-10-relevant cases)    | `7 → 4 → 3`                           |

EXCEPTIONS — do NOT rewrite:
- The CHANGELOG.md entries from Phases 8 and 9 that describe the historical 7→4 consolidation. Leave history intact; add NEW entries for the 4→3 + .app split.
- `@shoki/binding-darwin-arm64` and `@shoki/binding-darwin-x64` references — those package names DO still exist (scoped, optionalDependencies of `shoki`).

<!-- First-run flow snippet that should appear in README.md AND docs/getting-started/install.md verbatim: -->

```bash
# Install (one command, one package)
npm install shoki

# First-run setup: downloads Shoki.app from GitHub Releases, installs to
# ~/Applications/, strips quarantine, opens the macOS TCC permission flow.
npx shoki setup

# Verify everything is wired
npx shoki doctor
```

<!-- CHANGELOG.md entries to add under [Unreleased]: -->

```markdown
### Changed

- **BREAKING — package rename:** `@shoki/sdk` is now published as the unscoped
  package `shoki`. Update imports: `import { voiceOver } from 'shoki'`. The
  scoped name is gone — the npm registry slot for `shoki` was confirmed
  available and claimed in this release. (Phase 10 Plan 01)
- **BREAKING — `@shoki/vitest` collapsed into `shoki`:** the Vitest plugin
  now lives at the subpaths `shoki/vitest`, `shoki/vitest/setup`, and
  `shoki/vitest/browser`. Update consumers:
  - `import { shokiVitest } from '@shoki/vitest'` → `from 'shoki/vitest'`
  - `import '@shoki/vitest/setup'` → `import 'shoki/vitest/setup'`
  - `import { voiceOver } from '@shoki/vitest/browser'` → `from 'shoki/vitest/browser'`
  - Drop `@shoki/vitest` from `devDependencies` — it's now a subpath of `shoki`.
  vitest and @vitest/browser are now OPTIONAL peer deps of `shoki`. (Phase 10 Plan 01)
- **3 packages total** (down from 4): `shoki` + `@shoki/binding-darwin-arm64`
  + `@shoki/binding-darwin-x64`. The platform bindings stay scoped — the
  napi platform-package pattern requires it. (Phase 10 Plan 01)

### Added

- **`shoki setup` is now a real download flow.** Previously a path resolver,
  it now: (1) detects Shoki.app + Shoki Setup.app in `~/Applications/`,
  (2) downloads `shoki-darwin-<arch>.zip` from GitHub Releases when missing
  or out-of-date, (3) verifies SHA256 against the published `.sha256`
  sidecar, (4) unzips into `~/Applications/`, (5) strips `com.apple.quarantine`
  via `xattr`, (6) launches Shoki Setup.app for the one-time TCC dialog.
  New flags: `--force`, `--no-download`, `--install-dir <path>`,
  `--skip-launch`, `--json`, `--version <ver>`. No new runtime dependencies
  — uses Node 24 native `fetch` + `crypto.createHash('sha256')`. (Phase 10 Plan 02)
- **GitHub Releases workflow for helper bundles** (`.github/workflows/app-release.yml`)
  triggered by `app-v*` tags. Publishes `shoki-darwin-arm64.zip`,
  `shoki-darwin-x64.zip`, and matching `.sha256` files. SDK release cadence
  (`v*` tags) and helper release cadence (`app-v*` tags) are now independent.
  (Phase 10 Plan 03)

### Removed

- **`.app` bundles no longer ship inside `@shoki/binding-darwin-*` tarballs.**
  Binding packages now contain only `shoki.node`, dropping tarball size from
  ~10MB to ~2MB. The .apps live on GitHub Releases and are fetched on demand
  by `npx shoki setup`. (Phase 10 Plan 04)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep README + all docs/ + CHANGELOG for the new package shape and first-run flow</name>
  <read_first>
    - README.md
    - CHANGELOG.md
    - docs/getting-started/install.md
    - docs/getting-started/permission-setup.md
    - docs/getting-started/vitest-quickstart.md
    - docs/getting-started/ci-quickstart.md
    - docs/api/sdk.md
    - docs/api/cli.md
    - docs/api/vitest.md
    - docs/api/matchers.md
    - docs/guides/matchers.md
    - docs/guides/migration-from-guidepup.md
    - docs/guides/troubleshooting.md
    - docs/background/architecture.md
    - examples/vitest-browser-qwik/README.md
  </read_first>
  <files>
    README.md,
    CHANGELOG.md,
    docs/getting-started/install.md,
    docs/getting-started/permission-setup.md,
    docs/getting-started/vitest-quickstart.md,
    docs/getting-started/ci-quickstart.md,
    docs/api/sdk.md,
    docs/api/cli.md,
    docs/api/vitest.md,
    docs/api/matchers.md,
    docs/guides/matchers.md,
    docs/guides/migration-from-guidepup.md,
    docs/guides/troubleshooting.md,
    docs/background/architecture.md,
    examples/vitest-browser-qwik/README.md
  </files>
  <action>
    Apply the substitution table from `<interfaces>` to every file in `<files>`. Specifics per file:

    `README.md`:
    - Replace the "Shoki ships **4 npm packages**" section with a "Shoki ships **3 npm packages**" version listing: `shoki`, `@shoki/binding-darwin-arm64`, `@shoki/binding-darwin-x64`. The bindings are described as "platform binary, auto-installed via optionalDependencies — never installed by hand".
    - Update the architecture ASCII diagram: change `@shoki/sdk (TS)` to `shoki (TS)` and the surrounding text.
    - Replace the npm shield URL `npmjs.com/package/@shoki/sdk` with `npmjs.com/package/shoki` and the alt text `@shoki/sdk` → `shoki`.
    - Add the first-run flow snippet from `<interfaces>` near the top of the README, immediately after "What is this?" — this is the canonical install paragraph users see first.

    `docs/getting-started/install.md`:
    - Replace the "Install the packages" section to show `npm install shoki` (or `pnpm add -D shoki`) as the single command.
    - Replace the table of two packages with a one-row table: `shoki` — Core SDK + CLI + matchers + Vitest plugin.
    - Update the "Run the pre-flight check" section to use `npx shoki setup` BEFORE `npx shoki doctor` and explain that setup downloads + installs the helper apps on first run.
    - Update the "Verify the install" snippet: change `require('@shoki/sdk')` to `require('shoki')`.
    - Update the "Why just two packages?" section title and body to "Why one package?" and explain the consolidation.
    - Update the "Heads up (v1.1)" admonition to mention the additional v1.1 consolidation: `@shoki/sdk` → `shoki` + `@shoki/vitest` collapsed into `shoki/vitest` subpaths.

    `docs/getting-started/permission-setup.md`:
    - Update the install + setup section to use `shoki` and `npx shoki setup` (now a download flow, not a path resolver).
    - Add a callout box explaining that on first run, `npx shoki setup` downloads ~10MB from GitHub Releases. Document the SHA256 verification + quarantine strip steps in user-friendly language.

    `docs/getting-started/vitest-quickstart.md`:
    - Update install command + every code snippet's imports per the substitution table.
    - The `vitest.config.ts` snippet: `import { shokiVitest } from 'shoki/vitest'` (was `@shoki/vitest`).
    - The setup file snippet: `import 'shoki/vitest/setup'`.
    - The browser test snippet: `import { voiceOver } from 'shoki/vitest/browser'`.

    `docs/getting-started/ci-quickstart.md`: substitution table sweep only.

    `docs/api/sdk.md`: every "from `@shoki/sdk`" → "from `shoki`"; install snippets updated; mention that the CLI ships in the same package via the `shoki` bin.

    `docs/api/cli.md`: document the new `shoki setup` flag matrix from Plan 02 (`--force`, `--no-download`, `--install-dir`, `--skip-launch`, `--json`, `--version`). Include a fully-worked example of `--json` output structure (paste the SetupResult interface from Plan 02). Document the download URL pattern `github.com/<owner>/shoki/releases/download/app-v<version>/shoki-<platform>.zip`.

    `docs/api/vitest.md`: every import updated to `shoki/vitest*` subpaths; example configs updated.

    `docs/api/matchers.md`: every import updated to `shoki/matchers`.

    `docs/guides/matchers.md`: substitution table sweep + verify the side-by-side import snippets show the new subpath form.

    `docs/guides/migration-from-guidepup.md`: this is high-traffic; ensure every shoki-side code snippet uses the new imports. Add a short callout: "If you were on shoki ≤ 1.0.x, see CHANGELOG for the package rename."

    `docs/guides/troubleshooting.md`: substitution table sweep; if there's a "shoki setup fails" entry, expand it with the new error modes (download failure, sha mismatch, no-network with `--no-download`).

    `docs/background/architecture.md`: substitution table sweep; if it has the "X packages total" diagram, update to 3.

    `examples/vitest-browser-qwik/README.md`: substitution table sweep + reflect that the example now has a single `shoki` workspace dep.

    `CHANGELOG.md`:
    - Locate the `[Unreleased]` section heading. Update the heading to `## [Unreleased] — Phase 10 (v1.1 prep, continued)`.
    - INSERT the entire Changed/Added/Removed block from `<interfaces>` at the top of the [Unreleased] section, BEFORE the existing Phase 8 + 9 entries. Keep all existing entries intact.

    Final pass: do a repo-wide grep for `@shoki/sdk` and `@shoki/vitest` across `docs/`, `README.md`, `examples/*/README.md`, and `CHANGELOG.md`. The ONLY allowed matches are inside historical Phase-8 / Phase-9 CHANGELOG entries that describe the prior consolidation history.
  </action>
  <verify>
    <automated>
      cd /Users/jackshelton/dev/open-source/shoki &amp;&amp; \
      grep -q "npm install shoki" README.md &amp;&amp; \
      grep -q "npx shoki setup" README.md &amp;&amp; \
      grep -q "3 npm packages" README.md &amp;&amp; \
      grep -q "npm install shoki" docs/getting-started/install.md &amp;&amp; \
      grep -q "npx shoki setup" docs/getting-started/install.md &amp;&amp; \
      grep -q "shoki/vitest/setup" docs/getting-started/vitest-quickstart.md &amp;&amp; \
      grep -q "shoki/vitest/browser" docs/getting-started/vitest-quickstart.md &amp;&amp; \
      grep -q "Phase 10" CHANGELOG.md &amp;&amp; \
      grep -q "shoki-darwin-" docs/api/cli.md &amp;&amp; \
      grep -q "\-\-no-download" docs/api/cli.md &amp;&amp; \
      grep -q "\-\-install-dir" docs/api/cli.md &amp;&amp; \
      ! grep -rE "@shoki/(sdk|vitest)([^-]|$)" README.md docs/ examples/vitest-browser-qwik/README.md --include='*.md' | grep -v "CHANGELOG\|/CHANGELOG" | grep -v "Phase 8\|Phase 9\|prior version\|previously\|formerly\|was published\|was \`@shoki" &amp;&amp; \
      ! grep -E "@shoki/(sdk|vitest)" CHANGELOG.md | grep -v -E "Phase 8|Phase 9|previously|was published|was \`@shoki|formerly"
    </automated>
  </verify>
  <done>
    - README + every docs/ markdown file uses `shoki` (not `@shoki/sdk` or `@shoki/vitest`) in install commands and code snippets
    - First-run flow snippet appears in README and docs/getting-started/install.md
    - CHANGELOG.md has Phase 10 entries under [Unreleased] before Phase 8/9 entries
    - docs/api/cli.md documents all 6 new `shoki setup` flags with example outputs
    - Repo-wide grep confirms zero `@shoki/sdk` or `@shoki/vitest` outside of historical CHANGELOG entries
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification gate + 10-SUMMARY.md</name>
  <read_first>
    - All Plan 01-04 SUMMARYs
    - README.md (post-Task-1 sweep)
    - docs/getting-started/install.md (post-Task-1 sweep)
  </read_first>
  <files>
    .planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-SUMMARY.md
  </files>
  <what-built>
    Phase 10 ships:
    - 3-package monorepo (`shoki` + 2 scoped bindings); `@shoki/sdk` and `@shoki/vitest` no longer exist
    - `shoki setup` as a real GitHub-Releases download flow with SHA256 verify + quarantine strip + auto-launch
    - `.github/workflows/app-release.yml` that publishes `shoki-darwin-<arch>.zip` + `.sha256` on `app-v*` tags
    - Slimmed binding tarballs (no `helper/**` entries, ~2MB instead of ~10MB)
    - Docs + CHANGELOG sweep covering the new install + first-run + release cadence story
  </what-built>
  <how-to-verify>
    Run these in order from `/Users/jackshelton/dev/open-source/shoki`. Each must exit 0 (or as noted).

    1. **Package shape — Plan 01:**
       ```
       ls packages/        # Expect: binding-darwin-arm64  binding-darwin-x64  sdk
       ls packages/vitest 2>&1 | grep -q "No such file or directory"  # exit 0
       jq -r '.name' packages/sdk/package.json   # Expect: shoki
       jq -e '.exports."./vitest" and .exports."./vitest/browser" and .exports."./vitest/setup"' packages/sdk/package.json   # exit 0
       jq -e '.peerDependenciesMeta.vitest.optional and .peerDependenciesMeta."@vitest/browser".optional' packages/sdk/package.json   # exit 0
       ```

    2. **Build + test gate:**
       ```
       pnpm install
       pnpm -r typecheck    # exit 0
       pnpm -r test          # exit 0 (darwin host required for VO integration tests; CI gates on SHOKI_INTEGRATION env)
       ```

    3. **CLI shape — Plan 02:**
       ```
       pnpm --filter shoki build
       node packages/sdk/dist/cli/main.js --version    # prints sdk version
       node packages/sdk/dist/cli/main.js setup --help # lists --force, --no-download, --install-dir, --skip-launch, --json, --version
       node packages/sdk/dist/cli/main.js setup --dry-run --install-dir /tmp/shoki-fake-applications  # prints planned URL + path, no fs/network
       node packages/sdk/dist/cli/main.js setup --no-download --install-dir /tmp/shoki-empty-$$  # exits non-zero with clear error
       ```

    4. **Release workflow shape — Plan 03:**
       ```
       test -f .github/workflows/app-release.yml
       grep -q "tags: \['app-v\*'\]\|tags: \[\"app-v\*\"\]" .github/workflows/app-release.yml
       grep -q "package-app-zip.sh" .github/workflows/app-release.yml
       grep -q "gh release create" .github/workflows/app-release.yml
       test -x helper/scripts/package-app-zip.sh
       ```

       Local sanity (darwin only):
       ```
       cd helper && ./scripts/build-app-bundle.sh && ./scripts/package-app-zip.sh --arch arm64
       test -f helper/.build/shoki-darwin-arm64.zip && test -f helper/.build/shoki-darwin-arm64.zip.sha256
       head -c 64 helper/.build/shoki-darwin-arm64.zip.sha256 | grep -qE "^[0-9a-f]{64}$"
       ```

    5. **Binding tarball shape — Plan 04:**
       ```
       jq -e '.files == ["shoki.node", "README.md", "LICENSE"]' packages/binding-darwin-arm64/package.json
       jq -e '.files == ["shoki.node", "README.md", "LICENSE"]' packages/binding-darwin-x64/package.json
       (cd packages/binding-darwin-arm64 && npm pack --dry-run --json | jq -r '.[0].files[].path' | grep -c "^helper/")  # expect 0
       (cd packages/binding-darwin-x64  && npm pack --dry-run --json | jq -r '.[0].files[].path' | grep -c "^helper/")  # expect 0
       ! grep -E "packages/binding-darwin-(arm64|x64)/helper/" .github/workflows/release.yml
       ```

    6. **Docs build — Plan 05:**
       ```
       cd docs && pnpm install --ignore-workspace && pnpm build && cd -
       ```

    7. **Docs sweep — Plan 05:**
       ```
       grep -q "npm install shoki" README.md && grep -q "npx shoki setup" README.md
       grep -q "shoki/vitest/setup" docs/getting-started/vitest-quickstart.md
       grep -q "Phase 10" CHANGELOG.md
       ```

    Then read `.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-SUMMARY.md` (after Task 2 writes it). Confirm it accurately captures all 5 plans.

    Approve when all commands exit as expected. If anything fails, describe which step + the actual output.
  </how-to-verify>
  <action>
    After running the verification commands and confirming each, write `.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-SUMMARY.md` with the following sections:

    1. **Phase outcome (1 paragraph):** What changed for users — `npm install shoki` + `npx shoki setup` is the entire install. 3 packages total. .apps fetched on demand.
    2. **Plan-by-plan recap:** One paragraph per plan (01-05) summarizing what shipped + the verification proof.
    3. **Final package shape:** Tree view of `packages/` + the `shoki` package.json `exports` + `peerDependencies` blocks.
    4. **Final release cadence:** `v*` tags publish SDK + bindings to npm via OIDC. `app-v*` tags publish helper bundles to GitHub Releases. `compatibleAppVersion` in `packages/sdk/package.json` couples the two.
    5. **Verification evidence:** Paste of the command outputs from `<how-to-verify>` (truncate long outputs, but include exit codes + key lines).
    6. **Carry-forward:** Anything deferred or noted for v1.1+ (homebrew cask, `shoki update-app`, telemetry — all from CONTEXT.md `<deferred>`).
    7. **Done checklist:** All 5 plans' SUMMARY.md files committed; ROADMAP.md Phase 10 row marked complete; STATE.md updated with new "Last activity" + decision entries.

    The SUMMARY is the artifact future planners read for Phase 11+ — make it dense and copy-pasteable.
  </action>
  <resume-signal>Type "approved" once all verification commands pass and 10-SUMMARY.md is written. If any step fails, describe the failure + the command + the actual output.</resume-signal>
</task>

</tasks>

<verification>
Final phase-level gate (already covered in Task 2):
- `pnpm -r typecheck` exits 0
- `pnpm -r test` exits 0
- `cd docs && pnpm install --ignore-workspace && pnpm build` exits 0
- `node packages/sdk/dist/cli/main.js setup --help` lists every Phase 10 flag
- `npm pack --dry-run` on binding packages shows zero `helper/` entries
- README + docs sweep grep confirms no stale `@shoki/sdk` / `@shoki/vitest` references outside CHANGELOG history
- 10-SUMMARY.md exists and captures all 5 plans
</verification>

<success_criteria>
- Every consumer-facing markdown reflects the 3-package, single-install reality
- CHANGELOG.md documents Phase 10 breaking changes under [Unreleased]
- End-to-end verification gate passes on a darwin host
- 10-SUMMARY.md committed
- No `@shoki/sdk` / `@shoki/vitest` references remain in current docs (only in historical CHANGELOG entries)
</success_criteria>

<output>
After completion, ensure `.planning/phases/10-cli-driven-shoki-app-distribution-shoki-setup-downloads-from/10-SUMMARY.md` is committed and `.planning/STATE.md` is updated:
- `progress.completed_phases` += 1
- `last_activity` to today
- `stopped_at` = "Completed Phase 10 (CLI-driven Shoki.app distribution + 4→3 package consolidation)"
- Add a Decisions row: "[Phase 10]: Renamed @shoki/sdk → shoki; collapsed @shoki/vitest into shoki/vitest subpath; .apps moved out of npm tarballs onto GitHub Releases; new app-v* tag cadence independent from v* SDK cadence"
</output>

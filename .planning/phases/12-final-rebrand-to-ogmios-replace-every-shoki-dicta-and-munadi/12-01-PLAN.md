---
phase: 12-final-rebrand-to-ogmios-replace-every-shoki-dicta-and-munadi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/package.json
  - packages/sdk/src/**/*.ts
  - packages/sdk/src/vitest/commands/munadi-start.ts
  - packages/sdk/src/vitest/commands/munadi-stop.ts
  - packages/sdk/src/vitest/commands/munadi-listen.ts
  - packages/sdk/src/vitest/commands/munadi-drain.ts
  - packages/sdk/src/vitest/commands/munadi-phrase-log.ts
  - packages/sdk/src/vitest/commands/munadi-last-phrase.ts
  - packages/sdk/src/vitest/commands/munadi-clear.ts
  - packages/sdk/src/vitest/commands/munadi-reset.ts
  - packages/sdk/src/vitest/commands/munadi-await-stable.ts
  - packages/sdk/src/vitest/commands/munadi-get-dropped-count.ts
  - packages/sdk/test/**/*.ts
  - packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip
  - packages/sdk/test/fixtures/setup/munadi-darwin-arm64.tampered.zip
  - packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip.sha256
  - packages/sdk/test/fixtures/setup/Info.plist
  - packages/sdk/test/fixtures/setup/build-fixtures.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "packages/sdk/package.json name is 'ogmios', bin is 'ogmios', optionalDependencies scope is @ogmios (not @munadi)"
    - "description, repository.url mention ogmios (not munadi); github.com/thejackshelton/ogmios"
    - "Every exported MunadiX error class renamed to OgmiosX; ERR_MUNADI_* -> ERR_OGMIOS_*"
    - "Factory munadiVitest() renamed to ogmiosVitest()"
    - "IMPORT_NEEDLE is 'ogmios/vitest/browser'; plugin name is 'ogmios/vitest'"
    - "All 10 command files renamed from munadi-*.ts to ogmios-*.ts via git mv (history preserved)"
    - "All MUNADI_* env vars renamed to OGMIOS_* (HELPER_PATH, AX_TARGET_PID, INTEGRATION, SNAPSHOT_PATH, NATIVE_BUILT, SETUP_APP_PATH)"
    - "_munadi_snapshot_* plist keys renamed to _ogmios_snapshot_*; __MUNADI_MISSING__ -> __OGMIOS_MISSING__"
    - "DEFAULT_SNAPSHOT_PATH points at ~/.ogmios/ (not ~/.munadi/)"
    - "Test fixture zip files renamed munadi-darwin-arm64.* -> ogmios-darwin-arm64.*"
    - "pnpm --filter ogmios test passes (all unit tests green)"
  artifacts:
    - path: "packages/sdk/package.json"
      provides: "SDK package metadata, name=ogmios, bin=ogmios, deps=@ogmios/binding-*"
      contains: '"name": "ogmios"'
    - path: "packages/sdk/src/errors.ts"
      provides: "OgmiosError base class (not MunadiError)"
      contains: "OgmiosError"
    - path: "packages/sdk/src/vitest/plugin.ts"
      provides: "IMPORT_NEEDLE='ogmios/vitest/browser', ogmiosVitest factory"
      contains: "ogmios/vitest/browser"
    - path: "packages/sdk/src/vitest/session-store.ts"
      provides: "OGMIOS_AX_TARGET_PID env var setter"
      contains: "OGMIOS_AX_TARGET_PID"
    - path: "packages/sdk/src/cli/restore-vo-settings.ts"
      provides: "DEFAULT_SNAPSHOT_PATH=~/.ogmios/, _ogmios_snapshot_* regex, __OGMIOS_MISSING__ sentinel"
      contains: "_ogmios_snapshot_"
    - path: "packages/sdk/src/vitest/commands/ogmios-start.ts"
      provides: "ogmiosStart command handler (renamed from munadi-start.ts)"
  key_links:
    - from: "packages/sdk/src/vitest/plugin.ts"
      to: "packages/sdk/src/vitest/commands/index.ts"
      via: "IMPORT_NEEDLE + plugin name + handler registration"
      pattern: "ogmios/vitest"
    - from: "packages/sdk/src/vitest/commands/index.ts"
      to: "packages/sdk/src/vitest/commands/ogmios-*.ts"
      via: "import './ogmios-start.js' etc"
      pattern: "from './ogmios-"
    - from: "packages/sdk/package.json"
      to: "packages/binding-darwin-arm64/package.json"
      via: "optionalDependencies: @ogmios/binding-darwin-arm64 workspace link"
      pattern: "@ogmios/binding-darwin-arm64"
---

<objective>
Rename every `Munadi` / `munadi` / `MUNADI_*` token in `packages/sdk/` to `Ogmios` / `ogmios` / `OGMIOS_*`. This is the core TS surface — error classes, factory, plugin, CLI, env vars, plist keys, state dir, test fixtures, 10 command files (renamed via `git mv` to preserve history). The package.json was already partially edited (name/bin are `ogmios`) — this plan finishes the job (optionalDependencies scope, description, all source files, tests).

Purpose: Phase 11 renamed SDK surface to Munadi. Munadi was then rejected by npm's similarity filter (vs. `minami`). Phase 12 flips the final pivot to Ogmios across every token in the SDK package, landing `pnpm --filter ogmios test` green.

Output: SDK package fully rebranded to Ogmios; all 242+ unit tests pass; workspace dep name flip aligns with Plan 12-02's `@ogmios/binding-*` scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-final-rebrand-to-ogmios-replace-every-shoki-dicta-and-munadi/12-CONTEXT.md
@.planning/phases/11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli/11-01-SUMMARY.md

<read_first>
Before editing, understand the current state:
1. Plan 11-01 SUMMARY lists the exhaustive set of files touched in the dicta -> munadi rename of packages/sdk/** (99 files). This plan applies the same substitution table to flip Munadi -> Ogmios across those same files.
2. packages/sdk/package.json has already been PARTIALLY edited (name/bin are `ogmios`, but optionalDependencies still `@munadi/binding-*` and description still mentions Munadi). Complete it.
3. The 10 command files are currently named `packages/sdk/src/vitest/commands/munadi-*.ts`. Use `git mv` to rename each to `ogmios-*.ts`. Do NOT copy+delete — `git mv` preserves history.
4. Test fixture zips at packages/sdk/test/fixtures/setup/munadi-darwin-arm64.{zip,tampered.zip,zip.sha256} must be renamed via `git mv`. Content bytes are unchanged; SHA256 stays valid.
5. Test fixture packages/sdk/test/fixtures/setup/Info.plist contains bundle ID `org.munadi.fixture` and CFBundleName "Munadi Fixture" — flip to `org.ogmios.fixture` / "Ogmios Fixture".
6. packages/sdk/test/fixtures/setup/build-fixtures.sh references `_staging/Munadi.app/Contents/MacOS/Munadi` — flip to Ogmios.
7. The ~/.shoki/ and ~/.dicta/ legacy-detection one-liner in `ogmios doctor` first run is scope for Plan 12-06 (CLI checks), not here. Here we only flip ~/.munadi/ -> ~/.ogmios/ in `DEFAULT_SNAPSHOT_PATH`.
</read_first>

<interfaces>
<!-- Key substitution map for this plan. Executor should apply these verbatim. -->

Identifier-level substitutions (case-sensitive, applied across packages/sdk/src/** and test/**):

```
munadi                          -> ogmios
Munadi                          -> Ogmios
MUNADI_                         -> OGMIOS_
@munadi/                        -> @ogmios/
ERR_MUNADI_                     -> ERR_OGMIOS_
_munadi_snapshot_               -> _ogmios_snapshot_
__MUNADI_MISSING__              -> __OGMIOS_MISSING__
__MUNADI_SEP__                  -> __OGMIOS_SEP__   (if any remain in TS side)
~/.munadi/                      -> ~/.ogmios/
munadi-darwin-                  -> ogmios-darwin-
github.com/thejackshelton/munadi -> github.com/thejackshelton/ogmios
MunadiRunner                    -> OgmiosRunner
MunadiSetup                     -> OgmiosSetup
Munadi Runner (spaced display)  -> Ogmios Runner
Munadi Setup (spaced display)   -> Ogmios Setup
org.munadi.                     -> org.ogmios.
munadiVitest                    -> ogmiosVitest
MunadiVitestPluginOptions       -> OgmiosVitestPluginOptions
```

Class / type renames (enumerated from 11-01-SUMMARY.md):
```
MunadiError                     -> OgmiosError
MunadiConcurrentTestError       -> OgmiosConcurrentTestError
MunadiPlatformUnsupportedError  -> OgmiosPlatformUnsupportedError
MunadiBindingNotAvailableError  -> OgmiosBindingNotAvailableError
MunadiSessionNotFoundError      -> OgmiosSessionNotFoundError
MunadiEvent                     -> OgmiosEvent
MunadiEventSource               -> OgmiosEventSource
MunadiDriver                    -> OgmiosDriver
MunadiSdkDriver                 -> OgmiosSdkDriver
MunadiCommands                  -> OgmiosCommands
MunadiRpc                       -> OgmiosRpc
MunadiBrowserSession            -> OgmiosBrowserSession
MunadiVitestPluginOptions       -> OgmiosVitestPluginOptions
MunadiExpectMatchers            -> OgmiosExpectMatchers
MunadiAsymmetricMatchers        -> OgmiosAsymmetricMatchers
MunadiSessionRef                -> OgmiosSessionRef
WireMunadiEvent                 -> WireOgmiosEvent
Munadi*Args / Result / Deps / Handler (per-command types, 10 × 4 = 40 identifiers)
  -> Ogmios*Args / Result / Deps / Handler
munadiStart / munadiStop / munadiListen / munadiDrain / munadiPhraseLog /
munadiLastPhrase / munadiClear / munadiReset / munadiAwaitStable / munadiGetDroppedCount
  -> ogmiosStart / ogmiosStop / ... / ogmiosGetDroppedCount
```

CLI command strings (in src/cli/**):
```
"munadi" (as commander .name())          -> "ogmios"
"munadi doctor"                           -> "ogmios doctor"
"munadi-doctor" (quiet prefix)            -> "ogmios-doctor"
"munadi v${version}"                      -> "ogmios v${version}"
"[munadi/vitest]" (warning prefix)        -> "[ogmios/vitest]"
"munadi restore-vo-settings"              -> "ogmios restore-vo-settings"
```

File renames (git mv, preserve history):
```
packages/sdk/src/vitest/commands/munadi-start.ts           -> ogmios-start.ts
packages/sdk/src/vitest/commands/munadi-stop.ts            -> ogmios-stop.ts
packages/sdk/src/vitest/commands/munadi-listen.ts          -> ogmios-listen.ts
packages/sdk/src/vitest/commands/munadi-drain.ts           -> ogmios-drain.ts
packages/sdk/src/vitest/commands/munadi-phrase-log.ts      -> ogmios-phrase-log.ts
packages/sdk/src/vitest/commands/munadi-last-phrase.ts     -> ogmios-last-phrase.ts
packages/sdk/src/vitest/commands/munadi-clear.ts           -> ogmios-clear.ts
packages/sdk/src/vitest/commands/munadi-reset.ts           -> ogmios-reset.ts
packages/sdk/src/vitest/commands/munadi-await-stable.ts    -> ogmios-await-stable.ts
packages/sdk/src/vitest/commands/munadi-get-dropped-count.ts -> ogmios-get-dropped-count.ts

packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip           -> ogmios-darwin-arm64.zip
packages/sdk/test/fixtures/setup/munadi-darwin-arm64.tampered.zip  -> ogmios-darwin-arm64.tampered.zip
packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip.sha256    -> ogmios-darwin-arm64.zip.sha256
```

Snapshot-file strategy (learned from Plan 11-01):
- If packages/sdk/test/cli/reporters/__snapshots__/human.test.ts.snap contains any `munadi` tokens, DELETE the file and let `vitest run` regenerate it with Ogmios identifiers. Do NOT sed-edit .snap files.

Scope guards (DO NOT modify):
- .planning/phases/11-*/** (Phase 11 historical)
- .planning/phases/01-10-*/** (prior phase history)
- .planning/quick/260418-f0a-*, .planning/quick/260418-lfz-* (quick-task history)
- .planning/research/**
- Zig core (packages/binding-*/package.json `main` stays `munadi.node` until Plan 12-04)
- Helper bundles and helper/** (Plan 12-03 owns)
- examples/** (Plan 12-08 owns)
- .github/workflows/** and .github/actions/** (Plan 12-07 owns)
- Root README.md, CHANGELOG.md, CLAUDE.md, docs/** (Plan 12-09 owns)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename Munadi -> Ogmios across packages/sdk/src/** + package.json (source surface)</name>
  <files>
    packages/sdk/package.json,
    packages/sdk/src/errors.ts,
    packages/sdk/src/vitest/errors.ts,
    packages/sdk/src/vitest/plugin.ts,
    packages/sdk/src/vitest/session-store.ts,
    packages/sdk/src/vitest/browser.ts,
    packages/sdk/src/vitest/commands/index.ts,
    packages/sdk/src/vitest/commands/munadi-*.ts (10 files renamed via git mv),
    packages/sdk/src/vitest/command-types.ts,
    packages/sdk/src/cli/main.ts,
    packages/sdk/src/cli/errors.ts,
    packages/sdk/src/cli/reporters/human.ts,
    packages/sdk/src/cli/reporters/quiet.ts,
    packages/sdk/src/cli/checks/tcc-grants.ts,
    packages/sdk/src/cli/checks/helper-discovery.ts,
    packages/sdk/src/cli/checks/helper-signature.ts,
    packages/sdk/src/cli/setup-app-path.ts,
    packages/sdk/src/cli/setup-command.ts,
    packages/sdk/src/cli/setup-download.ts,
    packages/sdk/src/cli/setup-install.ts,
    packages/sdk/src/cli/fix-executor.ts,
    packages/sdk/src/cli/restore-vo-settings.ts,
    packages/sdk/src/matchers/types.ts,
    packages/sdk/src/matchers/matchers.ts,
    packages/sdk/src/matchers/fixtures.ts,
    packages/sdk/src/native-types.ts,
    packages/sdk/src/binding-loader.ts,
    packages/sdk/src/index.ts,
    packages/sdk/src/screen-reader.ts,
    packages/sdk/src/driver-handle.ts,
    packages/sdk/src/handle-internals.ts,
    packages/sdk/src/listen.ts,
    packages/sdk/src/wire.ts,
    packages/sdk/src/commander-commands.ts,
    packages/sdk/src/voice-over.ts
  </files>
  <action>
Apply the substitution table from the `<interfaces>` block to every .ts file under `packages/sdk/src/**` and to `packages/sdk/package.json`. Execute in this order:

1. **Rename 10 command files first** so import rewrites in `commands/index.ts` land on paths that exist:
   ```bash
   cd packages/sdk/src/vitest/commands
   for f in munadi-start.ts munadi-stop.ts munadi-listen.ts munadi-drain.ts munadi-phrase-log.ts munadi-last-phrase.ts munadi-clear.ts munadi-reset.ts munadi-await-stable.ts munadi-get-dropped-count.ts; do
     new="${f/munadi-/ogmios-}"
     git mv "$f" "$new"
   done
   ```

2. **Finish packages/sdk/package.json edit** (name/bin already flipped interactively; optionalDependencies + description + repo URL remain):
   - `"description"`: replace `Munadi's Zig native core` -> `Ogmios's Zig native core`
   - `"repository.url"`: `git+https://github.com/thejackshelton/munadi.git` -> `git+https://github.com/thejackshelton/ogmios.git`
   - `"optionalDependencies"`: `@munadi/binding-darwin-arm64` -> `@ogmios/binding-darwin-arm64`

3. **Apply bulk substitution to packages/sdk/src/** (use sed with explicit substitution table, NOT word boundaries — macOS sed does not support `\b`). Run per file, or use a loop over `rg --files -t ts packages/sdk/src`. Preserve the substitution order from 11-01-SUMMARY.md:
   - Error class names (Munadi*Error) first
   - Type names (MunadiEvent, MunadiDriver, etc.) next
   - Per-command type suffixes (MunadiStartArgs, MunadiStartResult, ...)
   - camelCase identifiers (munadiStart, munadiStop, ...) and `munadiVitest` factory
   - Env vars (MUNADI_AX_TARGET_PID, MUNADI_HELPER_PATH, MUNADI_INTEGRATION, MUNADI_SNAPSHOT_PATH, MUNADI_NATIVE_BUILT, MUNADI_SETUP_APP_PATH)
   - Error codes (ERR_MUNADI_*)
   - Plist keys (_munadi_snapshot_{version,pid,ts_unix,domain}) and sentinel __MUNADI_MISSING__
   - App filenames (MunadiRunner, MunadiSetup, "Munadi Runner", "Munadi Setup")
   - Bundle IDs (org.munadi.runner, org.munadi.setup)
   - State dir path (~/.munadi/)
   - URLs (github.com/thejackshelton/munadi)
   - IMPORT_NEEDLE and plugin name ("munadi/vitest", "munadi/vitest/browser")
   - CLI commander strings (`.name("munadi")`, help text mentioning "munadi", "munadi-doctor" quiet prefix)
   - Artifact filenames (munadi-darwin-*.zip)
   - Bare `munadi` prose tokens last (safe because no Ogmios collisions)

4. **Rewrite commands/index.ts imports and handler map**:
   - Import paths: `./munadi-start.js` -> `./ogmios-start.js` (×10)
   - Handler registration names: `munadiStart` -> `ogmiosStart`, `munadiStop` -> `ogmiosStop`, etc.

5. **Rewrite restore-vo-settings.ts explicitly** (11-01-SUMMARY noted this was a Rule 2 fix — do NOT skip):
   - `DEFAULT_SNAPSHOT_PATH = ~/.munadi/vo-snapshot.plist` -> `~/.ogmios/vo-snapshot.plist`
   - Constant `MUNADI_KEYS` -> `OGMIOS_KEYS`
   - Sentinel literal `__MUNADI_MISSING__` -> `__OGMIOS_MISSING__`
   - Regex extraction patterns `_munadi_snapshot_{version,domain,ts_unix}` -> `_ogmios_snapshot_*`

6. **Verify zero Munadi residuals in src/** after the sweep:
   ```bash
   rg -n "(munadi|Munadi|MUNADI_|@munadi/|_munadi_|org\.munadi\.|\.munadi/)" packages/sdk/src/
   # MUST exit 1 (zero matches)
   ```

Notes:
- Use `picocolors` code style existing in the file — do not reformat.
- If sed leaves a file unchanged (0 substitutions), proceed — that file had no tokens.
- Do NOT touch packages/sdk/src/cli/checks/helper-*.ts or setup-*.ts references to `Shoki*.app` / `org.shoki.*` / `~/.shoki/` yet. Those are hard-guarded Shoki residuals owned by Plan 12-06.

Wait — re-check: per Phase 11 plan 01 SUMMARY, helper-discovery.ts / helper-signature.ts / setup-*.ts were already flipped Shoki -> Munadi in Phase 11. So this plan just flips the Munadi -> Ogmios layer. The "hard-guarded Shoki residuals" Plan 12-06 owns are the ones that Phase 11 explicitly LEFT as Shoki (per 12-CONTEXT.md: "packages/sdk/src/cli/checks/helper-*.ts, setup-*.ts — hard-guarded in Phase 11 (still reference Shoki*.app / org.shoki.* / ~/.shoki/)"). So those files have BOTH a Munadi layer (helper path references, bundle IDs used in checks) AND residual Shoki strings. Here, flip only the Munadi layer. Shoki residuals stay for Plan 12-06.

Commit message: `refactor(12-01): rename Munadi -> Ogmios across packages/sdk/src/** (SDK TS surface)`
  </action>
  <verify>
    <automated>cd packages/sdk &amp;&amp; pnpm build &amp;&amp; pnpm typecheck &amp;&amp; rg -n "(munadi|Munadi|MUNADI_|@munadi/|_munadi_|org\.munadi\.|\.munadi/)" src/ &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>
    packages/sdk/src/** has zero `munadi` / `Munadi` / `MUNADI_` / `@munadi/` / `_munadi_` / `org.munadi.` / `.munadi/` tokens (verify grep exits 1). TypeScript compiles (`pnpm typecheck` exits 0). 10 command files renamed via git mv and present at `packages/sdk/src/vitest/commands/ogmios-*.ts`. packages/sdk/package.json: description mentions Ogmios, repository.url is `github.com/thejackshelton/ogmios`, optionalDependencies has `@ogmios/binding-darwin-arm64`. packages/sdk/src/cli/restore-vo-settings.ts contains `_ogmios_snapshot_`, `~/.ogmios/`, `__OGMIOS_MISSING__`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update SDK tests + fixtures to match new identifiers; run full test suite</name>
  <files>
    packages/sdk/test/**/*.ts,
    packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip (renamed),
    packages/sdk/test/fixtures/setup/munadi-darwin-arm64.tampered.zip (renamed),
    packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip.sha256 (renamed),
    packages/sdk/test/fixtures/setup/Info.plist,
    packages/sdk/test/fixtures/setup/build-fixtures.sh,
    packages/sdk/test/cli/reporters/__snapshots__/human.test.ts.snap (regenerated if needed)
  </files>
  <action>
1. **Rename test fixture zips via git mv** (binary content unchanged; SHA256 stays valid):
   ```bash
   cd packages/sdk/test/fixtures/setup
   git mv munadi-darwin-arm64.zip ogmios-darwin-arm64.zip
   git mv munadi-darwin-arm64.tampered.zip ogmios-darwin-arm64.tampered.zip
   git mv munadi-darwin-arm64.zip.sha256 ogmios-darwin-arm64.zip.sha256
   ```

2. **Update fixture Info.plist**:
   - CFBundleIdentifier `org.munadi.fixture` -> `org.ogmios.fixture`
   - CFBundleName `Munadi Fixture` -> `Ogmios Fixture`

3. **Update fixture build script**:
   - `packages/sdk/test/fixtures/setup/build-fixtures.sh`: `_staging/Munadi.app/Contents/MacOS/Munadi` -> `_staging/Ogmios.app/Contents/MacOS/Ogmios`; `org.munadi.fixture` -> `org.ogmios.fixture`; "Munadi Fixture" -> "Ogmios Fixture"; any `munadi-darwin-*` output name refs -> `ogmios-darwin-*`.

4. **Apply Munadi -> Ogmios substitution to packages/sdk/test/** (same substitution table as Task 1):
   - Commander smoke tests: `/munadi v/` pattern -> `/ogmios v/`; `/^munadi-doctor/` -> `/^ogmios-doctor/`
   - Session store tests: `"munadi-1"`, `"munadi-2"` session ID templates -> `"ogmios-1"`, `"ogmios-2"`; `MUNADI_AX_TARGET_PID` -> `OGMIOS_AX_TARGET_PID`
   - Plugin tests: assertion text `"munadi/vitest"` -> `"ogmios/vitest"`
   - Needle tests: `'munadi/vitest/browser'` import strings in test/vitest/fixtures/has-import/test.ts + opt-out/test.ts
   - Commands tests: `munadiStart`/`munadiDrain`/etc. handler names -> `ogmiosStart`/`ogmiosDrain`/etc.
   - Error-code tests: `ERR_MUNADI_*` -> `ERR_OGMIOS_*`
   - CLI setup tests: `munadi-darwin-arm64.zip` expected paths -> `ogmios-darwin-arm64.zip`; `github.com/thejackshelton/munadi` URLs -> `github.com/thejackshelton/ogmios`; `MUNADI_SETUP_APP_PATH` -> `OGMIOS_SETUP_APP_PATH`; `munadi-*-` tmp prefixes -> `ogmios-*-`
   - restore-vo-settings test: `MUNADI_SNAPSHOT_PATH` -> `OGMIOS_SNAPSHOT_PATH`; `munadi-restore-test-` -> `ogmios-restore-test-`; `munadi restore-vo-settings` prose -> `ogmios restore-vo-settings`
   - Matcher tests: `MunadiEvent[]` type references -> `OgmiosEvent[]`
   - Integration tests: `MUNADI_INTEGRATION`/`MUNADI_NATIVE_BUILT`/`MUNADI_SNAPSHOT_PATH` env gating -> `OGMIOS_*`
   - TCC grants fixtures: `test/cli/fixtures/tcc-rows.ts` + `codesign-output.ts` bundle IDs `org.munadi.runner` / `org.munadi.setup` -> `org.ogmios.runner` / `org.ogmios.setup`
   - Helper-discovery/helper-signature/tcc-grants tests: `MunadiRunner.app` / `MunadiSetup.app` paths -> `OgmiosRunner.app` / `OgmiosSetup.app`
   - SIGKILL fixture: `test/fixtures/sigkill-child.ts` — `MUNADI_SNAPSHOT_PATH` env + `~/.munadi/` prose -> `OGMIOS_*` / `~/.ogmios/`

5. **Regenerate human.test.ts.snap if contaminated**:
   ```bash
   # Check if snapshot has munadi tokens
   if rg -l "munadi|Munadi|MUNADI_" packages/sdk/test/cli/reporters/__snapshots__/ ; then
     rm packages/sdk/test/cli/reporters/__snapshots__/human.test.ts.snap
     # vitest run will regenerate
   fi
   ```

6. **Run full SDK test suite**:
   ```bash
   cd packages/sdk && pnpm test
   ```
   Expect all unit tests to pass (integration tests gated on OGMIOS_INTEGRATION=1 will skip — that's fine).

7. **Final grep verification in packages/sdk/test/**:
   ```bash
   rg -n "(munadi|Munadi|MUNADI_|@munadi/|_munadi_|org\.munadi\.|\.munadi/)" packages/sdk/test/
   # MUST exit 1
   ```

Commit message: `test(12-01): update SDK tests + fixtures to ogmios identifiers`
  </action>
  <verify>
    <automated>cd packages/sdk &amp;&amp; pnpm test 2&gt;&amp;1 | tail -20 &amp;&amp; rg -n "(munadi|Munadi|MUNADI_|@munadi/|_munadi_)" test/ &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>
    packages/sdk/test/** has zero Munadi-family tokens. `pnpm --filter ogmios test` exits 0 (all unit tests pass; integration tests skip cleanly). Three test fixture zips renamed and present at `ogmios-darwin-arm64.{zip,tampered.zip,zip.sha256}`. Fixture Info.plist has `org.ogmios.fixture` / "Ogmios Fixture".
  </done>
</task>

</tasks>

<verification>
Run all of these from the repo root; all must exit 0 (or grep must exit 1 for "no matches"):

```bash
# Package metadata
jq -r '.name, .bin.ogmios, .optionalDependencies["@ogmios/binding-darwin-arm64"]' packages/sdk/package.json
# Expect: ogmios / dist/cli/main.js / workspace:*

# Zero Munadi residuals across SDK
rg -n "(munadi|Munadi|MUNADI_|@munadi/|_munadi_|org\.munadi\.|\.munadi/|ogmios)" packages/sdk/src/ | rg -v ogmios
# Expect: empty

# Build + typecheck + test
cd packages/sdk && pnpm build && pnpm typecheck && pnpm test
```
</verification>

<success_criteria>
- packages/sdk/package.json: name=ogmios, bin=ogmios, optionalDependencies=@ogmios/binding-darwin-arm64, repository.url=github.com/thejackshelton/ogmios
- packages/sdk/src/** compiles with zero TS errors and zero Munadi-family tokens
- All 10 command files renamed to `ogmios-*.ts` via git mv (history preserved)
- `pnpm --filter ogmios test` exits 0 (242 or more tests pass)
- Test fixture zips renamed to `ogmios-darwin-arm64.*`
- Two atomic commits: `refactor(12-01): ...` + `test(12-01): ...`
</success_criteria>

<output>
After completion, create `.planning/phases/12-final-rebrand-to-ogmios-replace-every-shoki-dicta-and-munadi/12-01-SUMMARY.md`
</output>

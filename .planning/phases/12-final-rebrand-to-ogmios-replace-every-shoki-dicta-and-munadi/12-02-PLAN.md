---
phase: 12-final-rebrand-to-ogmios-replace-every-shoki-dicta-and-munadi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/binding-darwin-arm64/package.json
  - packages/binding-darwin-arm64/README.md
  - packages/binding-darwin-x64/package.json
  - packages/binding-darwin-x64/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "packages/binding-darwin-arm64/package.json name is @ogmios/binding-darwin-arm64"
    - "packages/binding-darwin-x64/package.json name is @ogmios/binding-darwin-x64"
    - "Both package.jsons reference repository.url github.com/thejackshelton/ogmios"
    - "Both READMEs are scrubbed of @munadi/, munadi, Munadi tokens"
    - "main: and files: fields still point at munadi.node (Plan 12-04 flips compiled artifact)"
  artifacts:
    - path: "packages/binding-darwin-arm64/package.json"
      provides: "arm64 native binding package metadata (name=@ogmios/binding-darwin-arm64)"
      contains: '"name": "@ogmios/binding-darwin-arm64"'
    - path: "packages/binding-darwin-x64/package.json"
      provides: "x64 native binding package metadata (name=@ogmios/binding-darwin-x64)"
      contains: '"name": "@ogmios/binding-darwin-x64"'
  key_links:
    - from: "packages/sdk/package.json"
      to: "packages/binding-darwin-arm64/package.json"
      via: "optionalDependencies @ogmios/binding-darwin-arm64 workspace link"
      pattern: "@ogmios/binding-darwin-arm64"
---

<objective>
Rename both platform binding packages from `@munadi/binding-darwin-{arm64,x64}` to `@ogmios/binding-darwin-{arm64,x64}`. Update package.json `name`, `description`, `repository.url`. Scrub README prose of all Munadi tokens (including `MunadiRunner.app`, `munadi` package refs). Keep `main: "munadi.node"` and `files: ["munadi.node", ...]` unchanged — Plan 12-04 owns the compiled artifact rename (cascades libmunadi.dylib -> libogmios.dylib and munadi.node -> ogmios.node together with Zig core).

Purpose: Phase 12's final pivot requires @ogmios npm scope for both binding packages. Runs in Wave 1 parallel with Plan 12-01 (SDK) — no file overlap.

Output: Both binding packages carry the @ogmios scope and correct repo URL; README prose is clean; workspace resolution succeeds with the new name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-final-rebrand-to-ogmios-replace-every-shoki-dicta-and-munadi/12-CONTEXT.md
@.planning/phases/11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli/11-02-SUMMARY.md

<read_first>
1. Plan 11-02 already flipped these packages shoki -> munadi. This plan flips munadi -> ogmios — same 4 files.
2. The compiled artifact name (`main: "munadi.node"`, `files: ["munadi.node", ...]`) stays unchanged here; Plan 12-04 flips it to `ogmios.node` alongside the Zig core rebuild.
3. README prose must also scrub `MunadiRunner.app` -> `OgmiosRunner.app` (Plan 11-02 used the same pattern: pre-rename helper app references so verify grep passes without a second sweep).
</read_first>

<interfaces>
Substitution table (applied to both `packages/binding-darwin-arm64/{package.json,README.md}` and `packages/binding-darwin-x64/{package.json,README.md}`):

```
@munadi/binding-        -> @ogmios/binding-
munadi (bare token)     -> ogmios
Munadi                  -> Ogmios
MunadiRunner.app        -> OgmiosRunner.app
MunadiSetup.app         -> OgmiosSetup.app
MunadiRunner / MunadiSetup (display) -> OgmiosRunner / OgmiosSetup
github.com/thejackshelton/munadi -> github.com/thejackshelton/ogmios
"Munadi native addon"   -> "Ogmios native addon"
"Munadi contributors"   -> "Ogmios contributors"
```

**Do NOT touch:**
- `main: "munadi.node"` — Plan 12-04 flips artifact filename
- `files: ["munadi.node", ...]` — Plan 12-04 flips
- LICENSE files — Plan 12-09 owns copyright/prose sweep

Post-edit verification:
```bash
rg -n "(munadi|Munadi|@munadi/)" packages/binding-darwin-arm64/package.json packages/binding-darwin-x64/package.json \
  | rg -v 'munadi\.node'
# Expect: empty (only munadi.node refs remain, which is fine — Plan 12-04 owns those)

rg -n "(munadi|Munadi|@munadi/)" packages/binding-darwin-arm64/README.md packages/binding-darwin-x64/README.md
# Expect: empty (READMEs have zero munadi refs, no artifact filename in README)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename both binding packages to @ogmios scope + update metadata + scrub READMEs</name>
  <files>
    packages/binding-darwin-arm64/package.json,
    packages/binding-darwin-arm64/README.md,
    packages/binding-darwin-x64/package.json,
    packages/binding-darwin-x64/README.md
  </files>
  <action>
Apply the substitution table from `<interfaces>` to all four files. Specifics:

**packages/binding-darwin-arm64/package.json:**
- `"name"`: `@munadi/binding-darwin-arm64` -> `@ogmios/binding-darwin-arm64`
- `"description"`: `Munadi native addon ...` -> `Ogmios native addon ...`
- `"repository.url"`: `git+https://github.com/thejackshelton/munadi.git` -> `git+https://github.com/thejackshelton/ogmios.git`
- Leave `main: "munadi.node"` and `files: ["munadi.node", "README.md", "LICENSE"]` unchanged (Plan 12-04 owns)
- Leave `publishConfig.access: public` and `publishConfig.provenance: true` unchanged

**packages/binding-darwin-arm64/README.md:**
- H1 `@munadi/binding-darwin-arm64` -> `@ogmios/binding-darwin-arm64`
- Any `munadi` SDK package reference -> `ogmios`
- Any `MunadiRunner.app` / `MunadiSetup.app` prose -> `OgmiosRunner.app` / `OgmiosSetup.app`
- Any `github.com/thejackshelton/munadi` link -> `github.com/thejackshelton/ogmios`
- Any "Munadi contributors" / "Munadi native addon" prose -> Ogmios equivalents

**packages/binding-darwin-x64/package.json + README.md:** same edits, swapping `arm64` for `x64` in the package name.

Verify workspace resolution with:
```bash
pnpm install --no-frozen-lockfile
```
Expect no ERR_PNPM_WORKSPACE_PKG_NOT_FOUND. The SDK's new `optionalDependencies.@ogmios/binding-darwin-arm64` (written by Plan 12-01 in parallel) resolves to the workspace package once both plans land.

**Note on parallel coupling with Plan 12-01:** If this plan lands before Plan 12-01's package.json edit, the SDK will temporarily reference `@munadi/binding-darwin-arm64` while the binding package is already `@ogmios/*`. That's fine — `pnpm install` will fail ONLY if BOTH plans are in the middle of execution AND someone runs install. The orchestrator coordinates Wave 1 such that install isn't run until both plans commit.

Commit message: `refactor(12-02): rename binding packages @munadi -> @ogmios scope`
  </action>
  <verify>
    <automated>jq -r '.name, .repository.url, .description' packages/binding-darwin-arm64/package.json packages/binding-darwin-x64/package.json &amp;&amp; rg -n "(munadi|Munadi|@munadi/)" packages/binding-darwin-arm64/README.md packages/binding-darwin-x64/README.md &amp;&amp; exit 1 || echo "READMEs clean"</automated>
  </verify>
  <done>
    Both `packages/binding-darwin-*/package.json` have `name: "@ogmios/binding-darwin-<arch>"`, `description` references Ogmios, `repository.url` points at `github.com/thejackshelton/ogmios`. Both READMEs contain zero `munadi` / `Munadi` / `@munadi/` tokens. `main: "munadi.node"` and `files` array with `munadi.node` entry are preserved (Plan 12-04 will flip). One atomic commit: `refactor(12-02): ...`.
  </done>
</task>

</tasks>

<verification>
```bash
# Package names
jq -r '.name' packages/binding-darwin-arm64/package.json  # @ogmios/binding-darwin-arm64
jq -r '.name' packages/binding-darwin-x64/package.json    # @ogmios/binding-darwin-x64

# Repo URL
jq -r '.repository.url' packages/binding-darwin-arm64/package.json
# Expect: git+https://github.com/thejackshelton/ogmios.git

# Zero munadi in README prose
rg -n "munadi|Munadi|@munadi/" packages/binding-darwin-*/README.md
# Expect: empty

# main/files still reference munadi.node (Plan 12-04 owns artifact rename)
jq -r '.main' packages/binding-darwin-arm64/package.json
# Expect: munadi.node
```
</verification>

<success_criteria>
- Both binding package.jsons renamed to @ogmios scope
- Both READMEs scrubbed of Munadi prose
- Repo URLs updated to github.com/thejackshelton/ogmios
- main/files intentionally preserve munadi.node (Plan 12-04 follow-up)
- One atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/12-final-rebrand-to-ogmios-replace-every-shoki-dicta-and-munadi/12-02-SUMMARY.md`
</output>

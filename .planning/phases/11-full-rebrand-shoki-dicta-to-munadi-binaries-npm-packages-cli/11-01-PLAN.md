---
phase: 11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/package.json
  - packages/sdk/src/index.ts
  - packages/sdk/src/errors.ts
  - packages/sdk/src/wire.ts
  - packages/sdk/src/binding-loader.ts
  - packages/sdk/src/voice-over.ts
  - packages/sdk/src/screen-reader.ts
  - packages/sdk/src/driver-handle.ts
  - packages/sdk/src/handle-internals.ts
  - packages/sdk/src/listen.ts
  - packages/sdk/src/native-types.ts
  - packages/sdk/src/commander-commands.ts
  - packages/sdk/src/matchers/index.ts
  - packages/sdk/src/matchers/types.ts
  - packages/sdk/src/matchers/matchers.ts
  - packages/sdk/src/matchers/fixtures.ts
  - packages/sdk/src/vitest/index.ts
  - packages/sdk/src/vitest/plugin.ts
  - packages/sdk/src/vitest/setup.ts
  - packages/sdk/src/vitest/browser.ts
  - packages/sdk/src/vitest/errors.ts
  - packages/sdk/src/vitest/command-types.ts
  - packages/sdk/src/vitest/session-store.ts
  - packages/sdk/src/vitest/commands/index.ts
  - packages/sdk/src/vitest/commands/shoki-listen.ts
  - packages/sdk/src/vitest/commands/shoki-drain.ts
  - packages/sdk/src/vitest/commands/shoki-stop.ts
  - packages/sdk/src/vitest/commands/shoki-clear.ts
  - packages/sdk/src/vitest/commands/shoki-reset.ts
  - packages/sdk/src/vitest/commands/shoki-start.ts
  - packages/sdk/src/vitest/commands/shoki-phrase-log.ts
  - packages/sdk/src/vitest/commands/shoki-last-phrase.ts
  - packages/sdk/src/vitest/commands/shoki-await-stable.ts
  - packages/sdk/src/vitest/commands/shoki-get-dropped-count.ts
  - packages/sdk/src/cli/main.ts
  - packages/sdk/src/cli/index.ts
  - packages/sdk/src/cli/errors.ts
  - packages/sdk/src/cli/run-doctor.ts
  - packages/sdk/src/cli/fix-executor.ts
  - packages/sdk/src/cli/report-types.ts
  - packages/sdk/src/cli/restore-vo-settings.ts
  - packages/sdk/src/cli/setup-download.ts
  - packages/sdk/src/cli/setup-install.ts
  - packages/sdk/src/cli/setup-app-path.ts
  - packages/sdk/src/cli/setup-command.ts
  - packages/sdk/src/cli/reporters/human.ts
  - packages/sdk/src/cli/reporters/quiet.ts
  - packages/sdk/src/cli/checks/helper-discovery.ts
  - packages/sdk/src/cli/checks/helper-signature.ts
  - packages/sdk/src/cli/checks/csreq-compare.ts
  - packages/sdk/src/cli/checks/tcc-grants.ts
  - packages/sdk/src/cli/checks/sip-status.ts
  - packages/sdk/src/cli/checks/vo-plist.ts
  - packages/sdk/src/cli/checks/macos-version.ts
  - packages/sdk/test/**/*.ts
  - packages/sdk/test/fixtures/setup/*
autonomous: true
requirements: []

must_haves:
  truths:
    - "The npm package name is `munadi` (was `dicta`). `jq -r '.name' packages/sdk/package.json` returns `munadi`."
    - "The CLI bin command is `munadi`. Users type `npx munadi setup`, `munadi doctor`. `jq -r '.bin.munadi'` returns `dist/cli/main.js` and `bin.dicta` does not exist."
    - "The unscoped SDK package depends on `@munadi/binding-darwin-arm64` (was `@shoki/binding-darwin-arm64`) in `optionalDependencies`."
    - "Every TS import of the SDK uses `munadi` (or `munadi/matchers`, `munadi/cli`, `munadi/vitest`, `munadi/vitest/browser`, `munadi/vitest/setup`). Zero `from 'dicta'` or `from '@shoki/core'` imports remain in `packages/sdk/`."
    - "Plugin auto-detection needle in `packages/sdk/src/vitest/plugin.ts` is `munadi/vitest/browser` so consumer imports still trigger `singleThread=true`."
    - "Exported error class names are `Munadi*` (e.g., `MunadiError`, `MunadiConcurrentTestError`, `MunadiPlatformUnsupportedError`, `MunadiBindingNotAvailableError`, `MunadiSessionNotFoundError`, `MunadiTimeoutError`, `MunadiCapturePathFailedError`, `MunadiVoiceOverUnavailableError`). No `Shoki*` or `Dicta*` error-class exports remain."
    - "Factory function is `munadiVitest(opts)` (was `shokiVitest`). Plugin option type is `MunadiVitestPluginOptions`."
    - "Every `process.env.SHOKI_*` reference is now `process.env.MUNADI_*` (`MUNADI_HELPER_PATH`, `MUNADI_AX_TARGET_PID`, `MUNADI_INTEGRATION`, `MUNADI_SNAPSHOT_PATH`, `MUNADI_NATIVE_BUILT`). Every `SHOKI_*` string literal in CLI option descriptions is `MUNADI_*`."
    - "Helper bundle ID strings in `packages/sdk/src/cli/checks/tcc-grants.ts` and any other check reference `org.munadi.runner` / `org.munadi.setup` (these names are written by Plan 04 in the same wave — Plan 01 only owns the TS references to them)."
    - "State-dir default path is `~/.munadi/vo-snapshot.plist` in TS prose/errors that reference the directory (no `~/.shoki/`)."
    - "Reporter prefixes, help text, description, and commander `.name()` are `munadi` (`dicta-doctor` → `munadi-doctor`, `dicta v<ver>` → `munadi v<ver>`). `jq -r '.description' packages/sdk/package.json` mentions `Munadi` not `Shoki`/`Dicta`."
    - "`repository.url` in `packages/sdk/package.json` is `git+https://github.com/thejackshelton/munadi.git` (was `github.com/shoki/shoki`)."
    - "`pnpm install --no-frozen-lockfile`, `pnpm --filter munadi build`, `pnpm -r typecheck`, and `pnpm --filter munadi test` all exit 0."
  artifacts:
    - path: "packages/sdk/package.json"
      provides: "npm name=munadi, bin.munadi, description+repo updated, optionalDependencies scope=@munadi"
      contains: '"name": "munadi"'
    - path: "packages/sdk/src/vitest/plugin.ts"
      provides: "Auto-singleThread needle + plugin name + factory"
      contains: "munadi/vitest/browser"
    - path: "packages/sdk/src/errors.ts"
      provides: "MunadiError base class + subclasses"
      contains: "class MunadiError"
    - path: "packages/sdk/src/vitest/errors.ts"
      provides: "MunadiConcurrentTestError, MunadiPlatformUnsupportedError, MunadiBindingNotAvailableError"
      contains: "MunadiConcurrentTestError"
    - path: "packages/sdk/src/vitest/session-store.ts"
      provides: "MUNADI_AX_TARGET_PID env-var wiring"
      contains: "MUNADI_AX_TARGET_PID"
  key_links:
    - from: "packages/sdk/src/vitest/plugin.ts"
      to: "consumer test files importing munadi/vitest/browser"
      via: "hard-coded IMPORT_NEEDLE constant"
      pattern: "munadi/vitest/browser"
    - from: "packages/sdk/src/cli/main.ts"
      to: "commander program name + quiet reporter"
      via: ".name('munadi') + 'munadi-doctor' prefix"
      pattern: "\\.name\\(['\\\"]munadi['\\\"]\\)"
    - from: "packages/sdk/src/vitest/session-store.ts"
      to: "zig driver AX observer (via MUNADI_AX_TARGET_PID env var)"
      via: "process.env assignment before handle boot"
      pattern: "MUNADI_AX_TARGET_PID"
    - from: "packages/sdk/src/cli/checks/tcc-grants.ts"
      to: "helper bundle ID (written by Plan 04)"
      via: "expected TCC grant subject"
      pattern: "org\\.munadi\\.(runner|setup)"
---

<objective>
Mechanical rename of the SDK package (`packages/sdk/`) from `dicta` → `munadi` across TypeScript surface, CLI commander wiring, error class names, plugin needle, env var strings, and helper-bundle-ID expectations. This plan owns ONLY `packages/sdk/**`. Bindings (Plan 02), Zig core (Plan 03), helper app (Plan 04), infra (Plan 05), docs/examples (Plan 06), and publish/folder-rename (Plan 07) are separate plans.

Purpose: Produce a clean-building SDK named `munadi` with the `munadi` CLI bin command, `munadi/vitest/*` subpath imports, `Munadi*` error classes, `munadiVitest()` factory, and `MUNADI_*` env vars. This is the most content-heavy plan in Phase 11 and forms the backbone every other plan references.

Output: `packages/sdk/package.json` with `name: munadi`, every SDK TS source rewritten to use `munadi` identifiers, all tests updated. `pnpm --filter munadi test` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli/11-CONTEXT.md
@.planning/quick/260418-lfz-rename-to-dicta/260418-lfz-PLAN.md
@.planning/quick/260418-lfz-rename-to-dicta/260418-lfz-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Substitution table for this plan. Applies to packages/sdk/** ONLY. -->

| Old                                                | New                                          |
|----------------------------------------------------|----------------------------------------------|
| `"name": "dicta"` (packages/sdk/package.json)      | `"name": "munadi"`                           |
| `"bin": { "dicta": "..." }`                        | `"bin": { "munadi": "..." }`                 |
| `"@shoki/binding-darwin-arm64": "workspace:*"` (optionalDependencies) | `"@munadi/binding-darwin-arm64": "workspace:*"` |
| `"url": "git+https://github.com/shoki/shoki.git"`  | `"url": "git+https://github.com/thejackshelton/munadi.git"` |
| `"description": "... Shoki's Zig native core"`     | Update to say Munadi (no Shoki/Dicta tokens) |
| `from 'dicta'`                                     | `from 'munadi'`                              |
| `from 'dicta/matchers'`                            | `from 'munadi/matchers'`                     |
| `from 'dicta/cli'`                                 | `from 'munadi/cli'`                          |
| `from 'dicta/vitest'`                              | `from 'munadi/vitest'`                       |
| `from 'dicta/vitest/browser'`                      | `from 'munadi/vitest/browser'`               |
| `import 'dicta/vitest/setup'`                      | `import 'munadi/vitest/setup'`               |
| `IMPORT_NEEDLE = 'dicta/vitest/browser'`           | `IMPORT_NEEDLE = 'munadi/vitest/browser'`    |
| plugin `name: 'dicta/vitest'`                      | `name: 'munadi/vitest'`                      |
| warning prefix `[dicta/vitest]`                    | `[munadi/vitest]`                            |
| commander `.name('dicta')`                         | `.name('munadi')`                            |
| quiet reporter prefix `dicta-doctor`               | `munadi-doctor`                              |
| `info` subcommand output `dicta v<ver>`            | `munadi v<ver>`                              |
| `SHOKI_HELPER_PATH` env var (string + usage)       | `MUNADI_HELPER_PATH`                         |
| `SHOKI_AX_TARGET_PID` env var                      | `MUNADI_AX_TARGET_PID`                       |
| `SHOKI_INTEGRATION` env var                        | `MUNADI_INTEGRATION`                         |
| `SHOKI_SNAPSHOT_PATH` env var                      | `MUNADI_SNAPSHOT_PATH`                       |
| `SHOKI_NATIVE_BUILT` env var                       | `MUNADI_NATIVE_BUILT`                        |
| `~/.shoki/` state dir in TS errors/comments        | `~/.munadi/`                                 |
| Class `ShokiError`                                 | `MunadiError`                                |
| Class `ShokiConcurrentTestError`                   | `MunadiConcurrentTestError`                  |
| Class `ShokiPlatformUnsupportedError`              | `MunadiPlatformUnsupportedError`             |
| Class `ShokiBindingNotAvailableError`              | `MunadiBindingNotAvailableError`             |
| Class `ShokiSessionNotFoundError`                  | `MunadiSessionNotFoundError`                 |
| Class `ShokiTimeoutError`                          | `MunadiTimeoutError`                         |
| Class `ShokiCapturePathFailedError`                | `MunadiCapturePathFailedError`               |
| Class `ShokiVoiceOverUnavailableError`             | `MunadiVoiceOverUnavailableError`            |
| `VoiceOverUnsupportedPlatformError` (already non-prefixed — keep) | keep as-is                  |
| Any other `ShokiX` / `DictaX` exported symbol      | `MunadiX`                                    |
| Factory `shokiVitest(opts)`                        | `munadiVitest(opts)`                         |
| Interface `ShokiVitestPluginOptions`               | `MunadiVitestPluginOptions`                  |
| Helper bundle ID `com.shoki.runner` in `checks/tcc-grants.ts` | `org.munadi.runner`                |
| Helper bundle ID `org.shoki.runner`                | `org.munadi.runner`                          |
| Helper bundle ID `app.shoki.setup` / `org.shoki.setup` | `org.munadi.setup`                       |
| ShokiRunner.app string literal in CLI option desc  | MunadiRunner.app (or "Munadi Runner.app" per whatever Plan 04 ships; see "Alignment" below) |
| "Shoki" proper noun in user-facing error messages  | "Munadi"                                     |
| "Shoki" proper noun in internal comments           | "Munadi" (prose sweep — this is a full rebrand, no v0.2 deferral) |
| Repo URL `https://github.com/shoki/shoki`          | `https://github.com/thejackshelton/munadi`   |
| GitHub Release artifact path `shoki-darwin-<arch>.zip` in `setup-download.ts` DEFAULT_BASE_URL and reference strings | `munadi-darwin-<arch>.zip` |

<!-- Alignment with Plan 04 (helper) — these decisions MUST match. -->

Plan 04 WILL produce:
- Bundle identifiers: `org.munadi.setup` (setup app), `org.munadi.runner` (runner app + MachServices key)
- Executable names: `MunadiRunner`, `MunadiSetup` (PascalCase, no space, matches existing ShokiRunner/ShokiSetup convention)
- .app bundle filenames: `MunadiRunner.app`, `MunadiSetup.app` (per CONTEXT.md decision "Munadi.app / Munadi Setup.app" — BUT the existing convention is single-word PascalCase, so use `MunadiRunner.app` + `MunadiSetup.app`. If a future prose reference says "Munadi.app / Munadi Setup.app", that is the user-facing CFBundleDisplayName, not the .app directory name.)
- CFBundleDisplayName strings: `Munadi Setup`, `Munadi Runner`
- dylib: `libMunadiXPCClient.dylib`
- Zip filenames: `munadi-darwin-arm64.zip`, `munadi-darwin-x64.zip`

Plan 01 references these names in TS strings but does NOT create the helper files. Any reference string Plan 01 writes must use the Plan 04 canonical names above.

<!-- IMPORTANT — env var in Zig (owned by Plan 03). -->

`packages/sdk/src/vitest/session-store.ts` sets `process.env.SHOKI_AX_TARGET_PID` which the Zig driver reads via `c.getenv("SHOKI_AX_TARGET_PID")`. Plan 03 renames the Zig side to `MUNADI_AX_TARGET_PID`. Plan 01 must rename the JS side to `MUNADI_AX_TARGET_PID` at the same time. Both land in Wave 1 and the rename is only meaningful if both sides flip together — Plan 06 (integration harness) depends on BOTH Plans 01 and 03 to pass the env var test.

<!-- Guards. -->

DO NOT modify in this plan:
- `packages/binding-darwin-arm64/**`, `packages/binding-darwin-x64/**` (Plan 02)
- `zig/**`, `helper/**` (Plan 03, Plan 04)
- `infra/**` (Plan 05)
- `examples/**` (Plan 06)
- `docs/**`, root `README.md`, `CHANGELOG.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `.planning/RELEASE-v0.1.0-RUNBOOK.md` (Plan 06)
- `.planning/phases/**`, `.planning/research/**`, `.planning/quick/260418-f0a-*`, `.planning/quick/260418-lfz-*` (historical)
- `.github/**` (Plan 06)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename TS surface, plugin, CLI, error classes, env vars across packages/sdk/src/**</name>
  <read_first>
    - packages/sdk/package.json
    - packages/sdk/src/index.ts
    - packages/sdk/src/errors.ts
    - packages/sdk/src/wire.ts
    - packages/sdk/src/binding-loader.ts
    - packages/sdk/src/voice-over.ts
    - packages/sdk/src/screen-reader.ts
    - packages/sdk/src/driver-handle.ts
    - packages/sdk/src/handle-internals.ts
    - packages/sdk/src/listen.ts
    - packages/sdk/src/native-types.ts
    - packages/sdk/src/commander-commands.ts
    - packages/sdk/src/matchers/index.ts
    - packages/sdk/src/matchers/types.ts
    - packages/sdk/src/matchers/matchers.ts
    - packages/sdk/src/matchers/fixtures.ts
    - packages/sdk/src/vitest/index.ts
    - packages/sdk/src/vitest/plugin.ts
    - packages/sdk/src/vitest/setup.ts
    - packages/sdk/src/vitest/browser.ts
    - packages/sdk/src/vitest/errors.ts
    - packages/sdk/src/vitest/command-types.ts
    - packages/sdk/src/vitest/session-store.ts
    - packages/sdk/src/vitest/commands/index.ts
    - packages/sdk/src/vitest/commands/shoki-listen.ts
    - packages/sdk/src/vitest/commands/shoki-drain.ts
    - packages/sdk/src/vitest/commands/shoki-stop.ts
    - packages/sdk/src/vitest/commands/shoki-clear.ts
    - packages/sdk/src/vitest/commands/shoki-reset.ts
    - packages/sdk/src/vitest/commands/shoki-start.ts
    - packages/sdk/src/vitest/commands/shoki-phrase-log.ts
    - packages/sdk/src/vitest/commands/shoki-last-phrase.ts
    - packages/sdk/src/vitest/commands/shoki-await-stable.ts
    - packages/sdk/src/vitest/commands/shoki-get-dropped-count.ts
    - packages/sdk/src/cli/main.ts
    - packages/sdk/src/cli/index.ts
    - packages/sdk/src/cli/errors.ts
    - packages/sdk/src/cli/run-doctor.ts
    - packages/sdk/src/cli/fix-executor.ts
    - packages/sdk/src/cli/report-types.ts
    - packages/sdk/src/cli/restore-vo-settings.ts
    - packages/sdk/src/cli/setup-download.ts
    - packages/sdk/src/cli/setup-install.ts
    - packages/sdk/src/cli/setup-app-path.ts
    - packages/sdk/src/cli/setup-command.ts
    - packages/sdk/src/cli/reporters/human.ts
    - packages/sdk/src/cli/reporters/quiet.ts
    - packages/sdk/src/cli/checks/helper-discovery.ts
    - packages/sdk/src/cli/checks/helper-signature.ts
    - packages/sdk/src/cli/checks/csreq-compare.ts
    - packages/sdk/src/cli/checks/tcc-grants.ts
    - packages/sdk/src/cli/checks/sip-status.ts
    - packages/sdk/src/cli/checks/vo-plist.ts
    - packages/sdk/src/cli/checks/macos-version.ts
  </read_first>
  <files>
    packages/sdk/package.json,
    packages/sdk/src/**/*.ts
  </files>
  <action>
Apply the substitution table in `<interfaces>` across every file in `packages/sdk/src/**` and the SDK `package.json`. This is a full rebrand — no v0.2 deferrals.

STEP A — `packages/sdk/package.json`:
- `"name": "dicta"` → `"name": "munadi"`
- `"bin": { "dicta": "dist/cli/main.js" }` → `"bin": { "munadi": "dist/cli/main.js" }`
- `"description"`: rewrite to "Screen-reader test automation SDK — TypeScript surface + CLI + matchers + Vitest plugin for Munadi's Zig native core". Zero `Shoki`/`Dicta` tokens.
- `"repository": { "url": "git+https://github.com/shoki/shoki.git" }` → `"url": "git+https://github.com/thejackshelton/munadi.git"`
- `"optionalDependencies": { "@shoki/binding-darwin-arm64": "workspace:*" }` → `"@munadi/binding-darwin-arm64": "workspace:*"` (the package that Plan 02 renames; using workspace:* so pnpm resolves via workspace link regardless of scope flip ordering within Wave 1)
- Leave unchanged: `compatibleAppVersion`, `exports` (subpaths are relative to the package, not to the package name — pnpm/Node resolve subpaths via the name at import time, no edit needed here), `version`, `files`, `dependencies`, `peerDependencies`, `peerDependenciesMeta`, `scripts`, `engines`, `keywords`, `publishConfig`, `devDependencies` (the `@shoki/binding-darwin-arm64` peer is already in optionalDependencies, not devDependencies).

STEP B — Error classes (packages/sdk/src/errors.ts, vitest/errors.ts, cli/errors.ts):
- `class ShokiError` → `class MunadiError`
- `class ShokiConcurrentTestError` → `class MunadiConcurrentTestError`
- `class ShokiPlatformUnsupportedError` → `class MunadiPlatformUnsupportedError`
- `class ShokiBindingNotAvailableError` → `class MunadiBindingNotAvailableError`
- `class ShokiSessionNotFoundError` → `class MunadiSessionNotFoundError`
- `class ShokiTimeoutError` → `class MunadiTimeoutError`
- `class ShokiCapturePathFailedError` → `class MunadiCapturePathFailedError`
- `class ShokiVoiceOverUnavailableError` → `class MunadiVoiceOverUnavailableError`
- Any other `class Shoki...` or `class Dicta...` → `class Munadi...`
- `VoiceOverUnsupportedPlatformError` (already unprefixed) — leave alone
- Update every `import { ShokiX }` / `import { DictaX }` anywhere in packages/sdk/src to the Munadi name
- Update error message string literals (user-visible text) to say "Munadi" instead of "Shoki" / "Dicta"

STEP C — Plugin needle + name + factory (packages/sdk/src/vitest/plugin.ts):
- `const IMPORT_NEEDLE = 'dicta/vitest/browser'` → `'munadi/vitest/browser'`
- Plugin `name: 'dicta/vitest'` → `'munadi/vitest'`
- Warning prefix `[dicta/vitest]` → `[munadi/vitest]`
- Interface `ShokiVitestPluginOptions` → `MunadiVitestPluginOptions` (also update its export + any JSDoc tag)
- Function `shokiVitest(opts)` → `munadiVitest(opts)` (also re-export from `packages/sdk/src/vitest/index.ts` under the new name)
- Warning message body that says "`shokiVitest({ autoSingleThread: false })`" → "`munadiVitest({ autoSingleThread: false })`"

STEP D — CLI program (packages/sdk/src/cli/main.ts + cli/index.ts):
- commander `.name('dicta')` → `.name('munadi')`
- Program description / usage / help text — replace any `dicta`/`shoki` bin-name token with `munadi`
- `info` subcommand that prints `dicta v<ver>` → `munadi v<ver>`
- `--helper-path` option description "Override the ShokiRunner.app path (also: $SHOKI_HELPER_PATH)" → "Override the MunadiRunner.app path (also: $MUNADI_HELPER_PATH)"
- `quiet` reporter prefix `dicta-doctor` → `munadi-doctor` (in `cli/reporters/quiet.ts`)
- `human` reporter: replace "Shoki"/"Dicta" mentions in the human-friendly output with "Munadi"
- `restore-vo-settings` error strings: "If you set $SHOKI_SNAPSHOT_PATH during the dicta run" → "If you set $MUNADI_SNAPSHOT_PATH during the munadi run"

STEP E — Env vars (search-replace across packages/sdk/src/**, but DO NOT touch the Zig side — Plan 03 owns that):
- `process.env.SHOKI_HELPER_PATH` → `process.env.MUNADI_HELPER_PATH`
- `process.env.SHOKI_AX_TARGET_PID` → `process.env.MUNADI_AX_TARGET_PID`
- `process.env.SHOKI_INTEGRATION` → `process.env.MUNADI_INTEGRATION`
- `process.env.SHOKI_SNAPSHOT_PATH` → `process.env.MUNADI_SNAPSHOT_PATH`
- `process.env.SHOKI_NATIVE_BUILT` → `process.env.MUNADI_NATIVE_BUILT`
- Every STRING LITERAL `'SHOKI_*'` / `"SHOKI_*"` / `$SHOKI_*` in prose error messages, comments, and option descriptions → `MUNADI_*`

STEP F — State dir references:
- Any string literal `~/.shoki/` or `.shoki/` in TS prose/errors → `~/.munadi/` / `.munadi/`
- Doctor first-run legacy-detection notice: Plan 01 does NOT add the legacy-detection code (that is a separate feature). Only the existing state-dir references get renamed here.

STEP G — Helper bundle ID references (in packages/sdk/src/cli/checks/*.ts):
- `packages/sdk/src/cli/checks/tcc-grants.ts` constant array: `'com.shoki.runner'` → `'org.munadi.runner'`. Per Plan 04 the actual Info.plist bundle ID is `org.munadi.runner` (matching existing `org.shoki.runner` convention — CONTEXT.md's `app.munadi.setup` and `com.munadi.runner` are incorrect; we normalize on `org.munadi.*` to match the existing Info.plist pattern).
- Any reference to `app.shoki.setup`, `org.shoki.setup`, `com.shoki.ShokiRunner`, `org.shoki.runner` anywhere in cli/checks/* → `org.munadi.setup` / `org.munadi.runner` (use `org.munadi.runner` for the runner and `org.munadi.setup` for the setup app; if `com.shoki.ShokiRunner` appears it was a docs typo — use `org.munadi.runner`).
- `packages/sdk/src/cli/checks/helper-discovery.ts`: any `.app` filename string `ShokiRunner.app` → `MunadiRunner.app`; `ShokiSetup.app` → `MunadiSetup.app`. Discovery paths continue to search `~/Applications/`.
- `packages/sdk/src/cli/setup-app-path.ts`, `setup-command.ts`, `setup-download.ts`, `setup-install.ts`: every `Shoki.app`, `ShokiRunner.app`, `Shoki Setup.app`, `ShokiSetup.app` → `MunadiRunner.app` / `MunadiSetup.app`. Every `shoki-darwin-<arch>.zip` → `munadi-darwin-<arch>.zip`. Every `github.com/shoki/shoki/releases/download` → `github.com/thejackshelton/munadi/releases/download`.

STEP H — Repo URL references:
- Every `github.com/shoki/shoki` in string literals across packages/sdk/src/** → `github.com/thejackshelton/munadi`
- Every `https://github.com/shoki/shoki/releases/download` → `https://github.com/thejackshelton/munadi/releases/download`

STEP I — Proper noun "Shoki" in comments/JSDoc:
- Replace every "Shoki" proper noun in comments + JSDoc with "Munadi" (e.g., "Shoki itself is observe-only" → "Munadi itself is observe-only"). This is a full rebrand — no v0.2 prose deferral.

STEP J — Imports of SDK subpaths inside packages/sdk/src:
- SDK files typically use RELATIVE imports internally (e.g., `../index.js`, `./matchers.js`). These stay relative. But any SDK test fixture or example-style comment that says `from 'dicta'` → `from 'munadi'`.

STEP K — Command file names (packages/sdk/src/vitest/commands/shoki-*.ts):
- The file names `shoki-listen.ts`, `shoki-drain.ts`, etc. are INTERNAL filenames. Do NOT rename the files in this task (file renames cascade to tsconfig/`src/vitest/commands/index.ts` imports and increase risk). Instead, inside these files:
  - Update any `BrowserCommand` registered name from `shokiListen` → `munadiListen`, `shokiDrain` → `munadiDrain`, etc., AND update the matching registrations in `packages/sdk/src/vitest/commands/index.ts` AND the consumer browser-side proxy in `packages/sdk/src/vitest/browser.ts`.
  - Update internal function names, exported identifiers, and docstrings from `Shoki*` / `shoki*` → `Munadi*` / `munadi*`.
  - File rename (`shoki-listen.ts` → `munadi-listen.ts`, etc.) is deferred to Plan 06's docs/examples wave — no it is NOT, per CONTEXT.md there is zero deferral. Rename the files via `git mv` AND update `commands/index.ts` imports in the same commit. Use `git mv` so history is preserved.

STEP L — `pnpm install --no-frozen-lockfile` from repo root to regenerate `pnpm-lock.yaml` against the new names (note: `@munadi/binding-darwin-arm64` is a workspace dep written by Plan 02; if Plan 02 has not landed yet, the install may warn but not fail — workspace:* resolution is lenient. If it fails, wait for Plan 02 to land).

STEP M — Run the build + typecheck for just the SDK package to confirm it compiles: `pnpm --filter munadi build && pnpm --filter munadi typecheck`.

STEP N — Run `pnpm --filter munadi test`. All tests must exit 0. The test-file updates land in Task 2 of this plan; this initial test run (after Task 1) may fail because tests assert on old identifiers — that is expected and resolved by Task 2.

DO NOT touch in this task:
- `packages/sdk/test/**` (Task 2 of this plan)
- `packages/binding-darwin-*` (Plan 02)
- `zig/**` (Plan 03)
- `helper/**` (Plan 04)
- `infra/**` (Plan 05)
- `examples/**` (Plan 06)
- `docs/**`, `README.md`, `CHANGELOG.md`, `CLAUDE.md`, `.planning/RELEASE-v0.1.0-RUNBOOK.md` (Plan 06)
- `.github/**` (Plan 06)
- `.planning/phases/**`, `.planning/research/**`, `.planning/quick/260418-f0a-*`, `.planning/quick/260418-lfz-*` (historical)
  </action>
  <verify>
    <automated>
      cd /Users/jackshelton/dev/open-source/shoki && \
      jq -r '.name' packages/sdk/package.json | grep -qx 'munadi' && \
      jq -e '.bin | has("dicta") | not' packages/sdk/package.json > /dev/null && \
      jq -e '.bin | has("shoki") | not' packages/sdk/package.json > /dev/null && \
      jq -r '.bin.munadi' packages/sdk/package.json | grep -qx 'dist/cli/main.js' && \
      jq -r '.repository.url' packages/sdk/package.json | grep -qE 'thejackshelton/munadi' && \
      jq -e '.optionalDependencies | has("@munadi/binding-darwin-arm64")' packages/sdk/package.json > /dev/null && \
      jq -e '.optionalDependencies | has("@shoki/binding-darwin-arm64") | not' packages/sdk/package.json > /dev/null && \
      grep -q 'munadi/vitest/browser' packages/sdk/src/vitest/plugin.ts && \
      grep -q 'munadiVitest' packages/sdk/src/vitest/plugin.ts && \
      grep -q 'MunadiError' packages/sdk/src/errors.ts && \
      grep -q 'MunadiConcurrentTestError' packages/sdk/src/vitest/errors.ts && \
      grep -q 'MUNADI_AX_TARGET_PID' packages/sdk/src/vitest/session-store.ts && \
      ! grep -rnE "(from|import)\s+['\"]dicta(/[^'\"]+)?['\"]" packages/sdk/src --include='*.ts' 2>/dev/null && \
      ! grep -rnE "process\.env\.SHOKI_" packages/sdk/src --include='*.ts' 2>/dev/null && \
      ! grep -rnE "class (Shoki|Dicta)[A-Z][A-Za-z]*Error" packages/sdk/src --include='*.ts' 2>/dev/null && \
      ! grep -rnE "(org|com|app)\.shoki\." packages/sdk/src --include='*.ts' 2>/dev/null
    </automated>
  </verify>
  <done>
    - `packages/sdk/package.json`: `name=munadi`, `bin.munadi=dist/cli/main.js`, no `bin.dicta`/`bin.shoki`, `optionalDependencies['@munadi/binding-darwin-arm64']=workspace:*`, repo URL is `github.com/thejackshelton/munadi`.
    - Zero `from 'dicta'` / `from 'dicta/...'` / `from '@shoki/core'` imports in `packages/sdk/src/**`.
    - Zero `process.env.SHOKI_*` references in `packages/sdk/src/**`.
    - Zero `Shoki*Error` or `Dicta*Error` class declarations in `packages/sdk/src/**`.
    - Zero `org.shoki.*` / `com.shoki.*` / `app.shoki.*` bundle-ID string literals in `packages/sdk/src/**`.
    - Plugin `IMPORT_NEEDLE = 'munadi/vitest/browser'`, plugin `name: 'munadi/vitest'`, factory exported as `munadiVitest`.
    - Classes `MunadiError`, `MunadiConcurrentTestError`, `MunadiPlatformUnsupportedError`, `MunadiBindingNotAvailableError`, `MunadiSessionNotFoundError`, `MunadiTimeoutError`, `MunadiCapturePathFailedError`, `MunadiVoiceOverUnavailableError` exported.
    - `process.env.MUNADI_AX_TARGET_PID` assignment exists in `packages/sdk/src/vitest/session-store.ts`.
    - CLI commander `.name('munadi')` in `packages/sdk/src/cli/main.ts`; quiet reporter prefix `munadi-doctor` in `packages/sdk/src/cli/reporters/quiet.ts`.
    - Command filenames renamed `shoki-*.ts` → `munadi-*.ts` under `packages/sdk/src/vitest/commands/` (via `git mv`), and `commands/index.ts` imports the new filenames.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SDK tests + fixtures to match new identifiers, run full test suite</name>
  <read_first>
    - packages/sdk/test/vitest/plugin.test.ts
    - packages/sdk/test/vitest/singleton-detection.test.ts
    - packages/sdk/test/vitest/structured-clone-safety.test.ts
    - packages/sdk/test/vitest/commands.test.ts
    - packages/sdk/test/vitest/session-store.test.ts
    - packages/sdk/test/vitest/fixtures/has-import/test.ts
    - packages/sdk/test/vitest/fixtures/opt-out/test.ts
    - packages/sdk/test/cli/integration/cli-smoke.test.ts
    - packages/sdk/test/cli/setup-command.test.ts
    - packages/sdk/test/cli/setup-install.test.ts
    - packages/sdk/test/cli/setup-download.test.ts
    - packages/sdk/test/cli/setup.test.ts
    - packages/sdk/test/cli/restore-vo-settings.test.ts
    - packages/sdk/test/cli/helper-discovery.test.ts
    - packages/sdk/test/cli/helper-signature.test.ts
    - packages/sdk/test/cli/tcc-grants.test.ts
    - packages/sdk/test/cli/reporters/human.test.ts
    - packages/sdk/test/cli/reporters/__snapshots__/human.test.ts.snap
    - packages/sdk/test/cli/fixtures/tcc-rows.ts
    - packages/sdk/test/cli/fixtures/codesign-output.ts
    - packages/sdk/test/matchers/to-have-announced.test.ts
    - packages/sdk/test/integration/voice-over.integration.test.ts
    - packages/sdk/test/integration/restore-on-sigkill.integration.test.ts
    - packages/sdk/test/fixtures/sigkill-child.ts
    - packages/sdk/test/fixtures/setup/
    - packages/sdk/test/ping.test.ts
    - packages/sdk/test/binding-loader.test.ts
    - packages/sdk/test/noop-roundtrip.test.ts
    - packages/sdk/test/listen.test.ts
    - packages/sdk/test/phrase-log.test.ts
    - packages/sdk/test/await-stable-log.test.ts
    - packages/sdk/test/stress.test.ts
    - packages/sdk/test/crash-recovery.test.ts
  </read_first>
  <files>
    packages/sdk/test/**/*.ts,
    packages/sdk/test/cli/reporters/__snapshots__/human.test.ts.snap,
    packages/sdk/test/fixtures/setup/shoki-darwin-arm64.zip.sha256
  </files>
  <action>
Update every test file and fixture under `packages/sdk/test/**` to match the new identifiers produced by Task 1. Run the full test suite.

STEP A — Test assertions that reference the old identifiers:
- `packages/sdk/test/vitest/plugin.test.ts`: plugin-name assertion `'dicta/vitest'` → `'munadi/vitest'`
- `packages/sdk/test/vitest/singleton-detection.test.ts`: needle assertion + warning prefix `[dicta/vitest]` → `[munadi/vitest]`
- `packages/sdk/test/vitest/commands.test.ts`: BrowserCommand name assertions `shokiListen` → `munadiListen`, `shokiDrain` → `munadiDrain`, etc.
- `packages/sdk/test/vitest/structured-clone-safety.test.ts`: error class name assertions `ShokiConcurrentTestError` → `MunadiConcurrentTestError`, SHOKI_* env-var assertions → MUNADI_*
- `packages/sdk/test/vitest/session-store.test.ts`: `SHOKI_AX_TARGET_PID` → `MUNADI_AX_TARGET_PID`
- `packages/sdk/test/cli/integration/cli-smoke.test.ts`: `/dicta v/` → `/munadi v/`, `/^dicta-doctor/` → `/^munadi-doctor/`
- `packages/sdk/test/cli/reporters/human.test.ts` + `.snap`: any "Shoki"/"Dicta" output string → "Munadi". Delete the snapshot and re-record it: `pnpm --filter munadi test -- -u` at the end of the task.
- `packages/sdk/test/cli/helper-discovery.test.ts`, `helper-signature.test.ts`, `tcc-grants.test.ts`: `ShokiRunner.app` → `MunadiRunner.app`, `ShokiSetup.app` → `MunadiSetup.app`, `org.shoki.runner` → `org.munadi.runner`, `org.shoki.setup` → `org.munadi.setup`, `com.shoki.runner` → `org.munadi.runner`
- `packages/sdk/test/cli/setup-download.test.ts`, `setup-install.test.ts`, `setup-command.test.ts`, `setup.test.ts`: `Shoki.app`, `Shoki Setup.app`, `ShokiRunner.app`, `ShokiSetup.app` in expected paths → `MunadiRunner.app` / `MunadiSetup.app`; `shoki-darwin-<arch>.zip` → `munadi-darwin-<arch>.zip`; `github.com/shoki/shoki` → `github.com/thejackshelton/munadi`
- `packages/sdk/test/cli/restore-vo-settings.test.ts`: `SHOKI_SNAPSHOT_PATH` → `MUNADI_SNAPSHOT_PATH`
- `packages/sdk/test/cli/fixtures/tcc-rows.ts`, `codesign-output.ts`: bundle IDs `com.shoki.*` / `org.shoki.*` → `org.munadi.*`
- `packages/sdk/test/matchers/to-have-announced.test.ts`: error class names if asserted
- `packages/sdk/test/integration/voice-over.integration.test.ts`, `restore-on-sigkill.integration.test.ts`, `stress.test.ts`, `crash-recovery.test.ts`: `SHOKI_INTEGRATION` → `MUNADI_INTEGRATION`, `SHOKI_NATIVE_BUILT` → `MUNADI_NATIVE_BUILT`, `SHOKI_SNAPSHOT_PATH` → `MUNADI_SNAPSHOT_PATH`
- `packages/sdk/test/fixtures/sigkill-child.ts`: `SHOKI_SNAPSHOT_PATH` → `MUNADI_SNAPSHOT_PATH`, any `~/.shoki/` → `~/.munadi/`
- `packages/sdk/test/noop-roundtrip.test.ts`, `ping.test.ts`, `binding-loader.test.ts`, `listen.test.ts`, `phrase-log.test.ts`, `await-stable-log.test.ts`: any `Shoki*` / `Dicta*` identifier or `SHOKI_*` / `from 'dicta'` → Munadi equivalents

STEP B — Test fixtures under `packages/sdk/test/vitest/fixtures/`:
- `has-import/test.ts`: `import ... from 'dicta/vitest/browser'` → `from 'munadi/vitest/browser'` (this is the string the detector scans FOR)
- `opt-out/test.ts`: same substitution if present

STEP C — Test fixtures under `packages/sdk/test/fixtures/setup/`:
- File rename: `shoki-darwin-arm64.zip.sha256` → `munadi-darwin-arm64.zip.sha256` via `git mv` (preserves history)
- `Info.plist` fixture: any `org.shoki.*` / `com.shoki.*` bundle IDs → `org.munadi.*`; `ShokiRunner`/`ShokiSetup` CFBundleExecutable/Name → `MunadiRunner`/`MunadiSetup`; `CFBundleDisplayName` "Shoki Runner" / "Shoki Setup" → "Munadi Runner" / "Munadi Setup"
- `build-fixtures.sh`: update any `shoki-` filename references to `munadi-`

STEP D — Re-record snapshots:
- Delete `packages/sdk/test/cli/reporters/__snapshots__/human.test.ts.snap`
- Run `pnpm --filter munadi test -- -u` to regenerate snapshots
- Review regenerated snapshots for accuracy (no stray `shoki`/`dicta` tokens — should only contain `munadi`)
- If regenerated snapshots still contain `shoki`/`dicta`, the source-of-truth strings in `packages/sdk/src/cli/reporters/human.ts` were missed in Task 1 — circle back, fix the source, re-generate

STEP E — Run the full test suite with all updates applied:
- `pnpm install --no-frozen-lockfile` (in case Plan 02 landed after Task 1)
- `pnpm --filter munadi build`
- `pnpm --filter munadi typecheck`
- `pnpm --filter munadi test` — must exit 0
- If ANY test fails, the failure is either a stale identifier in test files (fix here) or a Task 1 miss in src (circle back to Task 1). Do not skip.

STEP F — Helper-app file presence check: run `ls packages/sdk/test/fixtures/setup/` and confirm `munadi-darwin-arm64.zip.sha256` exists (and `shoki-darwin-arm64.zip.sha256` does not — `git mv` ensures the old name is gone).
  </action>
  <verify>
    <automated>
      cd /Users/jackshelton/dev/open-source/shoki && \
      ! grep -rnE "from ['\"]dicta(/[^'\"]+)?['\"]" packages/sdk/test --include='*.ts' --include='*.tsx' 2>/dev/null && \
      ! grep -rnE "process\.env\.SHOKI_" packages/sdk/test --include='*.ts' 2>/dev/null && \
      ! grep -rnE "(Shoki|Dicta)[A-Z][A-Za-z]*Error" packages/sdk/test --include='*.ts' 2>/dev/null && \
      ! grep -rnE "(org|com|app)\.shoki\." packages/sdk/test --include='*.ts' --include='*.plist' 2>/dev/null && \
      ls packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip.sha256 > /dev/null 2>&1 && \
      [ ! -e packages/sdk/test/fixtures/setup/shoki-darwin-arm64.zip.sha256 ] && \
      grep -q 'munadi/vitest/browser' packages/sdk/test/vitest/fixtures/has-import/test.ts && \
      pnpm install --no-frozen-lockfile && \
      pnpm --filter munadi build && \
      pnpm --filter munadi typecheck && \
      pnpm --filter munadi test
    </automated>
  </verify>
  <done>
    - Zero `from 'dicta'` / `from '@shoki/core'` imports in `packages/sdk/test/**`.
    - Zero `process.env.SHOKI_*` references in `packages/sdk/test/**`.
    - Zero `Shoki*Error` / `Dicta*Error` class references in `packages/sdk/test/**`.
    - Zero `org.shoki.*` / `com.shoki.*` / `app.shoki.*` bundle-ID strings in `packages/sdk/test/**` (including `.plist` fixtures).
    - `packages/sdk/test/fixtures/setup/munadi-darwin-arm64.zip.sha256` exists; `shoki-darwin-arm64.zip.sha256` does not.
    - Snapshot file `human.test.ts.snap` regenerated with `munadi` identifiers.
    - `pnpm --filter munadi build && pnpm --filter munadi typecheck && pnpm --filter munadi test` all exit 0.
  </done>
</task>

</tasks>

<verification>
- `jq -r '.name' packages/sdk/package.json` returns `munadi`
- `jq -r '.bin.munadi' packages/sdk/package.json` returns `dist/cli/main.js`
- `jq -e '.bin | has("dicta") | not'` returns true
- `jq -e '.optionalDependencies | has("@munadi/binding-darwin-arm64")'` returns true
- `grep -rnE "from ['\"]dicta(/[^'\"]+)?['\"]" packages/sdk/ --include='*.ts'` returns ZERO matches
- `grep -rnE "process\.env\.SHOKI_" packages/sdk/ --include='*.ts'` returns ZERO matches
- `grep -rnE "class (Shoki|Dicta)[A-Z][A-Za-z]*Error" packages/sdk/src` returns ZERO matches
- `grep -q 'MunadiError' packages/sdk/src/errors.ts` succeeds
- `grep -q 'munadi/vitest/browser' packages/sdk/src/vitest/plugin.ts` succeeds
- `grep -q 'munadiVitest' packages/sdk/src/vitest/plugin.ts` succeeds
- `grep -q 'MUNADI_AX_TARGET_PID' packages/sdk/src/vitest/session-store.ts` succeeds
- `pnpm --filter munadi build`, `pnpm --filter munadi typecheck`, `pnpm --filter munadi test` all exit 0
- No files under `packages/binding-*`, `zig/`, `helper/`, `infra/`, `examples/`, `docs/`, `.planning/phases/**`, `.planning/research/**`, `.planning/quick/260418-f0a-*`, `.planning/quick/260418-lfz-*` were modified by this plan
</verification>

<success_criteria>
- SDK package name is `munadi`
- CLI bin name is `munadi`
- Every TS source and test under `packages/sdk/**` uses `munadi` / `Munadi` / `MUNADI_*` identifiers
- Plugin needle, plugin name, warning prefix, factory, and option interface all use `munadi`
- Every `ShokiX` / `DictaX` error class renamed to `MunadiX`
- Every `SHOKI_*` env var reference renamed to `MUNADI_*` (JS side — Zig side lands in Plan 03)
- Every helper bundle-ID TS reference uses `org.munadi.runner` / `org.munadi.setup`
- Every Shoki/Dicta repo URL uses `github.com/thejackshelton/munadi`
- Full `pnpm --filter munadi test` green
- No files outside `packages/sdk/**` touched
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli/11-01-SUMMARY.md` summarizing:
- Count of TS source files + test files touched
- Count of error classes renamed
- Count of env var references renamed
- Confirmation that `pnpm --filter munadi test` exits 0
- Confirmation that the plan did not touch any file outside `packages/sdk/**`
- Any discoveries or deviations vs the plan
</output>

---
phase: 11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli
plan: 03
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - zig/build.zig
  - zig/build.zig.zon
  - zig/README.md
  - zig/src/root.zig
  - zig/src/core/driver.zig
  - zig/src/core/napi.zig
  - zig/src/core/registry.zig
  - zig/src/core/sync.zig
  - zig/src/drivers/README.md
  - zig/src/drivers/noop/driver.zig
  - zig/src/drivers/voiceover/applescript.zig
  - zig/src/drivers/voiceover/ax_notifications.zig
  - zig/src/drivers/voiceover/defaults.zig
  - zig/src/drivers/voiceover/driver.zig
  - zig/src/drivers/voiceover/lifecycle.zig
  - zig/test/voiceover_defaults_test.zig
  - zig/test/voiceover_driver_test.zig
  - zig/test/voiceover_lifecycle_test.zig
  - zig/all_tests.zig
  - packages/binding-darwin-arm64/shoki.node
  - packages/binding-darwin-x64/shoki.node
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Zig package name in `zig/build.zig.zon` is `.munadi_core` (was `.shoki_core`)."
    - "The Zig build product name in `zig/build.zig` is `munadi` (was `shoki`) so the compiled addon emits `munadi.node`."
    - "Every `SHOKI_*` env var reference in `zig/src/**` and `zig/test/**` is `MUNADI_*` (`MUNADI_AX_TARGET_PID`, `MUNADI_SNAPSHOT_PATH`, `MUNADI_INTEGRATION`, `MUNADI_NATIVE_BUILT`)."
    - "The plist snapshot magic keys are `_munadi_snapshot_*` (was `_shoki_snapshot_*`) — version, pid, ts_unix, domain."
    - "The default state-dir snapshot path is `$HOME/.munadi/vo-snapshot.plist` (was `$HOME/.shoki/vo-snapshot.plist`)."
    - "Every comment/docstring in `zig/src/**` and `zig/test/**` that says `Shoki` is now `Munadi`."
    - "`zig build test` exits 0 from `zig/` directory (tests pass with renamed plist keys + env vars)."
    - "The compiled addon file shipped to binding packages is named `munadi.node` and lives at `packages/binding-darwin-arm64/munadi.node` + `packages/binding-darwin-x64/munadi.node`. The `shoki.node` files are removed."
    - "Plan 02's binding package.json `main` field will be updated to `munadi.node` by this plan (cross-plan file touch — justified because Plan 02 does not rebuild the addon and Plan 03 does; Plan 03 owns the artifact rename end-to-end)."
  artifacts:
    - path: "zig/build.zig.zon"
      provides: "Zig package name"
      contains: ".name = .munadi_core"
    - path: "zig/build.zig"
      provides: "Build product name (controls .node filename)"
      contains: '.name = "munadi"'
    - path: "zig/src/drivers/voiceover/lifecycle.zig"
      provides: "Plist snapshot magic keys + default state dir"
      contains: "_munadi_snapshot_version"
    - path: "packages/binding-darwin-arm64/munadi.node"
      provides: "Renamed compiled arm64 addon"
    - path: "packages/binding-darwin-x64/munadi.node"
      provides: "Renamed compiled x64 addon"
  key_links:
    - from: "packages/sdk/src/binding-loader.ts (owned by Plan 01)"
      to: "@munadi/binding-darwin-arm64/munadi.node"
      via: "napi require of binding package main"
      pattern: "munadi\\.node"
    - from: "zig/src/drivers/voiceover/driver.zig"
      to: "packages/sdk/src/vitest/session-store.ts"
      via: "MUNADI_AX_TARGET_PID env var"
      pattern: "MUNADI_AX_TARGET_PID"
    - from: "packages/binding-darwin-arm64/package.json main field (owned by Plan 02)"
      to: "munadi.node"
      via: "napi binding-loader require"
      pattern: '"main": "munadi\\.node"'
---

<objective>
Rename the Zig core from `shoki`/`shoki_core` to `munadi`/`munadi_core`. Update Zig package metadata, build product name (which controls the compiled addon filename), every Zig source + test file, every `SHOKI_*` env var reference, the plist snapshot magic keys, and the default state-dir path. Rebuild the addon as `munadi.node` and move it into both binding packages, removing `shoki.node`.

Purpose: Flip the Zig compilation unit + env var ABI so the SDK's JS side (Plan 01) and the binding packages (Plan 02) point at a compiled artifact named `munadi.node` and an env-var contract named `MUNADI_*`.

Output: `munadi.node` sits in both binding packages, `zig build test` passes with renamed plist keys + env vars, and all Zig prose references say `Munadi`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli/11-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Zig-side substitution table. -->

| Old                                                   | New                                                   |
|-------------------------------------------------------|-------------------------------------------------------|
| `zig/build.zig.zon` `.name = .shoki_core`             | `.name = .munadi_core`                                |
| `zig/build.zig` `.name = "shoki"` (build product)     | `.name = "munadi"`                                    |
| `SHOKI_AX_TARGET_PID` in Zig source + tests           | `MUNADI_AX_TARGET_PID`                                |
| `SHOKI_SNAPSHOT_PATH` in Zig source + tests           | `MUNADI_SNAPSHOT_PATH`                                |
| `SHOKI_INTEGRATION` (if present in Zig)               | `MUNADI_INTEGRATION`                                  |
| `SHOKI_NATIVE_BUILT` (if present in Zig)              | `MUNADI_NATIVE_BUILT`                                 |
| Any other `SHOKI_*` env var in Zig                    | `MUNADI_*`                                            |
| Plist key `_shoki_snapshot_version`                   | `_munadi_snapshot_version`                            |
| Plist key `_shoki_snapshot_pid`                       | `_munadi_snapshot_pid`                                |
| Plist key `_shoki_snapshot_ts_unix`                   | `_munadi_snapshot_ts_unix`                            |
| Plist key `_shoki_snapshot_domain`                    | `_munadi_snapshot_domain`                             |
| Any other `_shoki_snapshot_*` key                     | `_munadi_snapshot_*`                                  |
| Default state dir path `.shoki/vo-snapshot.plist`     | `.munadi/vo-snapshot.plist`                           |
| Comment/docstring "Shoki" proper noun                 | "Munadi"                                              |
| Comment reference `@shoki/sdk` / `@shoki/vitest` / `dicta` | `munadi` (or `munadi/vitest` as appropriate)     |
| Compiled artifact `shoki.node`                        | `munadi.node`                                         |
| Output install path in `zig/build.zig` `shoki.node`   | `munadi.node`                                         |

<!-- Known specific locations (from grep). -->

- `zig/build.zig.zon` line 2: `.name = .shoki_core` → `.name = .munadi_core`
- `zig/build.zig` line 33: `.name = "shoki"` → `.name = "munadi"`
- `zig/build.zig` line 66 comment: `shoki_xpc_*` → leave if it refers to `libShokiXPCClient.dylib` symbol prefix (Plan 04 owns that symbol); re-check after Plan 04 ships
- `zig/src/drivers/voiceover/lifecycle.zig` lines 15, 28-37, 122, 132-134: snapshot path + plist keys
- `zig/src/drivers/voiceover/driver.zig` lines 128-134, 223, 231, 245-246: SHOKI_AX_TARGET_PID + `@shoki/vitest` comment
- `zig/src/drivers/voiceover/ax_notifications.zig` line 124: `SHOKI_AX_TARGET_PID` comment
- `zig/test/voiceover_lifecycle_test.zig` lines 841-845, 910, 915, 922: plist key + state dir + env var assertions
- `zig/test/voiceover_driver_test.zig` lines 410-411, 498-499: SHOKI_AX_TARGET_PID test

<!-- XPC client symbol. -->

`zig/build.zig` comment at line 66 references `shoki_xpc_*` — this is the XPC client C symbol prefix. The actual symbols are defined in `helper/` (Plan 04). Plan 04 MUST rename `shoki_xpc_*` → `munadi_xpc_*`. Plan 03 updates the comment in `zig/build.zig` to say `munadi_xpc_*`. If the Zig binding side imports this via `extern "c"` with the symbol name, the `extern` declaration must also flip — grep `shoki_xpc_` in `zig/src` and rename.

<!-- napi-zig dependency — DO NOT TOUCH. -->

`zig/build.zig.zon` has a `.napi_zig = .{ .url = "git+...", .hash = "napi_zig-..." }` dependency. The `.napi_zig` key name and `.hash` string are upstream-owned and must NOT change.

<!-- Binding package artifact names. -->

After this plan:
- `packages/binding-darwin-arm64/munadi.node` exists (was `shoki.node`)
- `packages/binding-darwin-x64/munadi.node` exists (was `shoki.node`)
- `packages/binding-darwin-arm64/shoki.node` does NOT exist
- `packages/binding-darwin-x64/shoki.node` does NOT exist
- Plan 02's `package.json` `main: "shoki.node"` must flip to `main: "munadi.node"` in both binding packages. This plan writes that change in Task 2 (cross-plan file touch, justified because Plan 02 explicitly deferred the artifact rename to Plan 03).

<!-- DO NOT touch. -->

- `helper/**` (Plan 04 — includes `libShokiXPCClient.dylib` and `shoki_xpc_*` C-ABI symbols)
- `packages/sdk/**` (Plan 01)
- `infra/**` (Plan 05)
- `examples/**`, `docs/**`, `.github/**`, root `README.md`, `CHANGELOG.md`, `CLAUDE.md`, `.planning/RELEASE-v0.1.0-RUNBOOK.md` (Plan 06)
- `.planning/phases/**`, `.planning/research/**`, `.planning/quick/260418-f0a-*`, `.planning/quick/260418-lfz-*` (historical)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Zig package + sources + tests + plist keys + env vars, then rebuild `munadi.node`</name>
  <read_first>
    - zig/build.zig
    - zig/build.zig.zon
    - zig/README.md
    - zig/src/root.zig
    - zig/src/core/driver.zig
    - zig/src/core/napi.zig
    - zig/src/core/registry.zig
    - zig/src/core/sync.zig
    - zig/src/drivers/README.md
    - zig/src/drivers/noop/driver.zig
    - zig/src/drivers/voiceover/applescript.zig
    - zig/src/drivers/voiceover/ax_notifications.zig
    - zig/src/drivers/voiceover/defaults.zig
    - zig/src/drivers/voiceover/driver.zig
    - zig/src/drivers/voiceover/lifecycle.zig
    - zig/test/voiceover_defaults_test.zig
    - zig/test/voiceover_driver_test.zig
    - zig/test/voiceover_lifecycle_test.zig
    - zig/all_tests.zig
  </read_first>
  <files>
    zig/build.zig,
    zig/build.zig.zon,
    zig/README.md,
    zig/src/**/*.zig,
    zig/test/**/*.zig,
    zig/all_tests.zig
  </files>
  <action>
Mechanical rename across Zig sources.

STEP A — `zig/build.zig.zon`:
- `.name = .shoki_core` → `.name = .munadi_core`
- Leave `.version`, `.fingerprint`, `.minimum_zig_version`, `.paths`, and the `.napi_zig` dependency entry (including its `.url` and `.hash`) UNCHANGED. `.hash` is upstream-owned.

STEP B — `zig/build.zig`:
- `.name = "shoki"` (the build product name that controls the compiled addon filename) → `.name = "munadi"`. This makes `zig build` emit `munadi.node` instead of `shoki.node`.
- Update the file-level doc comment ("Build graph for the Shoki Zig core" → "Build graph for the Munadi Zig core") and any other comment that says "Shoki"
- Update the comment at line ~66 `shoki_xpc_*` to `munadi_xpc_*` (matches Plan 04's symbol rename)
- Any `extern "c" fn shoki_xpc_*` declaration → `extern "c" fn munadi_xpc_*` (grep `shoki_xpc_` in the full zig/ tree — flip every occurrence)
- Any install output path reference to `shoki.node` → `munadi.node`
- If `zig build` references dylib link names (`libShokiXPCClient.dylib`), update to `libMunadiXPCClient.dylib` (matches Plan 04)

STEP C — Source files under `zig/src/**`:
- `zig/src/root.zig`: module header comment "Shoki" → "Munadi"
- `zig/src/core/driver.zig`: any comment/docstring "Shoki" → "Munadi"; any `Shoki`-prefixed struct/function name (e.g., `ShokiDriver` — this is the vtable defined in FOUND-01 EXT-01) → `MunadiDriver`. Plan 06's roadmap/docs references to `ShokiDriver` also update, but that's Plan 06's concern.
- `zig/src/core/napi.zig`, `registry.zig`, `sync.zig`: any `Shoki`/`shoki` token (struct, comment, log prefix) → `Munadi`/`munadi`
- `zig/src/drivers/README.md`: prose rewrite, `@shoki/binding-<os>-<arch>` → `@munadi/binding-<os>-<arch>`
- `zig/src/drivers/noop/driver.zig`: any `Shoki` → `Munadi`
- `zig/src/drivers/voiceover/driver.zig`:
  - Every `SHOKI_AX_TARGET_PID` string literal + comment → `MUNADI_AX_TARGET_PID`
  - Every `c.getenv("SHOKI_AX_TARGET_PID")` → `c.getenv("MUNADI_AX_TARGET_PID")`
  - Comment `@shoki/vitest` → `munadi/vitest`
  - Any other `Shoki`/`shoki` token → `Munadi`/`munadi`
- `zig/src/drivers/voiceover/ax_notifications.zig`: `SHOKI_AX_TARGET_PID` comment → `MUNADI_AX_TARGET_PID`; any `Shoki` → `Munadi`
- `zig/src/drivers/voiceover/applescript.zig`, `defaults.zig`: any `Shoki`/`shoki` token → `Munadi`/`munadi`
- `zig/src/drivers/voiceover/lifecycle.zig`:
  - Comment line 15: `~/.shoki/vo-snapshot.plist` → `~/.munadi/vo-snapshot.plist`
  - `SHOKI_SNAPSHOT_PATH` comments + string literals → `MUNADI_SNAPSHOT_PATH`
  - Default path literal `.shoki/vo-snapshot.plist` → `.munadi/vo-snapshot.plist`
  - `c.getenv("SHOKI_SNAPSHOT_PATH")` → `c.getenv("MUNADI_SNAPSHOT_PATH")`
  - `_shoki_snapshot_domain`, `_shoki_snapshot_version`, `_shoki_snapshot_pid`, `_shoki_snapshot_ts_unix` → `_munadi_snapshot_*`
  - `appendPlistMetaInt` + plist meta text → use `_munadi_` prefix

STEP D — Tests under `zig/test/**`:
- `zig/test/voiceover_lifecycle_test.zig`:
  - Every `_shoki_snapshot_*` key in assertions → `_munadi_snapshot_*`
  - `SHOKI_SNAPSHOT_PATH` test env var → `MUNADI_SNAPSHOT_PATH`
  - `.shoki/vo-snapshot.plist` expected path → `.munadi/vo-snapshot.plist`
- `zig/test/voiceover_driver_test.zig`:
  - `SHOKI_AX_TARGET_PID` test + env setenv/unsetenv → `MUNADI_AX_TARGET_PID`
  - Test description "SHOKI_AX_TARGET_PID env override" → "MUNADI_AX_TARGET_PID env override"
- `zig/test/voiceover_defaults_test.zig`: any `Shoki`/`shoki` token → `Munadi`/`munadi`
- `zig/all_tests.zig`: any `Shoki` → `Munadi`

STEP E — `zig/README.md`:
- H1 + hero: rewrite from "Shoki" to "Munadi"
- `@shoki/sdk` / `dicta` → `munadi`
- `shoki.node` → `munadi.node`
- `@shoki/binding-` → `@munadi/binding-`
- Any other Shoki/Dicta token → Munadi

STEP F — Build + test the Zig core in place:
- `cd zig && zig build test` — must exit 0. If a test fails due to stale identifiers the rename missed, circle back.
- After `zig build test` passes, run `zig build` to produce the compiled addon. The output should be `zig-out/lib/munadi.node` (or whatever path `zig/build.zig` writes to — it was `shoki.node` before, now `munadi.node`). If the output is still named `shoki.node`, the `.name = "munadi"` change in `zig/build.zig` did not take — debug before proceeding.

DO NOT touch in this task:
- `helper/**` (Plan 04)
- `packages/sdk/**` (Plan 01)
- `packages/binding-darwin-*/package.json` (Task 2 of this plan)
- Any file outside `zig/**`
  </action>
  <verify>
    <automated>
      cd /Users/jackshelton/dev/open-source/shoki && \
      grep -q '\.name = \.munadi_core' zig/build.zig.zon && \
      ! grep -q '\.name = \.shoki_core' zig/build.zig.zon && \
      grep -qE '\.name = "munadi"' zig/build.zig && \
      ! grep -rnE "SHOKI_(AX_TARGET_PID|SNAPSHOT_PATH|INTEGRATION|NATIVE_BUILT)" zig/ --include='*.zig' 2>/dev/null && \
      ! grep -rn "_shoki_snapshot_" zig/ --include='*.zig' 2>/dev/null && \
      ! grep -rn "\.shoki/vo-snapshot" zig/ --include='*.zig' 2>/dev/null && \
      grep -q "_munadi_snapshot_version" zig/src/drivers/voiceover/lifecycle.zig && \
      grep -q "MUNADI_AX_TARGET_PID" zig/src/drivers/voiceover/driver.zig && \
      cd zig && zig build test 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - `zig/build.zig.zon` package name is `.munadi_core`
    - `zig/build.zig` build product name is `"munadi"`
    - Zero `SHOKI_*` env var references in `zig/**`
    - Zero `_shoki_snapshot_*` plist keys in `zig/**`
    - Zero `.shoki/vo-snapshot` state-dir references in `zig/**`
    - `_munadi_snapshot_version`, `_munadi_snapshot_pid`, `_munadi_snapshot_ts_unix`, `_munadi_snapshot_domain` keys exist in `lifecycle.zig`
    - `MUNADI_AX_TARGET_PID`, `MUNADI_SNAPSHOT_PATH` env refs exist in Zig source
    - `zig build test` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild + rename compiled addon, move `munadi.node` into both bindings, update binding `main` fields</name>
  <read_first>
    - zig/build.zig
    - packages/binding-darwin-arm64/package.json
    - packages/binding-darwin-x64/package.json
  </read_first>
  <files>
    packages/binding-darwin-arm64/munadi.node,
    packages/binding-darwin-x64/munadi.node,
    packages/binding-darwin-arm64/package.json,
    packages/binding-darwin-x64/package.json,
    packages/binding-darwin-arm64/shoki.node,
    packages/binding-darwin-x64/shoki.node
  </files>
  <action>
Cross-compile the addon for both darwin arches, place `munadi.node` in each binding package, delete the old `shoki.node`, and update the `main` field in each binding `package.json`.

STEP A — Cross-compile for darwin-arm64:
- From repo root: `cd zig && zig build -Dtarget=aarch64-macos -Doptimize=ReleaseSafe`
- Output: `zig/zig-out/lib/munadi.node` (or wherever `zig/build.zig` installs it)
- Copy to binding: `cp zig/zig-out/lib/munadi.node packages/binding-darwin-arm64/munadi.node`
- Verify: `file packages/binding-darwin-arm64/munadi.node` should report `Mach-O 64-bit dynamically linked shared library arm64`

STEP B — Cross-compile for darwin-x64:
- From repo root: `cd zig && zig build -Dtarget=x86_64-macos -Doptimize=ReleaseSafe`
- Output: `zig/zig-out/lib/munadi.node`
- Copy to binding: `cp zig/zig-out/lib/munadi.node packages/binding-darwin-x64/munadi.node`
- Verify: `file packages/binding-darwin-x64/munadi.node` should report `Mach-O 64-bit dynamically linked shared library x86_64`

STEP C — Remove the old `shoki.node` files from both binding packages:
- `git rm packages/binding-darwin-arm64/shoki.node`
- `git rm packages/binding-darwin-x64/shoki.node`
- (Use `git rm` not `rm` so the removal is staged for commit)

STEP D — Update `packages/binding-darwin-arm64/package.json`:
- `"main": "shoki.node"` → `"main": "munadi.node"`
- `"files": ["shoki.node", "README.md", "LICENSE"]` → `"files": ["munadi.node", "README.md", "LICENSE"]`
- Leave everything else unchanged (name, description, repo URL are Plan 02's territory; this task only touches `main` + `files`)

STEP E — Update `packages/binding-darwin-x64/package.json`:
- Same substitutions as STEP D for x64

STEP F — Smoke test the native load:
- `pnpm install --no-frozen-lockfile`
- `pnpm --filter munadi build` (SDK build, assumes Plan 01 landed)
- `pnpm --filter munadi test -- --run test/ping.test.ts` — this exercises the binding loader which now looks for `munadi.node` via the binding package's `main` field. Must exit 0. If it fails with "could not resolve `munadi.node`", the `main` field update in STEP D/E did not propagate (check `package.json` diff).

STEP G — Cross-compile sanity check: run `file packages/binding-darwin-arm64/munadi.node` and `file packages/binding-darwin-x64/munadi.node` and confirm the arch tags match the package name.

DO NOT touch in this task:
- `packages/sdk/**` (Plan 01)
- `packages/binding-darwin-*/README.md` (Plan 02)
- `helper/**` (Plan 04)
  </action>
  <verify>
    <automated>
      cd /Users/jackshelton/dev/open-source/shoki && \
      [ -f packages/binding-darwin-arm64/munadi.node ] && \
      [ -f packages/binding-darwin-x64/munadi.node ] && \
      [ ! -f packages/binding-darwin-arm64/shoki.node ] && \
      [ ! -f packages/binding-darwin-x64/shoki.node ] && \
      file packages/binding-darwin-arm64/munadi.node | grep -qE 'arm64|aarch64' && \
      file packages/binding-darwin-x64/munadi.node | grep -qE 'x86_64' && \
      jq -r '.main' packages/binding-darwin-arm64/package.json | grep -qx 'munadi.node' && \
      jq -r '.main' packages/binding-darwin-x64/package.json | grep -qx 'munadi.node' && \
      jq -r '.files | .[]' packages/binding-darwin-arm64/package.json | grep -qx 'munadi.node' && \
      jq -r '.files | .[]' packages/binding-darwin-arm64/package.json | grep -vqx 'shoki.node' && \
      pnpm install --no-frozen-lockfile && \
      pnpm --filter munadi build && \
      pnpm --filter munadi test -- --run test/ping.test.ts
    </automated>
  </verify>
  <done>
    - Both binding packages contain `munadi.node` (arch-correct Mach-O)
    - Both binding packages do NOT contain `shoki.node`
    - Both binding `package.json` `main: "munadi.node"` and `files: ["munadi.node", ...]`
    - `pnpm --filter munadi test -- --run test/ping.test.ts` exits 0 (native addon loads)
  </done>
</task>

</tasks>

<verification>
- `grep -rnE "SHOKI_(AX_TARGET_PID|SNAPSHOT_PATH|INTEGRATION|NATIVE_BUILT)" zig/` returns ZERO matches
- `grep -rn "_shoki_snapshot_" zig/` returns ZERO matches
- `grep -rn "\.shoki/vo-snapshot" zig/` returns ZERO matches
- `zig build test` (from `zig/`) exits 0
- `packages/binding-darwin-arm64/munadi.node` + `packages/binding-darwin-x64/munadi.node` exist; `shoki.node` does not
- Both binding `main` fields are `munadi.node`
- `pnpm --filter munadi test -- --run test/ping.test.ts` exits 0
- No files modified outside `zig/**` and `packages/binding-darwin-*/{munadi.node,shoki.node,package.json}`
</verification>

<success_criteria>
- Zig core emits `munadi.node`
- Plist snapshot keys are `_munadi_snapshot_*`
- Env-var contract is `MUNADI_*` on both JS (Plan 01) and Zig (this plan) sides
- Binding packages load `munadi.node` at runtime
- Full Zig test suite + binding ping smoke test pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-rebrand-shoki-dicta-to-munadi-binaries-npm-packages-cli/11-03-SUMMARY.md` summarizing:
- Zig package name flip confirmation
- Env var contract flip confirmation
- Plist key flip confirmation
- Compiled addon rename + cross-arch artifact confirmation
- Any deviations vs the plan (e.g., if `libShokiXPCClient.dylib` link name conflicts required coordinating with Plan 04 across Wave 1)
</output>

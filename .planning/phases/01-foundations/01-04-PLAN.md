---
phase: 01-foundations
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - helper/Package.swift
  - helper/README.md
  - helper/Sources/ShokiRunner/main.swift
  - helper/Sources/ShokiRunner/Info.plist
  - helper/Sources/ShokiRunner/ShokiRunner.entitlements
  - helper/Sources/ShokiRunnerService/ShokiRunnerService.swift
  - helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift
  - helper/Tests/ShokiRunnerTests/XPCPingTests.swift
  - helper/scripts/build-app-bundle.sh
  - helper/scripts/sign-and-notarize.sh
  - helper/.gitignore
autonomous: true
requirements: [FOUND-03, FOUND-04]
must_haves:
  truths:
    - "helper/Package.swift defines a Swift package that produces a ShokiRunner executable"
    - "An XPC protocol (ShokiRunnerProtocol) declares a ping() method"
    - "ShokiRunnerService implements ping() returning 'shoki-runner-pong'"
    - "ShokiRunner main.swift boots an NSXPCListener with mach service name 'org.shoki.runner'"
    - "helper/scripts/build-app-bundle.sh produces a ShokiRunner.app bundle with Info.plist, entitlements, and the built executable"
    - "helper/scripts/sign-and-notarize.sh codesigns the app with a Developer ID Application cert passed via env var, then notarizes with notarytool"
    - "codesign -dvvv on the produced bundle shows the Developer ID when run in CI with secrets provided"
    - "A Swift test exercises the XPC ping round-trip in-process"
  artifacts:
    - path: "helper/Package.swift"
      provides: "Swift Package Manager manifest"
      contains: "ShokiRunner"
    - path: "helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift"
      provides: "NSXPCConnection protocol — frozen surface for Phase 3 to extend"
      contains: "@objc protocol ShokiRunnerProtocol"
    - path: "helper/Sources/ShokiRunnerService/ShokiRunnerService.swift"
      provides: "ShokiRunnerProtocol implementation (ping only in Phase 1)"
      contains: "ShokiRunnerService"
    - path: "helper/Sources/ShokiRunner/main.swift"
      provides: "NSXPCListener entry point"
      contains: "NSXPCListener"
    - path: "helper/Sources/ShokiRunner/Info.plist"
      provides: "App bundle metadata with CFBundleIdentifier org.shoki.runner"
      contains: "org.shoki.runner"
    - path: "helper/Sources/ShokiRunner/ShokiRunner.entitlements"
      provides: "hardened-runtime entitlements required for notarization"
      contains: "com.apple.security"
    - path: "helper/scripts/build-app-bundle.sh"
      provides: "builds ShokiRunner.app bundle from the SPM executable"
    - path: "helper/scripts/sign-and-notarize.sh"
      provides: "codesign + notarytool wrapper"
      contains: "notarytool"
  key_links:
    - from: "helper/Sources/ShokiRunner/main.swift"
      to: "helper/Sources/ShokiRunnerService/ShokiRunnerService.swift"
      via: "NSXPCListener delegate wires ShokiRunnerService as the exported object"
      pattern: "ShokiRunnerService"
    - from: "helper/Sources/ShokiRunnerService/ShokiRunnerService.swift"
      to: "helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift"
      via: "conforms to ShokiRunnerProtocol"
      pattern: "ShokiRunnerProtocol"
    - from: "helper/scripts/sign-and-notarize.sh"
      to: "helper/scripts/build-app-bundle.sh"
      via: "script runs after the bundle exists"
      pattern: "build-app-bundle"
---

<objective>
Implement the signed-wrapper-app architecture decided in CONTEXT.md §Signed-Wrapper-App Architecture (FOUND-04): a minimal Swift package producing a `ShokiRunner.app` bundle with an NSXPCListener and a single XPC method (`ping`). Provide scripts to build, codesign with Developer ID, and notarize the bundle so CI (Plan 05) can ship it alongside the native addon.

Purpose: the `ShokiRunner.app` is the stable TCC trust anchor. Node and the Zig `.node` binary may change signatures between releases; `ShokiRunner.app` stays signed by the same Developer ID cert, so TCC grants survive upgrades (PITFALLS.md Pitfall 4). FOUND-03 (every release binary Developer ID-signed) also hangs off this plan because the first signed artifact is this bundle.

Output: A buildable Swift package; scripts to produce, sign, and notarize `ShokiRunner.app`; an in-process XPC test that exercises the ping round-trip. Real VoiceOver calls route through XPC methods added in Phase 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundations/01-01-SUMMARY.md

<interfaces>
<!-- Signed-wrapper-app architecture locked in CONTEXT.md D-06:
     - Minimal Swift app (NSXPCConnection endpoint)
     - Developer ID-signed + notarized
     - Ships inside @shoki/binding-darwin-* packages at helper/ShokiRunner.app
     - Phase 1: ping endpoint only
     - Phase 3: real VO calls route through it

     Bundle identifier: org.shoki.runner
     Mach service name: org.shoki.runner
-->

<!-- XPC protocol evolution:
     Phase 1 (this plan):     func ping(reply: (String) -> Void)
     Phase 3 (future plan):   + startVoiceOver, stopVoiceOver, getLastPhrase, etc.

     The protocol lives in its own module (ShokiRunnerProtocol) so both the
     service and any external N-API bridge can import it.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swift package — protocol, service, executable</name>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md Signed-Wrapper-App Architecture section (D-06)
    - .planning/research/PITFALLS.md Pitfall 4 (TCC signature stickiness)
    - helper/.gitkeep (placeholder from Plan 01)
  </read_first>
  <files>
    helper/Package.swift
    helper/.gitignore
    helper/README.md
    helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift
    helper/Sources/ShokiRunnerService/ShokiRunnerService.swift
    helper/Sources/ShokiRunner/main.swift
    helper/Tests/ShokiRunnerTests/XPCPingTests.swift
  </files>
  <action>
Create the Swift Package Manager structure. Three modules: Protocol (pure interface), Service (implementation), Runner (executable entry point). Plus a unit test exercising XPC in-process.

Create `helper/Package.swift`:

    // swift-tools-version:5.9
    import PackageDescription

    let package = Package(
        name: "ShokiRunner",
        platforms: [
            .macOS(.v13),
        ],
        products: [
            .executable(name: "ShokiRunner", targets: ["ShokiRunner"]),
            .library(name: "ShokiRunnerProtocol", targets: ["ShokiRunnerProtocol"]),
            .library(name: "ShokiRunnerService", targets: ["ShokiRunnerService"]),
        ],
        dependencies: [],
        targets: [
            .target(
                name: "ShokiRunnerProtocol",
                path: "Sources/ShokiRunnerProtocol"
            ),
            .target(
                name: "ShokiRunnerService",
                dependencies: ["ShokiRunnerProtocol"],
                path: "Sources/ShokiRunnerService"
            ),
            .executableTarget(
                name: "ShokiRunner",
                dependencies: ["ShokiRunnerProtocol", "ShokiRunnerService"],
                path: "Sources/ShokiRunner",
                exclude: ["Info.plist", "ShokiRunner.entitlements"]
            ),
            .testTarget(
                name: "ShokiRunnerTests",
                dependencies: ["ShokiRunnerProtocol", "ShokiRunnerService"],
                path: "Tests/ShokiRunnerTests"
            ),
        ]
    )

Create `helper/.gitignore`:

    .build/
    build/
    DerivedData/
    *.xcodeproj/xcuserdata/
    ShokiRunner.app/
    *.dSYM/
    .notarize-log.json
    .notarize-submission-id

Create `helper/README.md`:

    # ShokiRunner.app

    The signed TCC trust anchor for Shoki. See CONTEXT.md §Signed-Wrapper-App Architecture (D-06) for the "why" in detail.

    ## What this is

    A minimal Swift app bundle that:
    1. Runs as a background process spawned by the Zig `.node` addon.
    2. Exposes an `NSXPCConnection` endpoint (`org.shoki.runner` mach service).
    3. Is Developer ID-signed + notarized by the Shoki release pipeline.

    Because this bundle's signature is stable across Shoki versions (we keep the same Developer ID cert), TCC grants survive upgrades of Node, Zig, and the `.node` addon itself (PITFALLS.md Pitfall 4).

    ## Phase 1 surface

    - `ping() -> String` — returns `"shoki-runner-pong"`. Used by `binding.runnerPing()` (Phase 3 N-API export) to confirm XPC plumbing works end-to-end.

    Phase 3 extends the protocol with VoiceOver lifecycle calls (`startVoiceOver`, `stopVoiceOver`, `getLastPhrase`, etc.) — those land in `Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift` without changing the bundle identity.

    ## Build

        cd helper
        swift build -c release
        scripts/build-app-bundle.sh        # wraps the SPM executable into ShokiRunner.app

    ## Sign + notarize (CI only)

        scripts/sign-and-notarize.sh ./ShokiRunner.app

    Requires env vars:
    - `APPLE_DEVELOPER_ID_APP` — `"Developer ID Application: Your Name (TEAMID)"`
    - `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD` — notarytool credentials

    ## Test

        swift test

    Runs `XPCPingTests.swift` which exercises the NSXPCListener round-trip in-process.

    ## Bundle layout

        ShokiRunner.app/
          Contents/
            Info.plist
            MacOS/
              ShokiRunner
            _CodeSignature/
              CodeResources
            embedded.provisionprofile (optional)

    Written into `packages/binding-darwin-<arch>/helper/ShokiRunner.app/` by the Plan 05 CI workflow.

Create `helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift`:

    import Foundation

    /// XPC protocol exposed by ShokiRunner.app. Loaded by both:
    /// - the Swift-side service (`ShokiRunnerService`)
    /// - any NSXPCConnection client (Phase 3: the Zig N-API addon calls this via a Swift bridge)
    ///
    /// ## Stability contract (EXT-01-adjacent)
    /// Phase 1 defines the minimal surface. Phase 3 ADDS methods; it does not modify or remove
    /// existing ones. Breaking changes require a mach-service-name bump to avoid silent drift.
    @objc public protocol ShokiRunnerProtocol {
        /// Liveness check. Returns `"shoki-runner-pong"` when the service is reachable.
        func ping(reply: @escaping (String) -> Void)

        // Phase 3 additions (not implemented here):
        //   func startVoiceOver(options: [String: Any], reply: @escaping (Error?) -> Void)
        //   func stopVoiceOver(reply: @escaping (Error?) -> Void)
        //   func getLastPhrase(reply: @escaping (String?) -> Void)
    }

    /// Mach service name. Do not hardcode this string elsewhere — import it from this module.
    public let ShokiRunnerMachServiceName = "org.shoki.runner"

Create `helper/Sources/ShokiRunnerService/ShokiRunnerService.swift`:

    import Foundation
    import ShokiRunnerProtocol

    /// Concrete implementation of ShokiRunnerProtocol. Exported over NSXPCConnection by
    /// ShokiRunner's NSXPCListener delegate.
    public final class ShokiRunnerService: NSObject, ShokiRunnerProtocol {
        public override init() {
            super.init()
        }

        public func ping(reply: @escaping (String) -> Void) {
            reply("shoki-runner-pong")
        }
    }

    /// NSXPCListenerDelegate that wires ShokiRunnerService as the exported interface
    /// for every incoming connection. Kept here (not in main.swift) so the test target
    /// can reuse it in-process.
    public final class ShokiRunnerListenerDelegate: NSObject, NSXPCListenerDelegate {
        public override init() {
            super.init()
        }

        public func listener(
            _ listener: NSXPCListener,
            shouldAcceptNewConnection newConnection: NSXPCConnection
        ) -> Bool {
            newConnection.exportedInterface = NSXPCInterface(with: ShokiRunnerProtocol.self)
            newConnection.exportedObject = ShokiRunnerService()
            newConnection.resume()
            return true
        }
    }

Create `helper/Sources/ShokiRunner/main.swift`:

    import Foundation
    import ShokiRunnerProtocol
    import ShokiRunnerService

    // NSXPCListener: publish the service under the mach service name declared in Info.plist
    // (MachServices dictionary -> org.shoki.runner). The launchd job definition lives in the
    // app bundle's Info.plist; `ShokiRunner` itself just starts the listener and parks.
    let delegate = ShokiRunnerListenerDelegate()
    let listener = NSXPCListener(machServiceName: ShokiRunnerMachServiceName)
    listener.delegate = delegate
    listener.resume()

    // Phase 1: park on the run loop. Phase 3 will extend the protocol to drive VO;
    // the run loop model doesn't change.
    RunLoop.main.run()

Create `helper/Tests/ShokiRunnerTests/XPCPingTests.swift`:

    import XCTest
    import Foundation
    @testable import ShokiRunnerProtocol
    @testable import ShokiRunnerService

    final class XPCPingTests: XCTestCase {
        /// In-process NSXPCConnection ping — bypasses launchd. Verifies the protocol +
        /// service wiring without needing the full signed bundle.
        func testAnonymousListenerPing() throws {
            let delegate = ShokiRunnerListenerDelegate()
            let listener = NSXPCListener.anonymous()
            listener.delegate = delegate
            listener.resume()
            defer { listener.invalidate() }

            let connection = NSXPCConnection(listenerEndpoint: listener.endpoint)
            connection.remoteObjectInterface = NSXPCInterface(with: ShokiRunnerProtocol.self)
            connection.resume()
            defer { connection.invalidate() }

            let proxy = connection.remoteObjectProxyWithErrorHandler { error in
                XCTFail("XPC proxy error: \\(error)")
            } as? ShokiRunnerProtocol

            XCTAssertNotNil(proxy)

            let expectation = self.expectation(description: "ping reply")
            proxy?.ping { reply in
                XCTAssertEqual(reply, "shoki-runner-pong")
                expectation.fulfill()
            }

            wait(for: [expectation], timeout: 5.0)
        }

        func testServiceDirectCall() {
            let service = ShokiRunnerService()
            let expectation = self.expectation(description: "direct reply")
            service.ping { reply in
                XCTAssertEqual(reply, "shoki-runner-pong")
                expectation.fulfill()
            }
            wait(for: [expectation], timeout: 1.0)
        }
    }
  </action>
  <verify>
    <automated>test -f helper/Package.swift && test -f helper/Sources/ShokiRunner/main.swift && test -f helper/Sources/ShokiRunnerService/ShokiRunnerService.swift && test -f helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift && test -f helper/Tests/ShokiRunnerTests/XPCPingTests.swift && grep -q "NSXPCListener" helper/Sources/ShokiRunner/main.swift && grep -q "@objc public protocol ShokiRunnerProtocol" helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift && grep -q "shoki-runner-pong" helper/Sources/ShokiRunnerService/ShokiRunnerService.swift && grep -q "org.shoki.runner" helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift && (command -v swift >/dev/null && cd helper && swift build -c release 2>&1 | tail -5 || echo "swift not installed in CI — defer to Plan 05 mac runner")</automated>
  </verify>
  <acceptance_criteria>
    - `helper/Package.swift` declares one executable target (`ShokiRunner`) and two library targets (`ShokiRunnerProtocol`, `ShokiRunnerService`)
    - `helper/Sources/ShokiRunnerProtocol/ShokiRunnerProtocol.swift` defines `@objc public protocol ShokiRunnerProtocol` with exactly one method in Phase 1 (`ping(reply:)`)
    - `ShokiRunnerMachServiceName` constant equals `"org.shoki.runner"` in a single location (imported elsewhere, not duplicated)
    - `helper/Sources/ShokiRunnerService/ShokiRunnerService.swift` `ShokiRunnerService.ping` calls `reply("shoki-runner-pong")`
    - `helper/Sources/ShokiRunner/main.swift` creates `NSXPCListener(machServiceName: ShokiRunnerMachServiceName)` and calls `RunLoop.main.run()`
    - When Swift is installed: `cd helper && swift build -c release` succeeds, and `swift test` runs `XPCPingTests` passing both test cases
    - When Swift is NOT installed in the execution environment: Plan 05's CI mac runner catches this (the workflow runs `swift test` before signing)
  </acceptance_criteria>
  <done>Swift package builds. In-process XPC ping round-trips. Protocol surface is frozen for Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Info.plist, entitlements, bundle + sign/notarize scripts</name>
  <read_first>
    - Previous task output (Swift package exists)
    - .planning/phases/01-foundations/01-CONTEXT.md CI Release Pipeline section (D-09)
    - .planning/research/PITFALLS.md Pitfall 4 (Developer ID signing)
    - Apple docs: notarytool --help, codesign --help (reference)
  </read_first>
  <files>
    helper/Sources/ShokiRunner/Info.plist
    helper/Sources/ShokiRunner/ShokiRunner.entitlements
    helper/scripts/build-app-bundle.sh
    helper/scripts/sign-and-notarize.sh
  </files>
  <action>
Create the app bundle metadata and the scripts that produce + sign the bundle. Scripts are invoked by Plan 05's CI workflow.

Create `helper/Sources/ShokiRunner/Info.plist`:

    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>CFBundleIdentifier</key>
        <string>org.shoki.runner</string>
        <key>CFBundleName</key>
        <string>ShokiRunner</string>
        <key>CFBundleDisplayName</key>
        <string>Shoki Runner</string>
        <key>CFBundleExecutable</key>
        <string>ShokiRunner</string>
        <key>CFBundleIconFile</key>
        <string></string>
        <key>CFBundleInfoDictionaryVersion</key>
        <string>6.0</string>
        <key>CFBundlePackageType</key>
        <string>APPL</string>
        <key>CFBundleShortVersionString</key>
        <string>0.0.0</string>
        <key>CFBundleVersion</key>
        <string>1</string>
        <key>LSMinimumSystemVersion</key>
        <string>13.0</string>
        <key>LSUIElement</key>
        <true/>
        <key>NSHumanReadableCopyright</key>
        <string>Copyright (c) 2026 Shoki contributors. MIT licensed.</string>
        <key>MachServices</key>
        <dict>
            <key>org.shoki.runner</key>
            <true/>
        </dict>
        <key>NSAccessibilityUsageDescription</key>
        <string>ShokiRunner uses Accessibility APIs to observe VoiceOver announcements during automated tests.</string>
        <key>NSAppleEventsUsageDescription</key>
        <string>ShokiRunner sends AppleScript commands to VoiceOver to retrieve spoken phrases during automated tests.</string>
    </dict>
    </plist>

Create `helper/Sources/ShokiRunner/ShokiRunner.entitlements`:

    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <!-- Hardened runtime required for notarytool acceptance. -->
        <key>com.apple.security.cs.allow-jit</key>
        <false/>
        <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
        <false/>
        <key>com.apple.security.cs.disable-library-validation</key>
        <false/>
        <!-- Allow sending Apple Events (needed for VoiceOver AppleScript in Phase 3). -->
        <key>com.apple.security.automation.apple-events</key>
        <true/>
    </dict>
    </plist>

Create `helper/scripts/build-app-bundle.sh`:

    #!/usr/bin/env bash
    # Build ShokiRunner.app from the SPM executable.
    # Usage: ./scripts/build-app-bundle.sh [--configuration release|debug]
    #
    # Runs from the helper/ directory. Produces ./ShokiRunner.app.
    set -euo pipefail

    CONFIG="release"
    while [[ $# -gt 0 ]]; do
        case "$1" in
            --configuration) CONFIG="$2"; shift 2 ;;
            *) echo "Unknown arg: $1" >&2; exit 2 ;;
        esac
    done

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    HELPER_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
    cd "$HELPER_DIR"

    echo "[build-app-bundle] Building Swift package (configuration=$CONFIG)"
    swift build -c "$CONFIG"

    BIN_PATH="$(swift build -c "$CONFIG" --show-bin-path)/ShokiRunner"
    if [[ ! -x "$BIN_PATH" ]]; then
        echo "error: executable not found at $BIN_PATH" >&2
        exit 1
    fi

    APP_DIR="$HELPER_DIR/ShokiRunner.app"
    rm -rf "$APP_DIR"
    mkdir -p "$APP_DIR/Contents/MacOS"
    cp "$BIN_PATH" "$APP_DIR/Contents/MacOS/ShokiRunner"
    cp "$HELPER_DIR/Sources/ShokiRunner/Info.plist" "$APP_DIR/Contents/Info.plist"

    echo "[build-app-bundle] Bundle ready at $APP_DIR"

Create `helper/scripts/sign-and-notarize.sh`:

    #!/usr/bin/env bash
    # Sign and notarize ShokiRunner.app.
    # Usage: ./scripts/sign-and-notarize.sh <path-to-ShokiRunner.app>
    #
    # Required env vars:
    #   APPLE_DEVELOPER_ID_APP     e.g. "Developer ID Application: Your Name (ABCDE12345)"
    #   APPLE_ID                   Apple ID email used for notarytool
    #   APPLE_TEAM_ID              10-char Apple team id
    #   APPLE_APP_SPECIFIC_PASSWORD  app-specific password for APPLE_ID
    #
    # Runs two steps:
    #   1. codesign with hardened runtime + entitlements
    #   2. notarytool submit --wait + staple
    set -euo pipefail

    if [[ $# -lt 1 ]]; then
        echo "usage: $0 <path-to-ShokiRunner.app>" >&2
        exit 2
    fi

    APP_PATH="$1"
    if [[ ! -d "$APP_PATH" ]]; then
        echo "error: app bundle not found: $APP_PATH" >&2
        exit 1
    fi

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    HELPER_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
    ENTITLEMENTS="$HELPER_DIR/Sources/ShokiRunner/ShokiRunner.entitlements"

    : "${APPLE_DEVELOPER_ID_APP:?APPLE_DEVELOPER_ID_APP must be set}"
    : "${APPLE_ID:?APPLE_ID must be set}"
    : "${APPLE_TEAM_ID:?APPLE_TEAM_ID must be set}"
    : "${APPLE_APP_SPECIFIC_PASSWORD:?APPLE_APP_SPECIFIC_PASSWORD must be set}"

    echo "[sign] Codesigning $APP_PATH with $APPLE_DEVELOPER_ID_APP"
    codesign --force \
        --options runtime \
        --entitlements "$ENTITLEMENTS" \
        --sign "$APPLE_DEVELOPER_ID_APP" \
        --timestamp \
        --deep \
        "$APP_PATH"

    echo "[sign] Verifying signature"
    codesign --verify --verbose=4 "$APP_PATH"
    codesign -dvvv "$APP_PATH" 2>&1 | tee "$HELPER_DIR/.codesign.out"

    echo "[notarize] Zipping bundle for notarytool"
    ZIP_PATH="$HELPER_DIR/ShokiRunner.zip"
    /usr/bin/ditto -c -k --keepParent "$APP_PATH" "$ZIP_PATH"

    echo "[notarize] Submitting to notarytool (wait mode)"
    xcrun notarytool submit "$ZIP_PATH" \
        --apple-id "$APPLE_ID" \
        --team-id "$APPLE_TEAM_ID" \
        --password "$APPLE_APP_SPECIFIC_PASSWORD" \
        --wait \
        --output-format json \
        | tee "$HELPER_DIR/.notarize-log.json"

    STATUS=$(node -e "const d=require('$HELPER_DIR/.notarize-log.json'); console.log(d.status || 'unknown')")
    if [[ "$STATUS" != "Accepted" ]]; then
        echo "error: notarization failed with status $STATUS" >&2
        exit 1
    fi

    echo "[notarize] Stapling"
    xcrun stapler staple "$APP_PATH"
    xcrun stapler validate "$APP_PATH"

    rm -f "$ZIP_PATH"
    echo "[done] ShokiRunner.app signed + notarized + stapled at $APP_PATH"

Make both scripts executable after creating them:

    chmod +x helper/scripts/build-app-bundle.sh
    chmod +x helper/scripts/sign-and-notarize.sh
  </action>
  <verify>
    <automated>test -f helper/Sources/ShokiRunner/Info.plist && test -f helper/Sources/ShokiRunner/ShokiRunner.entitlements && test -x helper/scripts/build-app-bundle.sh && test -x helper/scripts/sign-and-notarize.sh && grep -q "org.shoki.runner" helper/Sources/ShokiRunner/Info.plist && grep -q "LSUIElement" helper/Sources/ShokiRunner/Info.plist && grep -q "com.apple.security.automation.apple-events" helper/Sources/ShokiRunner/ShokiRunner.entitlements && grep -q "notarytool submit" helper/scripts/sign-and-notarize.sh && grep -q "codesign --force" helper/scripts/sign-and-notarize.sh && grep -q "options runtime" helper/scripts/sign-and-notarize.sh && bash -n helper/scripts/build-app-bundle.sh && bash -n helper/scripts/sign-and-notarize.sh</automated>
  </verify>
  <acceptance_criteria>
    - `helper/Sources/ShokiRunner/Info.plist` has `CFBundleIdentifier = org.shoki.runner`, `LSUIElement = true` (background app, no dock icon), `MachServices.org.shoki.runner = true`, and usage strings for Accessibility + AppleEvents
    - `helper/Sources/ShokiRunner/ShokiRunner.entitlements` enables `com.apple.security.automation.apple-events` and keeps hardened-runtime-friendly flags (no JIT, no unsigned memory, no disabled library validation)
    - `helper/scripts/build-app-bundle.sh` produces `ShokiRunner.app/Contents/MacOS/ShokiRunner` and `Contents/Info.plist`; passes `bash -n` syntax check
    - `helper/scripts/sign-and-notarize.sh` uses `codesign --options runtime --entitlements ... --timestamp` (every hardened-runtime requirement) and `xcrun notarytool submit --wait`; passes `bash -n` syntax check
    - Both scripts fail fast (`set -euo pipefail`) and check required env vars with `: "${VAR:?msg}"`
    - On a macOS runner with the secrets provided, running `scripts/build-app-bundle.sh && scripts/sign-and-notarize.sh ShokiRunner.app` produces an app where `codesign -dvvv ShokiRunner.app` outputs `Authority=Developer ID Application: ...` and `stapler validate ShokiRunner.app` reports success. Plan 05 CI exercises this.
  </acceptance_criteria>
  <done>Build and sign/notarize scripts in place. ShokiRunner.app can be produced and signed locally with a Developer ID cert, or by Plan 05's CI workflow using imported secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShokiRunner.app signature → TCC | TCC identifies process by code signature; stable signature = durable grants |
| NSXPCConnection → client | Any process can attempt to connect to mach service `org.shoki.runner`; Phase 1 ping is safe but future methods must validate senders |
| Hardened runtime → notarytool | Apple rejects binaries that opt out of hardened runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Spoofing | XPC clients connecting to `org.shoki.runner` | accept (Phase 1) | Phase 1 only exposes `ping`. No action gated on client identity yet. Phase 3 MUST validate `auditToken` / `codeSigningRequirement` before executing VoiceOver commands — noted in ShokiRunnerProtocol.swift comments. |
| T-01-15 | Tampering | Swapped/unsigned ShokiRunner.app at runtime | mitigate | Bundle is Developer ID-signed + notarized; `codesign -dvvv` in Plan 05 CI verifies before publishing; stapled ticket lets Gatekeeper verify offline. |
| T-01-16 | Repudiation | Notarization failure unnoticed | mitigate | `sign-and-notarize.sh` parses notarytool JSON output and exits non-zero if status != Accepted. CI job fails loudly. |
| T-01-17 | Information Disclosure | Apple credentials in logs | mitigate | Script reads `APPLE_APP_SPECIFIC_PASSWORD` from env; never echoes. `.notarize-log.json` is in helper/.gitignore. |
| T-01-18 | Denial of Service | NSXPCListener crash takes down helper | accept | Process supervisor (Phase 3) will relaunch; Phase 1 scope is "does it start and respond once." |
| T-01-19 | Elevation of Privilege | Hardened runtime bypass via entitlements | mitigate | Entitlements file explicitly disables `allow-jit`, `allow-unsigned-executable-memory`, and `disable-library-validation`. Only enables `com.apple.security.automation.apple-events` (required for Phase 3 VO AppleScript). |
</threat_model>

<verification>
- `swift test` in `helper/` passes both `XPCPingTests` cases (anonymous listener round-trip + direct service call)
- `helper/scripts/build-app-bundle.sh` produces a valid `.app` with the correct Info.plist
- `helper/scripts/sign-and-notarize.sh` with a real Developer ID cert (only in Plan 05 CI) produces a bundle where `codesign -dvvv` shows the Authority line
- Info.plist `CFBundleIdentifier` matches the mach service name (`org.shoki.runner`)
- Protocol surface in Phase 1 is exactly one method (`ping`) — any drift from this requires updating this plan's must_haves
</verification>

<success_criteria>
- [ ] FOUND-04: Signed-wrapper-app architecture implemented — ShokiRunner.app exists with bundle id org.shoki.runner and an NSXPCConnection endpoint
- [ ] FOUND-03: Scripts to Developer ID-sign + notarize exist; Plan 05 CI will exercise them
- [ ] XPC protocol is factored in its own module so Phase 3 can extend without touching service or main
- [ ] Hardened-runtime-friendly entitlements (no JIT, no unsigned memory, no disabled library validation); only Apple-Events entitlement enabled
- [ ] Info.plist declares usage strings for Accessibility and AppleEvents (required for TCC prompts in Phase 3)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-04-SUMMARY.md` documenting:
- Confirmed bundle layout (Contents/Info.plist, Contents/MacOS/ShokiRunner)
- `swift test` output (pass/fail counts)
- Which Developer ID cert flow the team decided on (github-actions/import-codesign-certs vs alternative)
- Any required secrets for Plan 05 CI (list names, never values)
- Gotchas discovered (e.g., if `LSUIElement` + `MachServices` interact in an unexpected way on macOS 26)
</output>

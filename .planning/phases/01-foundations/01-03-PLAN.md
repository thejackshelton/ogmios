---
phase: 01-foundations
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/sdk/src/index.ts
  - packages/sdk/src/binding-loader.ts
  - packages/sdk/src/screen-reader.ts
  - packages/sdk/src/driver-handle.ts
  - packages/sdk/src/wire.ts
  - packages/sdk/src/voice-over.ts
  - packages/sdk/src/errors.ts
  - packages/sdk/src/native-types.ts
  - packages/sdk/test/ping.test.ts
  - packages/sdk/test/binding-loader.test.ts
  - packages/sdk/test/wire.test.ts
  - packages/sdk/test/noop-roundtrip.test.ts
  - packages/sdk/vitest.config.ts
  - packages/sdk/package.json
autonomous: true
requirements: [FOUND-01, FOUND-02, EXT-01]
must_haves:
  truths:
    - "require('@shoki/sdk') resolves without errors on darwin-arm64 and darwin-x64"
    - "sdk.ping() returns 'pong' via the native addon"
    - "sdk.wireVersion() returns 1 (matches Zig WIRE_VERSION)"
    - "A ScreenReaderHandle for the 'noop' driver can be constructed, start/stop/drain/reset called, and deinit cleans up"
    - "drain() returns an array of structured events decoded from the wire format"
    - "On unsupported platform, binding-loader throws a clear error naming the expected @shoki/binding-* package"
    - "voiceOver factory exists and, in Phase 1, is wired to the noop driver as a stub (real driver ships Phase 3)"
  artifacts:
    - path: "packages/sdk/src/index.ts"
      provides: "public SDK entry — exports ScreenReaderHandle, voiceOver factory, ping/version"
      exports: ["voiceOver", "ping", "version", "wireVersion", "ScreenReaderHandle", "ShokiEvent"]
    - path: "packages/sdk/src/binding-loader.ts"
      provides: "platform/arch detection + dynamic require of the right binding package"
      contains: "process.platform"
    - path: "packages/sdk/src/screen-reader.ts"
      provides: "ScreenReaderHandle TS interface — mirrors Zig ShokiDriver vtable"
      contains: "interface ScreenReaderHandle"
    - path: "packages/sdk/src/driver-handle.ts"
      provides: "generic factory that wraps a Zig driver id into a ScreenReaderHandle"
      contains: "createDriverHandle"
    - path: "packages/sdk/src/wire.ts"
      provides: "TS-side decoder for the Zig wire format (WIRE_VERSION=1)"
      contains: "decodeEvents"
    - path: "packages/sdk/src/voice-over.ts"
      provides: "voiceOver factory — Phase 1 stub returning a noop-backed handle"
      contains: "voiceOver"
    - path: "packages/sdk/test/ping.test.ts"
      provides: "round-trip binding.ping() test"
    - path: "packages/sdk/test/noop-roundtrip.test.ts"
      provides: "end-to-end noop driver test through ScreenReaderHandle"
  key_links:
    - from: "packages/sdk/src/index.ts"
      to: "packages/sdk/src/binding-loader.ts"
      via: "loads platform-specific .node"
      pattern: "binding-loader"
    - from: "packages/sdk/src/binding-loader.ts"
      to: "packages/binding-darwin-arm64/shoki.node + packages/binding-darwin-x64/shoki.node"
      via: "require('@shoki/binding-' + platform + '-' + arch)"
      pattern: "require.*@shoki/binding-"
    - from: "packages/sdk/src/driver-handle.ts"
      to: "packages/sdk/src/wire.ts"
      via: "drain() Buffer decoded to ShokiEvent[]"
      pattern: "decodeEvents"
    - from: "packages/sdk/src/voice-over.ts"
      to: "packages/sdk/src/driver-handle.ts"
      via: "Phase 1 stub: voiceOver is createDriverHandle('noop')"
      pattern: "createDriverHandle"
---

<objective>
Build the TypeScript SDK package: platform-aware binding loader, ScreenReaderHandle interface mirroring the Zig vtable, TS-side wire-format decoder, voiceOver factory (Phase 1 stub pointing at the noop driver), error types, and Vitest-based round-trip tests.

Purpose: satisfies FOUND-02 (SDK loads via optionalDependencies) and completes EXT-01's TS side (ScreenReaderHandle matches the Zig ShokiDriver). Phase 3 will swap the voiceOver factory's driver name from "noop" to "voiceover" — nothing else changes.

Output: An importable `@shoki/sdk` package whose tests pass in Node 24 against the compiled native addon from Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
@.planning/phases/01-foundations/01-02-SUMMARY.md
@zig/src/core/napi.zig
@zig/src/core/wire.zig
@zig/src/core/options.zig
@packages/sdk/package.json

<interfaces>
<!-- ScreenReaderHandle TS interface (D-11 in CONTEXT.md). Mirrors ShokiDriver vtable. -->
<!-- Method set locked: start / stop / drain / reset / listen / phraseLog / lastPhrase / clear / deinit -->

```ts
export interface ShokiEvent {
  tsNanos: bigint;
  source: 'applescript' | 'ax' | 'caption' | 'commander' | 'noop';
  flags: number;
  phrase: string;
  role?: string;
  name?: string;
}

export interface ScreenReaderHandle {
  readonly name: string;
  start(): Promise<void>;
  stop(): Promise<void>;
  drain(): Promise<ShokiEvent[]>;
  reset(): Promise<void>;
  listen(): AsyncIterable<ShokiEvent>;
  phraseLog(): Promise<string[]>;
  lastPhrase(): Promise<string | undefined>;
  clear(): Promise<void>;
  droppedCount(): Promise<bigint>;
  deinit(): Promise<void>;
}
```

<!-- Native addon shape (from zig/src/core/napi.zig, plan 02): -->
```ts
interface NativeBinding {
  ping(): string;
  version(): string;
  wireVersion(): number;
  createDriver(name: string, logBufferSize: number): bigint;
  driverStart(id: bigint): boolean;
  driverStop(id: bigint): boolean;
  driverReset(id: bigint): boolean;
  driverDrain(id: bigint): Buffer;
  driverDeinit(id: bigint): boolean;
  droppedCount(id: bigint): bigint;
}
```

<!-- Wire format (from zig/src/core/wire.zig, WIRE_VERSION=1):
     [u32 version LE][u32 count LE][entry]*
     entry = [u64 ts_nanos LE][u8 source_tag][u8 flags]
             [u16 phrase_len LE][phrase utf8]
             [u16 role_len LE][role utf8?]
             [u16 name_len LE][name utf8?] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Binding loader + native types + errors</name>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md Distribution section (D-07 optionalDependencies)
    - .planning/research/PITFALLS.md Pitfall 12 (enterprise install failures)
    - packages/sdk/package.json (from plan 01 — optionalDependencies declared)
    - zig/src/core/napi.zig (to confirm exported function names)
  </read_first>
  <files>
    packages/sdk/src/errors.ts
    packages/sdk/src/native-types.ts
    packages/sdk/src/binding-loader.ts
    packages/sdk/test/binding-loader.test.ts
  </files>
  <action>
Create the platform-aware binding loader. Goal: on import, resolve the right `@shoki/binding-*` package via `createRequire(import.meta.url)` and expose a typed `NativeBinding` interface. No postinstall, no runtime download.

Create `packages/sdk/src/errors.ts`:

    export class ShokiError extends Error {
      constructor(
        message: string,
        public readonly code: string,
      ) {
        super(message);
        this.name = 'ShokiError';
      }
    }

    export class UnsupportedPlatformError extends ShokiError {
      constructor(
        public readonly platform: string,
        public readonly arch: string,
      ) {
        super(
          `Shoki does not support platform=${platform} arch=${arch}. ` +
            `Supported in v1: darwin-arm64, darwin-x64. ` +
            `See https://github.com/shoki/shoki/blob/main/README.md for the current platform matrix.`,
          'ERR_UNSUPPORTED_PLATFORM',
        );
        this.name = 'UnsupportedPlatformError';
      }
    }

    export class BindingNotInstalledError extends ShokiError {
      constructor(public readonly expectedPackage: string) {
        super(
          `The native binding package "${expectedPackage}" is not installed. ` +
            `This usually means your package manager skipped optionalDependencies — ` +
            `check your install (npm, pnpm, yarn) and confirm ${expectedPackage} is in node_modules.`,
          'ERR_BINDING_NOT_INSTALLED',
        );
        this.name = 'BindingNotInstalledError';
      }
    }

    export class DriverNotFoundError extends ShokiError {
      constructor(public readonly driverName: string) {
        super(
          `Driver "${driverName}" is not registered in this binding. ` +
            `Phase 1 v1 ships with drivers: noop. Phase 3 adds: voiceover (darwin).`,
          'ERR_DRIVER_NOT_FOUND',
        );
        this.name = 'DriverNotFoundError';
      }
    }

Create `packages/sdk/src/native-types.ts`:

    /**
     * The N-API surface exposed by @shoki/binding-{platform}-{arch}.
     * Mirrors zig/src/core/napi.zig from Plan 02. DO NOT change without
     * updating that file and bumping the wire format version.
     */
    export interface NativeBinding {
      ping(): string;
      version(): string;
      wireVersion(): number;
      createDriver(name: string, logBufferSize: number): bigint;
      driverStart(id: bigint): boolean;
      driverStop(id: bigint): boolean;
      driverReset(id: bigint): boolean;
      driverDrain(id: bigint): Buffer;
      driverDeinit(id: bigint): boolean;
      droppedCount(id: bigint): bigint;
    }

    export const SUPPORTED_PLATFORMS = [
      { platform: 'darwin', arch: 'arm64', pkg: '@shoki/binding-darwin-arm64' },
      { platform: 'darwin', arch: 'x64', pkg: '@shoki/binding-darwin-x64' },
    ] as const;

    export type SupportedPlatform = (typeof SUPPORTED_PLATFORMS)[number];

Create `packages/sdk/src/binding-loader.ts`:

    import { createRequire } from 'node:module';
    import {
      BindingNotInstalledError,
      ShokiError,
      UnsupportedPlatformError,
    } from './errors.js';
    import { SUPPORTED_PLATFORMS, type NativeBinding } from './native-types.js';

    const require = createRequire(import.meta.url);

    /**
     * Resolve the platform binding package name for the current process.
     * Throws UnsupportedPlatformError if no matching entry exists.
     */
    export function resolveBindingPackage(
      platform: NodeJS.Platform = process.platform,
      arch: string = process.arch,
    ): string {
      const match = SUPPORTED_PLATFORMS.find(
        (entry) => entry.platform === platform && entry.arch === arch,
      );
      if (!match) throw new UnsupportedPlatformError(platform, arch);
      return match.pkg;
    }

    /**
     * Load the native binding. Resolved once per process (module-scoped cache).
     * Throws BindingNotInstalledError when the expected package is missing
     * (usually an optionalDependencies skip — see PITFALLS.md Pitfall 12).
     */
    let cached: NativeBinding | undefined;

    export function loadBinding(): NativeBinding {
      if (cached) return cached;

      const pkg = resolveBindingPackage();
      let mod: unknown;
      try {
        mod = require(pkg);
      } catch (err) {
        if (
          err instanceof Error &&
          'code' in err &&
          (err as NodeJS.ErrnoException).code === 'MODULE_NOT_FOUND'
        ) {
          throw new BindingNotInstalledError(pkg);
        }
        throw new ShokiError(
          `Failed to load native binding "${pkg}": ${(err as Error).message}`,
          'ERR_BINDING_LOAD_FAILED',
        );
      }

      // Type-narrow the binding shape. If one of these fails, the Zig/TS ABI has drifted.
      assertIsNativeBinding(mod, pkg);
      cached = mod;
      return cached;
    }

    function assertIsNativeBinding(mod: unknown, pkg: string): asserts mod is NativeBinding {
      const required: Array<keyof NativeBinding> = [
        'ping',
        'version',
        'wireVersion',
        'createDriver',
        'driverStart',
        'driverStop',
        'driverReset',
        'driverDrain',
        'driverDeinit',
        'droppedCount',
      ];
      for (const fn of required) {
        if (typeof (mod as Record<string, unknown>)[fn] !== 'function') {
          throw new ShokiError(
            `Native binding "${pkg}" is missing required export "${fn}" ` +
              `— likely an ABI drift between @shoki/sdk and ${pkg}.`,
            'ERR_BINDING_ABI_MISMATCH',
          );
        }
      }
    }

    /** Test seam: reset the module-scoped cache so subsequent loadBinding() picks up changes. */
    export function __resetBindingCacheForTests(): void {
      cached = undefined;
    }

Create `packages/sdk/test/binding-loader.test.ts`:

    import { describe, expect, it } from 'vitest';
    import {
      resolveBindingPackage,
      loadBinding,
    } from '../src/binding-loader.js';
    import { UnsupportedPlatformError } from '../src/errors.js';

    describe('resolveBindingPackage', () => {
      it('returns the darwin-arm64 package on darwin-arm64', () => {
        expect(resolveBindingPackage('darwin', 'arm64')).toBe(
          '@shoki/binding-darwin-arm64',
        );
      });

      it('returns the darwin-x64 package on darwin-x64', () => {
        expect(resolveBindingPackage('darwin', 'x64')).toBe(
          '@shoki/binding-darwin-x64',
        );
      });

      it('throws UnsupportedPlatformError on unsupported combos', () => {
        expect(() => resolveBindingPackage('linux', 'x64')).toThrow(
          UnsupportedPlatformError,
        );
        expect(() => resolveBindingPackage('win32', 'x64')).toThrow(
          UnsupportedPlatformError,
        );
      });
    });

    describe('loadBinding', () => {
      it('loads the native addon exposing the Phase 1 N-API surface', () => {
        // Runs on darwin only in CI; skips elsewhere so local non-mac devs can lint.
        if (process.platform !== 'darwin') return;
        const binding = loadBinding();
        expect(typeof binding.ping).toBe('function');
        expect(typeof binding.createDriver).toBe('function');
        expect(typeof binding.wireVersion).toBe('function');
      });
    });
  </action>
  <verify>
    <automated>test -f packages/sdk/src/errors.ts && test -f packages/sdk/src/native-types.ts && test -f packages/sdk/src/binding-loader.ts && test -f packages/sdk/test/binding-loader.test.ts && grep -q "@shoki/binding-darwin-arm64" packages/sdk/src/native-types.ts && grep -q "@shoki/binding-darwin-x64" packages/sdk/src/native-types.ts && grep -q "createRequire" packages/sdk/src/binding-loader.ts && grep -q "UnsupportedPlatformError" packages/sdk/src/errors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sdk/src/binding-loader.ts` uses `createRequire(import.meta.url)` to resolve the binding package (ESM-native, no require() hacks)
    - `packages/sdk/src/binding-loader.ts` throws `UnsupportedPlatformError` for non-darwin targets and `BindingNotInstalledError` when the package is missing
    - `packages/sdk/src/native-types.ts` lists exactly the 10 native exports from `zig/src/core/napi.zig`
    - `packages/sdk/src/errors.ts` defines `ShokiError`, `UnsupportedPlatformError`, `BindingNotInstalledError`, `DriverNotFoundError`, each with a stable `code`
    - `assertIsNativeBinding` checks every required function name — ABI drift is caught at load time
  </acceptance_criteria>
  <done>Loader can resolve + validate the native binding or throw a clear error. All types mirror the Zig contract.</done>
</task>

<task type="auto">
  <name>Task 2: ScreenReaderHandle interface + driver-handle factory + wire decoder + voiceOver stub</name>
  <read_first>
    - Previous task output (binding-loader.ts, native-types.ts, errors.ts)
    - .planning/phases/01-foundations/01-CONTEXT.md Driver Interface (D-11 ScreenReaderHandle)
    - zig/src/core/wire.zig (to mirror exact byte layout in TS decoder)
    - zig/src/core/options.zig (SourceTag enum values)
  </read_first>
  <files>
    packages/sdk/src/screen-reader.ts
    packages/sdk/src/wire.ts
    packages/sdk/src/driver-handle.ts
    packages/sdk/src/voice-over.ts
    packages/sdk/src/index.ts
    packages/sdk/test/wire.test.ts
  </files>
  <action>
Define the TS-side interface mirror of the Zig ShokiDriver vtable (D-11), a generic factory that wraps a Zig driver id, the TS wire-format decoder, and the voiceOver factory (Phase 1 stub pointing at noop).

Create `packages/sdk/src/screen-reader.ts`:

    /**
     * TS mirror of zig/src/core/driver.zig ShokiDriver vtable (CONTEXT.md D-11).
     * Every concrete screen reader (voiceOver, nvda, orca, ...) is a factory
     * returning this interface. DO NOT extend without bumping the wire format
     * and updating every driver — EXT-01 depends on stability.
     */
    export type ShokiEventSource =
      | 'applescript'
      | 'ax'
      | 'caption'
      | 'commander'
      | 'noop';

    export interface ShokiEvent {
      /** Nanoseconds since epoch; bigint to preserve precision across JS number boundary. */
      tsNanos: bigint;
      source: ShokiEventSource;
      /** Driver-defined bit flags. Current drivers: 0 = none, 1 = interrupted prior. */
      flags: number;
      phrase: string;
      role?: string;
      name?: string;
    }

    export interface ScreenReaderHandle {
      readonly name: string;
      start(): Promise<void>;
      stop(): Promise<void>;
      drain(): Promise<ShokiEvent[]>;
      reset(): Promise<void>;
      /** Async iterator yielding events as they're drained. Phase 1 polls drain at a fixed 50ms cadence. */
      listen(): AsyncIterable<ShokiEvent>;
      /** Guidepup-compat: flat string array of phrases captured so far. */
      phraseLog(): Promise<string[]>;
      lastPhrase(): Promise<string | undefined>;
      /** Clear the internal event buffer without stopping the driver. */
      clear(): Promise<void>;
      droppedCount(): Promise<bigint>;
      /** Release all driver resources. After this call, the handle is unusable. */
      deinit(): Promise<void>;
    }

Create `packages/sdk/src/wire.ts`:

    import type { ShokiEvent, ShokiEventSource } from './screen-reader.js';
    import { ShokiError } from './errors.js';

    /**
     * Mirror of zig/src/core/wire.zig WIRE_VERSION.
     * Bump in lockstep with the Zig side — a mismatch throws at decode time.
     */
    export const EXPECTED_WIRE_VERSION = 1;

    const SOURCE_TAGS: Record<number, ShokiEventSource> = {
      0: 'applescript',
      1: 'ax',
      2: 'caption',
      3: 'commander',
      255: 'noop',
    };

    /**
     * Decode the wire-format buffer produced by binding.driverDrain(id).
     * Layout (little-endian):
     *   [u32 version][u32 count][entry]*
     *   entry = [u64 ts_nanos][u8 source][u8 flags]
     *           [u16 phrase_len][phrase]
     *           [u16 role_len][role?]
     *           [u16 name_len][name?]
     */
    export function decodeEvents(buf: Buffer): ShokiEvent[] {
      if (buf.byteLength < 8) {
        throw new ShokiError(
          `Wire buffer too short: ${buf.byteLength} bytes (need at least 8 for header)`,
          'ERR_WIRE_SHORT',
        );
      }

      const version = buf.readUInt32LE(0);
      if (version !== EXPECTED_WIRE_VERSION) {
        throw new ShokiError(
          `Wire format version mismatch: binding produced ${version}, SDK expects ${EXPECTED_WIRE_VERSION}. ` +
            `Reinstall @shoki/sdk and @shoki/binding-* so their versions match.`,
          'ERR_WIRE_VERSION_MISMATCH',
        );
      }

      const count = buf.readUInt32LE(4);
      const events: ShokiEvent[] = [];
      let offset = 8;

      for (let i = 0; i < count; i++) {
        const tsNanos = buf.readBigUInt64LE(offset);
        offset += 8;
        const sourceRaw = buf.readUInt8(offset);
        offset += 1;
        const flags = buf.readUInt8(offset);
        offset += 1;
        const source = SOURCE_TAGS[sourceRaw];
        if (!source) {
          throw new ShokiError(
            `Unknown source tag ${sourceRaw} at entry ${i}; binding/SDK out of sync.`,
            'ERR_WIRE_UNKNOWN_SOURCE',
          );
        }

        const phraseLen = buf.readUInt16LE(offset);
        offset += 2;
        const phrase = buf.toString('utf8', offset, offset + phraseLen);
        offset += phraseLen;

        const roleLen = buf.readUInt16LE(offset);
        offset += 2;
        const role = roleLen > 0 ? buf.toString('utf8', offset, offset + roleLen) : undefined;
        offset += roleLen;

        const nameLen = buf.readUInt16LE(offset);
        offset += 2;
        const name = nameLen > 0 ? buf.toString('utf8', offset, offset + nameLen) : undefined;
        offset += nameLen;

        events.push({ tsNanos, source, flags, phrase, role, name });
      }

      if (offset !== buf.byteLength) {
        throw new ShokiError(
          `Wire buffer has ${buf.byteLength - offset} trailing bytes after ${count} entries — decoder/encoder disagree.`,
          'ERR_WIRE_TRAILING_BYTES',
        );
      }

      return events;
    }

Create `packages/sdk/src/driver-handle.ts`:

    import { loadBinding } from './binding-loader.js';
    import { DriverNotFoundError, ShokiError } from './errors.js';
    import type { ScreenReaderHandle, ShokiEvent } from './screen-reader.js';
    import { decodeEvents } from './wire.js';

    export interface CreateDriverHandleOptions {
      driverName: string;
      logBufferSize?: number;
      /** Polling interval for the listen() iterator. */
      pollMs?: number;
    }

    const DEFAULT_LOG_BUFFER_SIZE = 10_000;
    const DEFAULT_POLL_MS = 50;

    /**
     * Generic factory that wraps a Zig driver id in a ScreenReaderHandle.
     * Used by voiceOver (this plan), and by future drivers (nvda, orca).
     */
    export function createDriverHandle(opts: CreateDriverHandleOptions): ScreenReaderHandle {
      const binding = loadBinding();
      const logBufferSize = opts.logBufferSize ?? DEFAULT_LOG_BUFFER_SIZE;
      const pollMs = opts.pollMs ?? DEFAULT_POLL_MS;

      let id: bigint | null;
      try {
        id = binding.createDriver(opts.driverName, logBufferSize);
      } catch (err) {
        if (err instanceof Error && err.message.includes('DriverNotFound')) {
          throw new DriverNotFoundError(opts.driverName);
        }
        throw new ShokiError(
          `Failed to create driver "${opts.driverName}": ${(err as Error).message}`,
          'ERR_DRIVER_CREATE_FAILED',
        );
      }

      const phraseLog: string[] = [];
      let deinited = false;

      function assertAlive(): bigint {
        if (deinited || id === null) {
          throw new ShokiError(
            `Driver "${opts.driverName}" has been deinit'd; create a new handle.`,
            'ERR_DRIVER_DEINITED',
          );
        }
        return id;
      }

      async function drainOnce(): Promise<ShokiEvent[]> {
        const currentId = assertAlive();
        const buf = binding.driverDrain(currentId);
        const events = decodeEvents(buf);
        for (const e of events) phraseLog.push(e.phrase);
        return events;
      }

      return {
        name: opts.driverName,

        async start() {
          binding.driverStart(assertAlive());
        },

        async stop() {
          binding.driverStop(assertAlive());
        },

        async drain() {
          return drainOnce();
        },

        async reset() {
          binding.driverReset(assertAlive());
          phraseLog.length = 0;
        },

        async *listen(): AsyncGenerator<ShokiEvent> {
          while (!deinited) {
            const events = await drainOnce();
            for (const e of events) yield e;
            await new Promise((r) => setTimeout(r, pollMs));
          }
        },

        async phraseLog() {
          await drainOnce();
          return [...phraseLog];
        },

        async lastPhrase() {
          await drainOnce();
          return phraseLog[phraseLog.length - 1];
        },

        async clear() {
          binding.driverReset(assertAlive());
          phraseLog.length = 0;
        },

        async droppedCount() {
          return binding.droppedCount(assertAlive());
        },

        async deinit() {
          if (deinited) return;
          const currentId = assertAlive();
          binding.driverDeinit(currentId);
          deinited = true;
          id = null;
        },
      };
    }

Create `packages/sdk/src/voice-over.ts`:

    import { createDriverHandle } from './driver-handle.js';
    import type { ScreenReaderHandle } from './screen-reader.js';

    export interface VoiceOverOptions {
      speechRate?: number;
      mute?: boolean;
      takeOverExisting?: boolean;
      timeoutMs?: number;
      logBufferSize?: number;
    }

    /**
     * Construct a VoiceOver ScreenReaderHandle.
     *
     * PHASE 1 STATUS: this factory wires to the "noop" driver as a placeholder.
     * The real "voiceover" driver lands in Phase 3 — when it does, the ONLY
     * change here is the driverName string. The ScreenReaderHandle surface
     * stays identical (EXT-01).
     */
    export function voiceOver(opts: VoiceOverOptions = {}): ScreenReaderHandle {
      return createDriverHandle({
        // TODO(phase-3): change to 'voiceover' once the Zig driver lands.
        driverName: 'noop',
        logBufferSize: opts.logBufferSize,
      });
    }

Create `packages/sdk/src/index.ts`:

    export type {
      ScreenReaderHandle,
      ShokiEvent,
      ShokiEventSource,
    } from './screen-reader.js';
    export { voiceOver, type VoiceOverOptions } from './voice-over.js';
    export { createDriverHandle } from './driver-handle.js';
    export {
      ShokiError,
      UnsupportedPlatformError,
      BindingNotInstalledError,
      DriverNotFoundError,
    } from './errors.js';
    export { decodeEvents, EXPECTED_WIRE_VERSION } from './wire.js';

    import { loadBinding } from './binding-loader.js';

    /** binding.ping() round-trip — confirms the native addon is loaded. */
    export function ping(): string {
      return loadBinding().ping();
    }

    /** Binding package version. */
    export function version(): string {
      return loadBinding().version();
    }

    /** Current wire-format version (matches zig/src/core/wire.zig WIRE_VERSION). */
    export function wireVersion(): number {
      return loadBinding().wireVersion();
    }

Create `packages/sdk/test/wire.test.ts`:

    import { describe, expect, it } from 'vitest';
    import { decodeEvents, EXPECTED_WIRE_VERSION } from '../src/wire.js';

    /**
     * TS-only wire decoder tests. Zig-side encoder is tested in zig/test/wire_test.zig.
     * These build hand-crafted buffers to exercise every decoder path.
     */

    function encodeHeader(count: number): Buffer {
      const buf = Buffer.alloc(8);
      buf.writeUInt32LE(EXPECTED_WIRE_VERSION, 0);
      buf.writeUInt32LE(count, 4);
      return buf;
    }

    function encodeEntry(
      tsNanos: bigint,
      source: number,
      flags: number,
      phrase: string,
      role = '',
      name = '',
    ): Buffer {
      const p = Buffer.from(phrase, 'utf8');
      const r = Buffer.from(role, 'utf8');
      const n = Buffer.from(name, 'utf8');
      const buf = Buffer.alloc(8 + 1 + 1 + 2 + p.length + 2 + r.length + 2 + n.length);
      let off = 0;
      buf.writeBigUInt64LE(tsNanos, off); off += 8;
      buf.writeUInt8(source, off); off += 1;
      buf.writeUInt8(flags, off); off += 1;
      buf.writeUInt16LE(p.length, off); off += 2;
      p.copy(buf, off); off += p.length;
      buf.writeUInt16LE(r.length, off); off += 2;
      r.copy(buf, off); off += r.length;
      buf.writeUInt16LE(n.length, off); off += 2;
      n.copy(buf, off);
      return buf;
    }

    describe('decodeEvents', () => {
      it('decodes an empty buffer (count=0)', () => {
        const buf = encodeHeader(0);
        expect(decodeEvents(buf)).toEqual([]);
      });

      it('decodes a single entry without role/name', () => {
        const buf = Buffer.concat([encodeHeader(1), encodeEntry(42n, 0, 0, 'hello')]);
        const events = decodeEvents(buf);
        expect(events).toHaveLength(1);
        expect(events[0]).toMatchObject({ tsNanos: 42n, source: 'applescript', phrase: 'hello' });
        expect(events[0].role).toBeUndefined();
        expect(events[0].name).toBeUndefined();
      });

      it('decodes an entry with role and name', () => {
        const buf = Buffer.concat([
          encodeHeader(1),
          encodeEntry(100n, 1, 1, 'Submit', 'button', 'Submit button'),
        ]);
        const [event] = decodeEvents(buf);
        expect(event.source).toBe('ax');
        expect(event.flags).toBe(1);
        expect(event.role).toBe('button');
        expect(event.name).toBe('Submit button');
      });

      it('throws on wire version mismatch', () => {
        const buf = Buffer.alloc(8);
        buf.writeUInt32LE(999, 0);
        buf.writeUInt32LE(0, 4);
        expect(() => decodeEvents(buf)).toThrow(/version mismatch/i);
      });

      it('throws on unknown source tag', () => {
        const entry = encodeEntry(1n, 77, 0, 'x');
        const buf = Buffer.concat([encodeHeader(1), entry]);
        expect(() => decodeEvents(buf)).toThrow(/unknown source tag/i);
      });

      it('throws on short buffer', () => {
        expect(() => decodeEvents(Buffer.alloc(4))).toThrow(/too short/i);
      });

      it('decodes the noop source tag (255)', () => {
        const buf = Buffer.concat([encodeHeader(1), encodeEntry(1n, 255, 0, 'noop-ping')]);
        const [event] = decodeEvents(buf);
        expect(event.source).toBe('noop');
      });
    });
  </action>
  <verify>
    <automated>test -f packages/sdk/src/screen-reader.ts && test -f packages/sdk/src/wire.ts && test -f packages/sdk/src/driver-handle.ts && test -f packages/sdk/src/voice-over.ts && test -f packages/sdk/src/index.ts && test -f packages/sdk/test/wire.test.ts && grep -q "interface ScreenReaderHandle" packages/sdk/src/screen-reader.ts && grep -q "EXPECTED_WIRE_VERSION = 1" packages/sdk/src/wire.ts && grep -q "driverName: 'noop'" packages/sdk/src/voice-over.ts && grep -q "createDriverHandle" packages/sdk/src/driver-handle.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sdk/src/screen-reader.ts` defines `ScreenReaderHandle` with all 11 members (name, start, stop, drain, reset, listen, phraseLog, lastPhrase, clear, droppedCount, deinit)
    - `packages/sdk/src/wire.ts` defines `EXPECTED_WIRE_VERSION = 1` and `decodeEvents(buf)` that throws on mismatch
    - `packages/sdk/src/driver-handle.ts` exposes `createDriverHandle(opts)` returning a `ScreenReaderHandle`
    - `packages/sdk/src/voice-over.ts` calls `createDriverHandle({ driverName: 'noop' })` with a `TODO(phase-3)` comment referencing the swap
    - `packages/sdk/src/index.ts` re-exports every public symbol listed in the `must_haves.artifacts.exports` field
    - `packages/sdk/test/wire.test.ts` covers: empty count, single entry, role+name entry, version mismatch, unknown source, short buffer, noop source
  </acceptance_criteria>
  <done>The TS interface + wire decoder + voiceOver stub + public API are in place. Phase 3 changes one string (`'noop'` → `'voiceover'`) to ship the real driver.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest config + round-trip tests + SDK build config</name>
  <read_first>
    - Previous task outputs (all sdk sources exist)
    - packages/sdk/package.json (scripts.test currently a no-op placeholder — this task replaces it)
    - .planning/research/STACK.md Supporting Libraries (Vitest 3.x)
  </read_first>
  <files>
    packages/sdk/vitest.config.ts
    packages/sdk/package.json
    packages/sdk/test/ping.test.ts
    packages/sdk/test/noop-roundtrip.test.ts
  </files>
  <action>
Add Vitest config, end-to-end tests that exercise the full binding, and update `packages/sdk/package.json` to run them.

Create `packages/sdk/vitest.config.ts`:

    import { defineConfig } from 'vitest/config';

    export default defineConfig({
      test: {
        include: ['test/**/*.test.ts'],
        environment: 'node',
        // Phase 4 introduces browser mode. Phase 1 is Node-only.
        testTimeout: 10_000,
      },
    });

Create `packages/sdk/test/ping.test.ts`:

    import { describe, expect, it } from 'vitest';
    import { ping, version, wireVersion } from '../src/index.js';

    const isMac = process.platform === 'darwin';

    describe.skipIf(!isMac)('native binding round-trip', () => {
      it('ping returns "pong"', () => {
        expect(ping()).toBe('pong');
      });

      it('version returns a non-empty string', () => {
        const v = version();
        expect(typeof v).toBe('string');
        expect(v.length).toBeGreaterThan(0);
      });

      it('wireVersion matches the TS EXPECTED_WIRE_VERSION', async () => {
        const { EXPECTED_WIRE_VERSION } = await import('../src/wire.js');
        expect(wireVersion()).toBe(EXPECTED_WIRE_VERSION);
      });
    });

Create `packages/sdk/test/noop-roundtrip.test.ts`:

    import { describe, expect, it } from 'vitest';
    import { voiceOver } from '../src/index.js';

    const isMac = process.platform === 'darwin';

    describe.skipIf(!isMac)('noop driver end-to-end via voiceOver stub', () => {
      it('start/drain/stop/deinit round-trips through the native binding', async () => {
        const handle = voiceOver();
        try {
          await handle.start();
          const events = await handle.drain();
          expect(events.length).toBeGreaterThanOrEqual(1);
          const first = events[0];
          expect(first.phrase).toBe('noop-ping');
          expect(first.source).toBe('noop');
          expect(typeof first.tsNanos).toBe('bigint');
          await handle.stop();
        } finally {
          await handle.deinit();
        }
      });

      it('droppedCount returns 0n for an under-filled buffer', async () => {
        const handle = voiceOver();
        try {
          await handle.start();
          await handle.drain();
          expect(await handle.droppedCount()).toBe(0n);
        } finally {
          await handle.deinit();
        }
      });

      it('reset clears the phraseLog', async () => {
        const handle = voiceOver();
        try {
          await handle.start();
          await handle.drain();
          expect((await handle.phraseLog()).length).toBeGreaterThan(0);
          await handle.reset();
          expect(await handle.phraseLog()).toHaveLength(1); // drain() inside phraseLog() produced one new entry
        } finally {
          await handle.deinit();
        }
      });
    });

Update `packages/sdk/package.json` — REPLACE the existing file (from plan 01) with this version that adds `vitest` scripts and a test devDependency. The existing optionalDependencies and other fields MUST be preserved. Read the file first, then write the updated version:

    {
      "name": "@shoki/sdk",
      "version": "0.0.0",
      "description": "Screen-reader test automation SDK — TypeScript surface for Shoki's Zig native core",
      "license": "MIT",
      "repository": {
        "type": "git",
        "url": "git+https://github.com/shoki/shoki.git",
        "directory": "packages/sdk"
      },
      "type": "module",
      "main": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js"
        }
      },
      "files": [
        "dist/**/*.js",
        "dist/**/*.d.ts",
        "dist/**/*.d.ts.map",
        "README.md",
        "LICENSE"
      ],
      "optionalDependencies": {
        "@shoki/binding-darwin-arm64": "workspace:*",
        "@shoki/binding-darwin-x64": "workspace:*"
      },
      "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
      },
      "devDependencies": {
        "typescript": "^6.0.0",
        "vitest": "^3.0.0"
      },
      "engines": {
        "node": ">=24.0.0"
      },
      "keywords": [
        "screen-reader",
        "voiceover",
        "accessibility",
        "testing",
        "vitest"
      ],
      "publishConfig": {
        "access": "public",
        "provenance": true
      }
    }
  </action>
  <verify>
    <automated>test -f packages/sdk/vitest.config.ts && test -f packages/sdk/test/ping.test.ts && test -f packages/sdk/test/noop-roundtrip.test.ts && node -e "const p=require('./packages/sdk/package.json'); if(!p.devDependencies.vitest) throw new Error('vitest not in devDeps'); if(p.scripts.test !== 'vitest run') throw new Error('test script not vitest'); if(!p.optionalDependencies['@shoki/binding-darwin-arm64']) throw new Error('lost arm64 optDep'); if(!p.optionalDependencies['@shoki/binding-darwin-x64']) throw new Error('lost x64 optDep')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sdk/package.json` retains BOTH `optionalDependencies` from Plan 01 AND adds `vitest` as a devDependency
    - `packages/sdk/package.json` scripts.test is exactly `vitest run` (no longer the placeholder)
    - `packages/sdk/test/ping.test.ts` skips on non-darwin and asserts `ping() === 'pong'` on darwin
    - `packages/sdk/test/noop-roundtrip.test.ts` exercises start → drain → stop → deinit end-to-end via the voiceOver factory
    - When the native addon is built (Plan 02 + Plan 05 CI): `pnpm --filter @shoki/sdk test` passes on darwin-arm64 and darwin-x64
    - On non-darwin platforms, `pnpm --filter @shoki/sdk test` still runs and skips the native-binding tests cleanly (`describe.skipIf`)
  </acceptance_criteria>
  <done>Vitest config + tests + updated package.json in place. `pnpm --filter @shoki/sdk test` exits 0 on all platforms (with darwin-specific tests skipped when appropriate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → optionalDependencies resolution | `--ignore-scripts` environments may skip native pkg (handled via clear error) |
| SDK load → native addon ABI | Shape drift between zig/src/core/napi.zig and packages/sdk/src/native-types.ts |
| Wire buffer → event decode | Untrusted bytes from binding; malformed buffer must fail fast, not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | Wire format drift between Zig and TS | mitigate | `wireVersion()` N-API export + `EXPECTED_WIRE_VERSION` TS constant; `decodeEvents` throws `ERR_WIRE_VERSION_MISMATCH` if they diverge. |
| T-01-10 | Denial of Service | Malformed wire buffer → infinite loop in decoder | mitigate | `decodeEvents` validates offset progress; throws `ERR_WIRE_TRAILING_BYTES` or `ERR_WIRE_SHORT` on any inconsistency. |
| T-01-11 | Information Disclosure | Binding package name leaked via error stacks | accept | Package names are public — no secret to protect. Clear error aids debugging (PITFALLS.md Pitfall 12). |
| T-01-12 | Tampering | Re-using a deinit'd ScreenReaderHandle | mitigate | `assertAlive()` throws `ERR_DRIVER_DEINITED` after `deinit()`; handle id nulled. |
| T-01-13 | Elevation of Privilege | Arbitrary driverName passed to `createDriverHandle` | mitigate | Zig registry is comptime-only in Phase 1; unknown names return `DriverNotFound` (the error is surfaced as `DriverNotFoundError` in TS). |
</threat_model>

<verification>
- `pnpm --filter @shoki/sdk typecheck` exits 0
- `pnpm --filter @shoki/sdk test` exits 0 (native tests skip on non-darwin)
- On darwin-arm64 after Plan 02 build: `node -e "console.log(require('@shoki/sdk').ping())"` prints `pong`
- `decodeEvents` throws `ERR_WIRE_VERSION_MISMATCH` when the version byte disagrees with `EXPECTED_WIRE_VERSION`
- `voiceOver()` returns a `ScreenReaderHandle` whose methods round-trip through the native binding
</verification>

<success_criteria>
- [ ] FOUND-01: SDK exercises the napi-zig-compiled addon (ping round-trip passes on darwin)
- [ ] FOUND-02: `@shoki/sdk` resolves platform bindings via optionalDependencies; unsupported platforms get clear errors
- [ ] EXT-01: `ScreenReaderHandle` mirrors the Zig `ShokiDriver` vtable; `voiceOver` is one string swap away from the real driver
- [ ] Wire format decoder matches Zig encoder (round-trip unit tests pass)
- [ ] All SDK tests pass on darwin-{arm64,x64} and skip gracefully elsewhere
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-03-SUMMARY.md` documenting:
- Full export surface of `@shoki/sdk` (types + functions)
- napi-zig API names actually used (if they differed from the plan's placeholders)
- Vitest run output (pass/skip counts)
- Any platform-specific test behavior
- The exact TODO(phase-3) comment location in voice-over.ts so Phase 3 planner finds it instantly
</output>

---
phase: 05-ci-story
plan: "04"
name: docs-updates
type: docs
autonomous: true
wave: 2
depends_on: ["05-01", "05-03"]
requirements: []
status: complete
---

# Phase 5 Plan 04 — Docs updates

## Objective

Thread the Phase 5 deliverables into the existing docs: add a tart-image
publish section to `docs/release-setup.md`, and a CI-topology comparison
to the README.

## Tasks

1. Extend `docs/release-setup.md` with § 6 "Tart image publish":
   required tooling, secrets (`GHCR_USERNAME`, `GHCR_TOKEN`), first-time
   bootstrap steps, manual-dispatch instructions, verification recipe,
   failure-mode triage.
2. Add "CI / Running in automation" section to `README.md` with the
   4-topology comparison table and links to the reference workflows and
   `infra/tart/`.

## Verification

- Both files changed without breaking previous sections
- Links in README point to real files (tart-selfhosted.yml,
  cirrus-runners.yml, getmac.yml, github-hosted.yml, setup/action.yml,
  infra/tart/README.md)

## Requirements coverage

(No requirements directly closed here — these docs support CI-01
through CI-06 indirectly.)

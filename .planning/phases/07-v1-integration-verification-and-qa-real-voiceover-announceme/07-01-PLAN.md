---
phase: 07-v1-integration-verification-and-qa-real-voiceover-announceme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zig/build.zig
  - helper/scripts/build-app-bundle.sh
  - packages/binding-darwin-arm64/shoki.node
  - .planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md
autonomous: true
verifies:
  - "Phase 1 FOUND-01 (signed addon loads) via real build"
  - "Phase 3 CAP-* indirectly (build must succeed before any Phase 3 runtime check)"
  - "Phase 6 DOCS-01 build (docs site compiles)"
must_haves:
  truths:
    - "Zig 0.16.x is installed on the dev machine and `zig version` reports it"
    - "`zig build` in zig/ produces libshoki.dylib without errors"
    - "`zig build test` passes all 10 registered Zig unit tests"
    - "`swift build -c release` in helper/ produces libShokiXPCClient.dylib"
    - "`swift test` in helper/ passes all ShokiRunner tests"
    - "helper/scripts/build-app-bundle.sh produces ShokiRunner.app"
    - "A .node binary exists at packages/binding-darwin-arm64/shoki.node and `node -e \"require('./packages/binding-darwin-arm64/shoki.node').ping()\"` returns a string"
    - "`pnpm --filter docs build` exits 0"
    - "`npx playwright install chromium` exits 0"
  artifacts:
    - path: "zig-out/lib/libshoki.dylib"
      provides: "Zig-built shared library"
    - path: "helper/.build/release/libShokiXPCClient.dylib"
      provides: "Swift-built XPC client dylib (link target for zig build)"
    - path: "helper/ShokiRunner.app/Contents/MacOS/ShokiRunner"
      provides: "Helper app bundle executable"
    - path: "packages/binding-darwin-arm64/shoki.node"
      provides: "Compiled N-API binding"
    - path: ".planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md"
      provides: "Verbatim record of every toolchain command + its exit code / first 50 lines of output — feeds the final QA-REPORT.md"
  key_links:
    - from: "zig/build.zig"
      to: "helper/.build/release/libShokiXPCClient.dylib"
      via: "lib.addLibraryPath + linkSystemLibrary"
      pattern: "linkSystemLibrary\\(\"ShokiXPCClient\"\\)"
    - from: "packages/binding-darwin-arm64/shoki.node"
      to: "zig-out/lib/libshoki.dylib"
      via: "cp or symlink produced by build step"
      pattern: "shoki\\.node"
---

<objective>
Install the full native toolchain (Zig 0.16, Swift, Playwright's Chromium) and
run every build step the v1 stack depends on: Zig unit tests, Swift helper tests,
helper .app bundle, libShokiXPCClient.dylib, the N-API .node binary, and the
VitePress docs build. Everything downstream in Phase 7 assumes these artifacts
exist; this plan proves they do.

Purpose: Phases 1-6 verified source-grep only. Before we can run a real
VoiceOver-against-Vitest test (Plan 07-06) or fix any known stubs (Plan 07-02),
we need a proven-green toolchain. This plan is the foundation — every later
plan's first action is "ensure the artifacts from 07-01 exist and are fresh."

Output: libshoki.dylib, libShokiXPCClient.dylib, ShokiRunner.app, shoki.node,
docs/.vitepress/dist/, and a QA-TOOLCHAIN.md that records each command + its
outcome for feeding into the final QA report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/07-CONTEXT.md
@.planning/phases/03-voiceover-capture-core/03-SUMMARY.md
@.planning/phases/06-docs-v1-release-polish/06-SUMMARY.md
@zig/build.zig
@helper/Package.swift
@helper/scripts/build-app-bundle.sh

<interfaces>
<!-- Pinned build-surface facts extracted from the codebase. -->

From zig/build.zig:
- The build registers 10 test files via `inline for` (ring_buffer_test,
  wire_test, noop_driver_test, voiceover_defaults_test,
  voiceover_applescript_test, voiceover_ax_notifications_test,
  voiceover_lifecycle_test, voiceover_driver_test, wire_regression_test,
  voiceover_integration_test). `zig build test` must run all 10.
- `zig build` default step installs the shared library to `zig-out/lib/`.
- On macOS targets, lib.linkSystemLibrary("ShokiXPCClient") expects
  `../helper/.build/release/libShokiXPCClient.dylib` to exist. Plan 07-02
  fixes the path if this plan surfaces it as broken; this plan's job is only
  to discover whether it is broken.

From helper/Package.swift:
- Products: ShokiRunner (exe), ShokiRunnerProtocol (lib), ShokiRunnerService
  (lib), ShokiXPCClient (dynamic lib → libShokiXPCClient.dylib).
- Tests target: ShokiRunnerTests depends on ShokiRunnerProtocol + ShokiRunnerService.
- `swift build -c release` is the canonical build line; `swift test` runs tests.

From helper/scripts/build-app-bundle.sh:
- Produces `helper/ShokiRunner.app` from the swift build output.

From packages/binding-darwin-arm64/package.json (not currently containing a
`shoki.node`): the .node file is not committed; this plan copies it from the
zig-out/lib/libshoki.dylib (napi-zig convention is to name the shared library
with `.node` suffix for require()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install toolchain + prove every compile step exits 0</name>
  <files>zig/build.zig, helper/.build/release/libShokiXPCClient.dylib, helper/ShokiRunner.app, packages/binding-darwin-arm64/shoki.node, docs/.vitepress/dist/</files>
  <read_first>
    - zig/build.zig (all lines — the test-step list)
    - helper/Package.swift (to know the exact swift build products)
    - helper/scripts/build-app-bundle.sh (to know the bundle output path)
    - packages/binding-darwin-arm64/package.json (to confirm expected binding location)
    - .planning/phases/03-voiceover-capture-core/03-SUMMARY.md § "Known Gaps / Deferred Items" (confirms Zig is not installed locally)
    - .planning/phases/06-docs-v1-release-polish/06-SUMMARY.md § "Gaps / Notes for v1.1+" (confirms pnpm --filter docs build was never run)
  </read_first>
  <action>
  Run every command below. Append each command's full output to
  `.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md`
  using the format below. DO NOT abort on first failure — run ALL steps, record
  outcome of each, then at the end, if any failed, STOP and bubble the failure up;
  do not attempt to autonomously "fix" toolchain bugs that aren't listed in the
  known-fix catalogue below.

  --- QA-TOOLCHAIN.md format ---
  ```
  ## Step N: <command name>

  **Command:** `<exact command>`
  **Working dir:** `<pwd>`
  **Exit code:** `<0 or non-zero>`
  **First 50 lines of combined stdout+stderr:**
  ```
  <output>
  ```
  **Outcome:** PASS | FAIL | FIXED (describe fix)
  ```
  ---

  Steps (run in this order):

  1. **Install Zig 0.16.** Try `brew list zig && brew info zig | grep -E 'stable: 0\\.16'` first.
     If absent or wrong version, `brew install zig` (Homebrew currently ships 0.16.x as
     stable). If the installed version is <0.16.0, `brew uninstall zig && brew install zig`.
     Verify: `zig version` must match regex `^0\\.16\\.`. If `brew install zig` fails,
     fall back to the official tarball: download from
     `https://ziglang.org/download/0.16.0/zig-aarch64-macos-0.16.0.tar.xz`
     (on arm64; use `x86_64-macos` on x64), extract to `$HOME/.local/zig-0.16.0`,
     and prepend that to PATH for the rest of this plan's steps.

  2. **Zig build.** `cd zig && zig build` — record exit code.

  3. **Zig test.** `cd zig && zig build test` — record exit code. Must pass all
     10 test files (see frontmatter). If any individual test file fails,
     record which one in the QA-TOOLCHAIN.md outcome; that is a `FAIL` that
     Plan 07-02 or 07-03 will triage but not a reason to stop this plan.

  4. **Swift build.** `cd helper && swift build -c release` — record exit code.
     This produces `helper/.build/release/libShokiXPCClient.dylib` (the exact
     file `zig/build.zig` wants to link against). Verify with
     `test -f helper/.build/release/libShokiXPCClient.dylib`.

  5. **Swift test.** `cd helper && swift test` — record exit code.

  6. **Helper .app bundle.** `cd helper && bash scripts/build-app-bundle.sh` —
     record exit code. Verify with `test -x helper/ShokiRunner.app/Contents/MacOS/ShokiRunner`.

  7. **N-API binding .node file.** napi-zig produces a shared library at
     `zig/zig-out/lib/libshoki.dylib` (step 2's output). Copy/symlink it into
     the binding package so Node can `require()` it:
     ```
     cp zig/zig-out/lib/libshoki.dylib packages/binding-darwin-arm64/shoki.node
     ```
     Verify:
     ```
     node -e "const b = require('./packages/binding-darwin-arm64/shoki.node'); console.log(typeof b.ping === 'function' ? 'ok' : 'MISSING_PING')"
     ```
     Expected output: `ok`.

  8. **`pnpm install`** at repo root if not already done — required for docs build.

  9. **Docs build.** `pnpm --filter docs build` — record exit code. Phase 6
     SUMMARY confirms this was never run. Must produce `docs/.vitepress/dist/`
     with at least index.html.

  10. **Playwright browser install.** `cd examples/vitest-browser-react &&
      npx playwright install chromium --with-deps` — record exit code. Phase 7
      Plans 04/06 need Chromium available.

  Known-fix catalogue (applied autonomously):

  - **Zig 0.16 `@cImport` translate-c drift:** If `zig build` fails with
    a message mentioning `translate-c` or `@cImport` on a file under
    `zig/src/drivers/voiceover/`, do NOT fix here — record the exact error
    message + file + line in QA-TOOLCHAIN.md as outcome=FAIL. Plan 07-02
    owns the fix (hand-writing extern decls).

  - **Missing CF/AppKit linker flags:** If `zig build` fails with
    `ld: symbol(s) not found` for Core Foundation / AppKit symbols,
    record it and STOP — do not rewrite zig/build.zig in this plan;
    the link-path fix is Plan 07-02's scope.

  - **Homebrew sandbox / Xcode CLT:** If `brew install zig` or `swift build`
    fails with missing command-line tools, run `xcode-select --install` and
    retry once. If the second attempt still fails, record as FAIL; user
    must complete CLT install interactively.

  - **Playwright download corporate proxy:** If `npx playwright install`
    fails with a network error, retry once with `--dry-run` diagnostics;
    if still failing, record FAIL and continue.

  For each step, the QA-TOOLCHAIN.md entry is the artifact — the log is the
  primary deliverable of this plan; the dylib/bundle/.node files are secondary.
  </action>
  <verify>
    <automated>
      test -f /Users/jackshelton/dev/open-source/shoki/zig/zig-out/lib/libshoki.dylib \
        && test -f /Users/jackshelton/dev/open-source/shoki/helper/.build/release/libShokiXPCClient.dylib \
        && test -x /Users/jackshelton/dev/open-source/shoki/helper/ShokiRunner.app/Contents/MacOS/ShokiRunner \
        && test -f /Users/jackshelton/dev/open-source/shoki/packages/binding-darwin-arm64/shoki.node \
        && test -d /Users/jackshelton/dev/open-source/shoki/docs/.vitepress/dist \
        && node -e "const b = require('/Users/jackshelton/dev/open-source/shoki/packages/binding-darwin-arm64/shoki.node'); if (typeof b.ping !== 'function') process.exit(1); console.log(b.ping())" \
        && grep -q "^## Step 10" /Users/jackshelton/dev/open-source/shoki/.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md
    </automated>
  </verify>
  <done>
    All 5 artifact files exist. `b.ping()` prints a non-empty string. QA-TOOLCHAIN.md
    records all 10 steps with exit codes. Any failed step has outcome=FAIL with the
    full first-50-line log and a classification (known-fix deferred to Plan 07-02 /
    truly unknown / user-action-required).
  </done>
</task>

<task type="auto">
  <name>Task 2: Acceptance-criteria sweep — grep-verify every toolchain truth</name>
  <files>.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md</files>
  <read_first>
    - QA-TOOLCHAIN.md as written by Task 1
  </read_first>
  <action>
  Append a `## Acceptance Criteria` section to QA-TOOLCHAIN.md that maps each
  must_haves.truths entry (see frontmatter) to either PASS with evidence, or
  FAIL with the full error text from Task 1. Format:

  ```
  ## Acceptance Criteria

  | Truth | Status | Evidence |
  |-------|--------|----------|
  | Zig 0.16.x installed | PASS | `zig version` output: 0.16.0 |
  | `zig build` produces libshoki.dylib | PASS | step 2 exit=0, libshoki.dylib size N bytes |
  | `zig build test` passes 10 tests | PASS\|FAIL | step 3 output tail showing `N/N passed` |
  | `swift build -c release` ok | PASS\|FAIL | step 4 exit code |
  | `swift test` ok | PASS\|FAIL | step 5 exit code |
  | build-app-bundle.sh ok | PASS\|FAIL | step 6 exit code |
  | shoki.node loads + ping() works | PASS\|FAIL | step 7 node -e output |
  | pnpm --filter docs build ok | PASS\|FAIL | step 9 exit code + dist/index.html exists |
  | npx playwright install chromium ok | PASS\|FAIL | step 10 exit code + ~/Library/Caches/ms-playwright/chromium-* exists |
  ```

  If ALL rows are PASS → QA-TOOLCHAIN.md ends with a single line:
    `**Toolchain gate: GREEN. Downstream plans unblocked.**`

  If ANY row is FAIL → end with:
    `**Toolchain gate: RED. Failed: <row names>. See Plan 07-02 / 07-03 for triage.**`

  Do NOT commit the .node binary to git (it's a build artifact). Do commit
  QA-TOOLCHAIN.md. Add `packages/binding-darwin-arm64/shoki.node` to
  `.gitignore` if not already ignored.
  </action>
  <verify>
    <automated>
      grep -E "^\\| Zig 0\\.16\\.x installed \\| (PASS|FAIL)" /Users/jackshelton/dev/open-source/shoki/.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md \
        && grep -E "^\\*\\*Toolchain gate: (GREEN|RED)" /Users/jackshelton/dev/open-source/shoki/.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-TOOLCHAIN.md
    </automated>
  </verify>
  <done>
    QA-TOOLCHAIN.md contains an Acceptance Criteria table with all 9 truths mapped
    to PASS/FAIL, plus a terminal gate-status line. File is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homebrew tap → local machine | `brew install zig` runs a tap's install formula as the current user |
| Playwright CDN → local machine | `npx playwright install chromium` downloads a pre-built Chromium to `~/Library/Caches/ms-playwright` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Homebrew formula for zig | accept | Using the canonical `homebrew/core` tap; no third-party tap added |
| T-07-02 | Tampering | Playwright browser archive | accept | Standard vendor path; SHA verified by playwright CLI |
| T-07-03 | Elevation | `xcode-select --install` fallback | accept | Only triggered if CLT absent; user sees the standard OS dialog |
</threat_model>

<verification>
- Run `pwd && git status` before and after; no unexpected files staged.
- Run `cat QA-TOOLCHAIN.md | grep -c '## Step '` — must return 10.
- Run the automated verify in both tasks from a fresh shell to prove it's
  order-independent once the artifacts exist.
</verification>

<success_criteria>
- libshoki.dylib, libShokiXPCClient.dylib, ShokiRunner.app, shoki.node,
  docs/.vitepress/dist/ all exist on disk.
- QA-TOOLCHAIN.md records every step + acceptance row + gate-status line.
- If gate is RED, the failed rows name exactly which downstream plan (07-02
  for link path / spawner, 07-03 for API, etc.) should triage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/07-01-SUMMARY.md`
with: gate-status (GREEN|RED), artifact paths, any known-fix classifications
deferred to Plan 07-02, and a "downstream impact" note if any Wave-2 plan is
blocked by a RED row.
</output>

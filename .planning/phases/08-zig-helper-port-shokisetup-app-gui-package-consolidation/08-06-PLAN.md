---
phase: 08-zig-helper-port-shokisetup-app-gui-package-consolidation
plan: 06
type: execute
wave: 5
depends_on: [08-04, 08-05]
files_modified:
  - pnpm-workspace.yaml
  - .github/workflows/docs.yml
  - docs/package.json
  - docs/getting-started/install.md
  - docs/getting-started/vitest-quickstart.md
  - docs/guides/matchers.md
  - docs/api/cli.md
  - docs/api/matchers.md
  - docs/api/vitest.md
  - docs/index.md
  - docs/.vitepress/config.ts
  - README.md
  - CHANGELOG.md
  - packages/vitest/README.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "`pnpm-workspace.yaml` does NOT list `docs` — the file's packages array is exactly: packages/*, examples/*"
    - "`cd docs && pnpm install && pnpm build` exits 0 — docs build as a standalone project"
    - "`.github/workflows/docs.yml` invokes `cd docs && pnpm install --frozen-lockfile=false && pnpm build` (or equivalent standalone install) instead of `pnpm --filter docs build` from the root"
    - "`pnpm -r typecheck` (from repo root) exits 0 and DOES NOT traverse docs/"
    - "`pnpm -r test` (from repo root) exits 0 and DOES NOT traverse docs/ — workspace membership confirms"
    - "`grep -r '@shoki/matchers\\|@shoki/doctor' docs/` returns zero matches — install/vitest-quickstart/matchers/CLI doc pages all updated to 4-package world"
    - "Final end-to-end sanity from clean clone: `pnpm install && pnpm -r test` green, then `cd docs && pnpm install && pnpm build` green — no root-level breakage from the workspace diff"
    - "Phase-8 SUMMARY (08-SUMMARY.md) is written and captures all 6 plan SUMMARYs' outcomes"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace members: packages/*, examples/* (no docs)"
      contains: "packages/*"
    - path: "docs/package.json"
      provides: "Standalone docs package with its own devDeps (not a workspace member)"
      contains: "vitepress"
    - path: ".github/workflows/docs.yml"
      provides: "Standalone docs CI: cd docs && pnpm install && pnpm build (no root pnpm --filter)"
    - path: "docs/getting-started/install.md"
      provides: "Install instructions updated to 4-package world (no @shoki/doctor/@shoki/matchers references)"
    - path: "docs/getting-started/vitest-quickstart.md"
      provides: "Vitest quickstart updated — setup file imports @shoki/vitest/setup"
    - path: "docs/guides/matchers.md"
      provides: "Matchers usage references @shoki/sdk/matchers"
    - path: "docs/api/cli.md"
      provides: "CLI reference references @shoki/sdk (bin: shoki)"
    - path: ".planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md"
      provides: "Phase rollup: what shipped, verification matrix, any BLOCKER.md references"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "docs/"
      via: "docs removed from packages list; root `pnpm install` no longer hoists docs deps"
      pattern: "packages/\\*"
    - from: ".github/workflows/docs.yml"
      to: "docs/"
      via: "build step runs `cd docs && pnpm install && pnpm build`"
      pattern: "cd docs"
---

<objective>
Close Phase 8 with two moves:
1. **Docs out of the workspace.** Remove `docs` from `pnpm-workspace.yaml`. Update `.github/workflows/docs.yml` to run docs build as a standalone project (`cd docs && pnpm install && pnpm build`). Verify docs still build standalone AND the root workspace's `-r` commands no longer traverse docs.
2. **Doc sweep for the 4-package world.** Update user-facing docs (`docs/**/*.md`, README.md, CHANGELOG.md, package READMEs) to reflect the consolidation: no more `@shoki/matchers` or `@shoki/doctor`; install snippet uses 3 packages (sdk + binding + vitest); `shoki` CLI comes from `@shoki/sdk`.
3. **Phase rollup.** Write `08-SUMMARY.md` that captures all 6 plans' outcomes, including the verification evidence (exit codes, test counts, `otool -L`, `find *.swift`, etc.).

Purpose: CONTEXT.md scope includes "move docs out of the workspace" and notes "package consolidation changes user-facing install docs." This plan is the cleanup that makes the phase shippable. CONTEXT.md Verification Mandate: the "final end-to-end" check runs actual commands from a clean workspace state.

Output:
- `pnpm-workspace.yaml` trimmed (2 entries)
- `docs` builds standalone on this Mac and in CI
- User-facing docs reference the 4-package world only
- Phase 8 SUMMARY written
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-CONTEXT.md
@.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-01-PLAN.md
@.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-02-PLAN.md
@.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-03-PLAN.md
@.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-04-PLAN.md
@.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-05-PLAN.md
@pnpm-workspace.yaml
@.github/workflows/docs.yml
@docs/package.json
@docs/getting-started/install.md
@docs/getting-started/vitest-quickstart.md
@docs/guides/matchers.md
@docs/api/cli.md

<docs_content_rewrite_targets>
<!-- Pages that currently reference @shoki/matchers or @shoki/doctor (per grep from Plan 05 prep): -->

docs/getting-started/install.md         -> update install snippet
docs/getting-started/vitest-quickstart.md -> update setup import example
docs/guides/matchers.md                 -> matchers reference -> @shoki/sdk/matchers
docs/api/sdk.md                         -> describe new exports['./cli', './matchers']
docs/api/cli.md                         -> bin source = @shoki/sdk
docs/api/matchers.md                    -> may merge into docs/api/sdk.md's matchers section (executor discretion: keep as separate page with updated import)
docs/api/vitest.md                      -> update setup import reference
docs/index.md                           -> any landing-page install snippet
docs/.vitepress/config.ts               -> sidebar/nav may still list "Matchers API" and "CLI" — keep both pages, but ensure contents reference @shoki/sdk

Root-level:
README.md                               -> install snippet (4 packages not 7)
CHANGELOG.md                            -> ADD an entry for Phase 8: "Consolidated packages from 7 to 4 (doctor + matchers merged into sdk; docs out of workspace). Helper ported from Swift to Zig. Added ShokiSetup.app for one-click TCC setup."
packages/vitest/README.md               -> update setup import

Example repo README (if present): examples/vitest-browser-react/README.md — Plan 05 already touched imports; confirm the README's install/docs snippet also matches.

Non-rewrite targets (keep as-is):
- .planning/** (historical artifacts of prior phases; do NOT rewrite)
- CHANGELOG pre-v1.1 entries (historical)
- packages/matchers/README.md (doesn't exist anymore, file deleted in Plan 05)
- packages/doctor/README.md (same)
</docs_content_rewrite_targets>

<standalone_docs_ci>
<!-- .github/workflows/docs.yml after Plan 06: -->

name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build VitePress site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      # Docs is NOT a workspace member — install standalone.
      - name: Install docs dependencies
        working-directory: docs
        run: pnpm install
      - name: Build docs
        working-directory: docs
        run: pnpm build
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
</standalone_docs_ci>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove docs from workspace + standalone docs CI + verify root -r scope</name>
  <read_first>
    - pnpm-workspace.yaml current state
    - .github/workflows/docs.yml current state (root pnpm --filter docs)
    - docs/package.json (confirm it carries its own deps or needs adjusting)
    - The `<standalone_docs_ci>` block above (target workflow shape)
  </read_first>
  <files>pnpm-workspace.yaml, .github/workflows/docs.yml, docs/package.json</files>
  <action>
Step 1 — Update `pnpm-workspace.yaml` to drop docs:
```yaml
packages:
  - "packages/*"
  - "examples/*"
# docs/ is intentionally NOT a workspace member — it has its own pnpm install
# and runs standalone in .github/workflows/docs.yml (Plan 08-06).
```

Step 2 — Update `.github/workflows/docs.yml` to the `<standalone_docs_ci>` shape exactly.

Step 3 — Confirm `docs/package.json` is self-contained. Current state (per Read earlier): it declares `vitepress: ^1.5.0` as devDependency. If any other deps were coming from the workspace implicitly (none expected, since VitePress is the only thing required), add them explicitly. No edit expected in practice.

Step 4 — Purge any leftover docs node_modules + lockfile that was hoisted by the monorepo before:
```bash
rm -rf docs/node_modules docs/pnpm-lock.yaml
```
(The standalone `pnpm install` in Task 1's verify step will create a fresh `docs/pnpm-lock.yaml`.)

Step 5 — Refresh root workspace (drops docs from pnpm's bookkeeping):
```bash
rm -rf node_modules packages/*/node_modules examples/*/node_modules
pnpm install
```

Step 6 — Verify ON THIS MAC:
  a. `grep -E "^\\s*-\\s+\"docs\"|^\\s*-\\s+docs" pnpm-workspace.yaml` returns NO matches
  b. `python3 -c "import yaml; y = yaml.safe_load(open('pnpm-workspace.yaml')); assert 'docs' not in y['packages'], y"` exits 0
  c. `pnpm install` exits 0 at repo root
  d. `pnpm list -r --depth=-1 | grep -q "@shoki/docs"` returns exit 1 (docs not listed in workspace)
  e. `pnpm -r typecheck` exits 0 AND its output does NOT include docs (should see only the 4 @shoki packages + examples)
  f. `cd docs && pnpm install && pnpm build` exits 0; `docs/.vitepress/dist/index.html` exists
  g. Python YAML parse of docs.yml: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docs.yml'))"` exits 0
  h. `grep -q 'pnpm --filter docs' .github/workflows/docs.yml` returns exit 1 (no root-filter invocation remains)
  i. `grep -q 'cd docs' .github/workflows/docs.yml` or `grep -q 'working-directory: docs' .github/workflows/docs.yml` — at least one matches
  </action>
  <verify>
    <automated>python3 -c "import yaml; y = yaml.safe_load(open('pnpm-workspace.yaml')); assert 'docs' not in y['packages'], 'docs still in workspace'" && pnpm install && (! pnpm list -r --depth=-1 2>&1 | grep -q '@shoki/docs') && pnpm -r typecheck && (! pnpm -r typecheck 2>&1 | grep -iq 'docs') && cd docs && pnpm install && pnpm build && test -f .vitepress/dist/index.html && cd .. && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docs.yml'))" && (! grep -q 'pnpm --filter docs' .github/workflows/docs.yml) && grep -q 'working-directory: docs' .github/workflows/docs.yml && echo OK</automated>
  </verify>
  <done>
- `pnpm-workspace.yaml` lists only `packages/*` and `examples/*`
- Root `pnpm install` + `pnpm -r typecheck` + `pnpm -r test` no longer traverse docs
- `cd docs && pnpm install && pnpm build` produces `docs/.vitepress/dist/index.html`
- `.github/workflows/docs.yml` runs standalone install + build in `working-directory: docs`
- No `pnpm --filter docs` invocations remain
- YAML parses cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Docs + README + CHANGELOG sweep for 4-package world</name>
  <read_first>
    - The `<docs_content_rewrite_targets>` block above (exhaustive list)
    - Each listed file's current content (identify exact strings to replace)
    - 08-CONTEXT.md § "Package consolidation" (the 4-package final state)
  </read_first>
  <files>docs/getting-started/install.md, docs/getting-started/vitest-quickstart.md, docs/guides/matchers.md, docs/api/cli.md, docs/api/matchers.md, docs/api/sdk.md, docs/api/vitest.md, docs/index.md, docs/.vitepress/config.ts, README.md, CHANGELOG.md, packages/vitest/README.md, examples/vitest-browser-react/README.md</files>
  <action>
For each file in `<docs_content_rewrite_targets>`, perform the substitutions below. Use the Edit tool (not sed) so every rewrite is reviewable.

Canonical substitution rules (apply everywhere):

1. Install snippet:
   OLD (any form of): `pnpm add -D @shoki/sdk @shoki/vitest @shoki/matchers @shoki/doctor` (or comma-separated variants)
   NEW: `pnpm add -D @shoki/sdk @shoki/vitest`
   (Plus a note: `The binding package (@shoki/binding-darwin-arm64 or @shoki/binding-darwin-x64) is installed automatically as an optionalDependency of @shoki/sdk.`)

2. Setup import:
   OLD: `import '@shoki/matchers/setup';`
   NEW: `import '@shoki/vitest/setup';`

3. Matchers import (for docs that reference matcher functions directly):
   OLD: `import { toHaveAnnounced } from '@shoki/matchers';`
   NEW: `import { toHaveAnnounced } from '@shoki/sdk/matchers';`
   OR (if the doc is only about `expect.extend` registration): reference `@shoki/vitest/setup` instead.

4. CLI reference:
   OLD: `@shoki/doctor` as a dependency or installation
   NEW: `@shoki/sdk` (the `shoki` CLI is the `bin` entry of `@shoki/sdk`)
   OLD: `npx @shoki/doctor doctor`
   NEW: `npx shoki doctor` (no change needed if the doc already uses `npx shoki ...`, but confirm the prose does not claim doctor is a separate package)

5. Package count claims:
   OLD: "shoki ships 7 packages" / "install @shoki/sdk, @shoki/doctor, and @shoki/matchers"
   NEW: "shoki ships 4 packages" / "install @shoki/sdk and @shoki/vitest (the binding package is an optional dep)"

6. CHANGELOG.md — add a new entry at the top:
```
## [Unreleased] — Phase 8 (v1.1 prep)

### Changed
- **Package consolidation (7 → 4):** `@shoki/doctor` and `@shoki/matchers` have been merged into `@shoki/sdk`. The CLI is now exposed via `@shoki/sdk`'s `bin: { shoki }` entry. Matchers are at the subpath `@shoki/sdk/matchers`. Framework wiring (`expect.extend`) is owned by `@shoki/vitest` at the subpath `@shoki/vitest/setup`.
- **Docs out of workspace:** `docs/` is no longer a pnpm workspace member. It builds standalone via `cd docs && pnpm install && pnpm build` (CI updated).
- **Helper ported from Swift to Zig:** `helper/` is now Zig-only; the `.app` bundle is Zig-compiled. `libShokiXPCClient.dylib` is now Zig-built with the same C ABI — Zig core linkage is unchanged.
- **Added ShokiSetup.app:** A Zig-compiled, minimal GUI bundle shipped alongside `ShokiRunner.app` in each platform binding. Double-clicking it triggers the Accessibility + Automation TCC prompts on first launch — replacing the manual System Settings walk.

### Breaking
- Imports from `@shoki/matchers` must migrate to `@shoki/sdk/matchers`
- Imports from `@shoki/doctor` must migrate to `@shoki/sdk` or `@shoki/sdk/cli`
- `@shoki/matchers/setup` is now `@shoki/vitest/setup`
```

7. docs/.vitepress/config.ts sidebar/nav — confirm Matchers and CLI nav entries still point at valid paths (docs/api/matchers.md and docs/api/cli.md); the page contents changed but the structure does not need to change.

Execute:
- Edit each file. Keep the edits surgical — rewrite only the `@shoki/*` references and directly-adjacent prose.
- Each edit must preserve doc voice and surrounding structure.

Verify ON THIS MAC after edits:
  a. `grep -r '@shoki/matchers' docs/ --include='*.md' --include='*.ts'` returns zero
  b. `grep -r '@shoki/doctor' docs/ --include='*.md' --include='*.ts'` returns zero
  c. `grep '@shoki/matchers' README.md CHANGELOG.md` returns zero
  d. `grep '@shoki/doctor' README.md` returns zero  (CHANGELOG.md's new entry mentions the migration — that's fine; grep for "has been merged" or similar context to distinguish active doc vs historical note)
  e. `grep -q '@shoki/sdk/matchers' docs/getting-started/vitest-quickstart.md` OR `grep -q '@shoki/vitest/setup' docs/getting-started/vitest-quickstart.md` — at least one matches (quickstart still teaches matchers)
  f. `cd docs && pnpm build` exits 0 (Task 1 already rebuilt; this re-runs after content edits to make sure VitePress still processes every page)
  g. Visually eyeball: the rewritten install snippet lists @shoki/sdk + @shoki/vitest (2 user-installed names, plus implicit binding)
  </action>
  <verify>
    <automated>test -z "$(grep -rl '@shoki/matchers' docs/ --include='*.md' --include='*.ts' 2>/dev/null)" && test -z "$(grep -rl '@shoki/doctor' docs/ --include='*.md' --include='*.ts' 2>/dev/null)" && (! grep -q '@shoki/matchers' README.md) && (cd docs && pnpm build) && test -f docs/.vitepress/dist/index.html && echo OK</automated>
  </verify>
  <done>
- Zero `@shoki/matchers` references in docs/, README.md, packages/*/README.md
- Zero `@shoki/doctor` references in docs/ and README.md (CHANGELOG.md mentions the migration as a historical changelog entry — expected)
- `cd docs && pnpm build` exits 0 after edits
- CHANGELOG.md has a new [Unreleased] — Phase 8 entry
- Install snippets show 2 user-installed packages (sdk + vitest) with the binding as an implicit optionalDependency
  </done>
</task>

<task type="auto">
  <name>Task 3: Final end-to-end verify + write 08-SUMMARY.md</name>
  <read_first>
    - .planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-01-SUMMARY.md (if present)
    - .planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-02-SUMMARY.md (if present)
    - .planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-03-SUMMARY.md (if present)
    - .planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-04-SUMMARY.md (if present)
    - .planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-05-SUMMARY.md (if present)
    - .planning/phases/07-v1-integration-verification-and-qa-real-voiceover-announceme/QA-REPORT.md (reference format for the rollup)
    - templates/summary.md (gsd templates)
  </read_first>
  <files>.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md</files>
  <action>
Run ONE final end-to-end verify from a (simulated) clean state. Capture exit codes + stdout tails for each command. Include them verbatim in 08-SUMMARY.md.

End-to-end sequence (run in this order, record each exit code):
```bash
# 1. Helper builds both bundles + dylib
cd helper && zig build
ls -la .build/ShokiRunner.app/Contents/MacOS/ShokiRunner .build/ShokiSetup.app/Contents/MacOS/ShokiSetup .build/libShokiXPCClient.dylib
./.build/ShokiRunner.app/Contents/MacOS/ShokiRunner --version
./.build/ShokiSetup.app/Contents/MacOS/ShokiSetup --version
./.build/ShokiSetup.app/Contents/MacOS/ShokiSetup --self-test
# 2. Helper tests pass
zig build test --summary all
cd ..

# 3. No Swift anywhere
find helper -name '*.swift' | head -5      # must be empty
test ! -f helper/Package.swift              # exit 0

# 4. Zig core still links + tests pass
cd zig && zig build && zig build test --summary all
cd ..

# 5. Workspace clean + all tests
pnpm install
pnpm -r typecheck
pnpm --filter @shoki/sdk test
pnpm --filter @shoki/vitest test
pnpm --filter vitest-browser-react-example test   # non-integration path

# 6. CLI + matcher subpath work
pnpm --filter @shoki/sdk build
node packages/sdk/dist/cli/index.js --version
node -e "require('@shoki/sdk/matchers').toHaveAnnounced"

# 7. Package count
ls packages/ | sort                         # exactly: binding-darwin-arm64 binding-darwin-x64 sdk vitest

# 8. Docs standalone
cd docs && pnpm install && pnpm build
test -f .vitepress/dist/index.html
cd ..

# 9. Workspace docs exclusion
python3 -c "import yaml; y=yaml.safe_load(open('pnpm-workspace.yaml')); assert 'docs' not in y['packages']"

# 10. No deleted package refs anywhere in live code
grep -rn '@shoki/matchers\|@shoki/doctor' packages/ examples/ docs/ --include='*.ts' --include='*.tsx' --include='*.md' --include='*.json' | head -20   # must be empty
```

If any command exits non-zero, STOP. Write BLOCKER.md in the phase dir with the failing command + exit code + stderr. Do NOT proceed with 08-SUMMARY.md until unblocked.

Then write `.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md` using this structure:

```markdown
---
phase: 08-zig-helper-port-shokisetup-app-gui-package-consolidation
status: complete
completed_date: <today>
tags: [zig-port, helper, xpc, ax, tcc, setup-app, package-consolidation, docs-standalone]
plans_completed: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06]
---

# Phase 08 Summary

## One-liner
<what shipped in one sentence>

## What Shipped

### Plan 08-01 — XPC core (Zig)
- Files: ...
- Exit codes: zig build=0, zig build test=6/6 tests

### Plan 08-02 — AX observer + main + Swift deletion
- find helper -name '*.swift' -> empty
- ShokiRunner --version -> 0
- timeout 2 ShokiRunner -> 124 (runloop alive)
- otool -L libShokiXPCClient.dylib -> no Swift runtime
- nm shows N shoki_xpc_* exports
- zig core tests still pass (87/87)

### Plan 08-03 — ShokiSetup.app
- ShokiSetup --version / --self-test / --bogus exit codes
- Human-verify result

### Plan 08-04 — CI rewiring
- Diff summary of ci.yml / release.yml / action.yml
- YAML validity confirmed

### Plan 08-05 — Package consolidation
- Pre-move test counts: doctor=N1, matchers=19, sdk=57
- Post-move test count on @shoki/sdk: >= sum
- packages/ dir listing: binding-darwin-arm64 binding-darwin-x64 sdk vitest

### Plan 08-06 — Docs out + final verify
- pnpm-workspace.yaml diff (docs removed)
- cd docs && pnpm build exit code
- Final end-to-end sequence (10 commands above, verbatim with exit codes)

## Verification Matrix

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `find helper -name '*.swift'` empty | PASS | <command output> |
| 2 | `./ShokiRunner --version` exits 0 | PASS | ... |
| 3 | `./ShokiSetup --self-test` exits 0 | PASS | ... |
| 4 | `packages/` has exactly 4 dirs | PASS | ... |
| 5 | `pnpm -r test` all green | PASS | ... |
| 6 | `cd docs && pnpm build` exits 0 | PASS | ... |
| 7 | No `@shoki/matchers|@shoki/doctor` refs in live code | PASS | ... |
| 8 | CI smoke job launches both bundles | PASS (definition) | ci.yml excerpt |
| 9 | Release signs + ships both bundles | PASS (definition) | release.yml excerpt |
| 10 | ShokiSetup.app triggers TCC prompts on manual launch | <from Plan 03 Task 3 checkpoint> | human-verify result |

## Blockers Hit
<list with links to BLOCKER.md if any was produced during the phase>

## Deviations from Plans
<anything that had to change during execution vs what the plan specified>

## Follow-ups for Next Phase
- Phase 9 (Qwik example + docs switch from React) — unblocked by the 4-package world in Plan 08-05
- Any remaining lint/format cleanup not covered here
```

Fill in real command outputs. Commit the SUMMARY.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md && cd helper && zig build && ./.build/ShokiRunner.app/Contents/MacOS/ShokiRunner --version && ./.build/ShokiSetup.app/Contents/MacOS/ShokiSetup --self-test && zig build test --summary all && cd ../zig && zig build && zig build test --summary all && cd .. && pnpm install && pnpm -r typecheck && pnpm --filter @shoki/sdk test && pnpm --filter @shoki/vitest test && cd docs && pnpm install && pnpm build && test -f .vitepress/dist/index.html && cd .. && test -z "$(grep -rl '@shoki/matchers\|@shoki/doctor' packages/ examples/ docs/ --include='*.ts' --include='*.tsx' --include='*.md' --include='*.json' 2>/dev/null)" && echo OK</automated>
  </verify>
  <done>
- 08-SUMMARY.md exists in the phase dir, committed
- Every truth in the phase frontmatter's must_haves (across all 6 plans) is re-verified and recorded in the SUMMARY
- End-to-end sequence ran clean (all 10 commands exit 0) OR a BLOCKER.md was written with specifics
- Final grep for `@shoki/matchers`/`@shoki/doctor` across live code trees returns empty
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify — phase close</name>
  <files>.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md</files>
  <action>
HUMAN CHECKPOINT. Do NOT automate. Hand off to the user and wait for their response.

Phase 8 as a whole:
- Swift helper deleted, Zig-only helper shipping in both arch's binding packages
- ShokiSetup.app bundled alongside ShokiRunner.app
- Packages consolidated 7 → 4 (sdk+vitest+2 bindings); docs standalone
- CI + release workflows updated (no Swift; both bundles signed); CI smoke launches both bundles
- Docs rewritten for the 4-package world; 08-SUMMARY.md committed

Present the following spot-check protocol to the user; wait for explicit approval before closing the phase.

Spot-check the end-to-end promise of this phase:

1. From a shell at repo root: `ls packages/` — confirm exactly four directories (binding-darwin-arm64, binding-darwin-x64, sdk, vitest).
2. `find helper -name '*.swift' | head` — confirm empty output.
3. `file helper/.build/ShokiRunner.app/Contents/MacOS/ShokiRunner` — Mach-O executable, arm64.
4. `helper/.build/ShokiRunner.app/Contents/MacOS/ShokiRunner --version` — prints "ShokiRunner X.Y.Z (zig-compiled)".
5. `helper/.build/ShokiSetup.app/Contents/MacOS/ShokiSetup --version` — same pattern.
6. `pnpm install && pnpm -r test` — all green.
7. `cd docs && pnpm install && pnpm build && cd ..` — exits 0, produces `docs/.vitepress/dist/index.html`.
8. (Optional) Open `helper/.build/ShokiSetup.app` via Finder double-click — does macOS show an Accessibility/Automation TCC dialog? This is the whole point of the plan. (If Plan 03 Task 3 already approved this, re-verification is optional.)
9. Read `.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md` — does the Verification Matrix cover all 10 truths with real evidence (command outputs, not "looks good")?

Resume signal: Type "phase 8 approved" to close, or describe any regressions step-by-step.
  </action>
  <verify>
Manual verification — no automated command. User's approval text is the verification artifact. Executor records it verbatim in 08-SUMMARY.md § "Phase-close approval".
  </verify>
  <done>
- User typed "phase 8 approved" OR described specific regressions (each regression opens a BLOCKER.md or follow-up plan)
- Approval text recorded in 08-SUMMARY.md § "Phase-close approval"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root workspace manager -> docs | With docs out of the workspace, `pnpm -r` commands no longer implicitly install or test docs. A user who adds a TS-typed docs config will not get it typechecked by `pnpm -r typecheck`. |
| User-facing docs -> user install commands | If the install snippet in docs/getting-started/install.md still says `@shoki/matchers`, users will get `ERR_PNPM_NO_MATCHING_VERSION` on `pnpm add`. |
| CHANGELOG entries -> SemVer promises | Breaking changes must be called out prominently. Plan 06 adds the entry with a "Breaking" section. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-25 | R (Repudiation) | Docs silently stale | mitigate | Task 2's grep gates (`grep -r '@shoki/matchers' docs/`) catch un-rewritten snippets; CHANGELOG entry is mandatory. |
| T-08-26 | I (Info Disclosure) | docs/ accidentally published to npm | accept | Docs was never an npm-published package (private: true in docs/package.json). Workspace removal does not change that. |
| T-08-27 | D (DoS) | Root `pnpm install` slower if docs pulled back in | N/A | Opposite — removing docs from the workspace reduces the root graph. |
| T-08-28 | T (Tampering) | Sidebar nav in vitepress references deleted pages | mitigate | `cd docs && pnpm build` in the verify step catches any broken internal link that VitePress can statically resolve. |
</threat_model>

<verification>
Final end-to-end on this Mac (all must exit 0 — see Task 3 action for the full sequence):
1. `cd helper && zig build && zig build test --summary all`
2. `./helper/.build/ShokiRunner.app/Contents/MacOS/ShokiRunner --version`
3. `./helper/.build/ShokiSetup.app/Contents/MacOS/ShokiSetup --self-test`
4. `find helper -name '*.swift'` (empty)
5. `cd zig && zig build && zig build test --summary all`
6. `pnpm install && pnpm -r typecheck && pnpm -r test`
7. `node packages/sdk/dist/cli/index.js --version`
8. `node -e "require('@shoki/sdk/matchers').toHaveAnnounced"`
9. `cd docs && pnpm install && pnpm build && test -f .vitepress/dist/index.html`
10. `ls packages/` == binding-darwin-arm64, binding-darwin-x64, sdk, vitest
11. No `@shoki/matchers` / `@shoki/doctor` in live code trees
12. `08-SUMMARY.md` exists and captures all evidence
</verification>

<success_criteria>
- `pnpm-workspace.yaml` has exactly two packages entries (packages/*, examples/*)
- `docs/` builds standalone (local + CI)
- User-facing docs (docs/, README.md, packages/*/README.md) are fully migrated to the 4-package world
- CHANGELOG.md has a new Phase 8 entry including a "Breaking" section
- 08-SUMMARY.md documents every plan's outcome with real evidence (exit codes, test counts, command outputs)
- Human-verify checkpoint approved
</success_criteria>

<output>
After completion:
- `.planning/phases/08-zig-helper-port-shokisetup-app-gui-package-consolidation/08-SUMMARY.md` is committed
- Plans 08-01..08-06 all marked complete in ROADMAP.md (if the orchestrator handles this) or left to `/gsd-complete-phase 8`
- If any BLOCKER.md exists, link it in 08-SUMMARY.md § "Blockers Hit" with its status
</output>
